/**
 * @deprecated Use HeaderMarket directly. This alias remains temporarily for
 * downstream imports while all market changes use the shared storefront context.
 */
export { HeaderMarket as MarketSwitcher } from './header-market';
