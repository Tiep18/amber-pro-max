---
phase: 05-fulfillment-and-purchase-access
plan: "09"
type: execute
wave: 5
depends_on: ["05-04", "05-06"]
files_modified:
  - src/fulfillment/physical.ts
  - src/fulfillment/email-outbox.ts
  - src/emails/transactional.ts
  - src/components/admin/fulfillment/physical-fulfillment-form.tsx
  - src/components/admin/orders/order-detail.tsx
  - src/components/admin/orders/order-queue.tsx
  - tests/unit/fulfillment/physical.test.ts
  - tests/e2e/admin-fulfillment.spec.ts
autonomous: true
requirements: [FUL-02, FUL-03]
requirements_addressed: [FUL-02, FUL-03]
user_setup: []
must_haves:
  truths:
    - "D-14/FUL-02 admin can advance physical status through awaiting_fulfillment, packing, shipped, delivered."
    - "D-15 shipped status permits optional carrier and tracking without blocking shipment."
    - "D-17/FUL-03 shipped queues a required shipping update email; delivered email is optional."
  artifacts:
    - path: "src/fulfillment/physical.ts"
      provides: "admin physical fulfillment transition action and validation"
    - path: "src/components/admin/fulfillment/physical-fulfillment-form.tsx"
      provides: "admin status, carrier, tracking, URL, and note form"
  key_links:
    - from: "src/fulfillment/physical.ts"
      to: "transactional_email_outbox"
      via: "shipped transition enqueues shipping_update email"
      pattern: "shipping_update"
---

<objective>
Implement admin physical fulfillment status, tracking, and shipping email enqueue.

Purpose: Handmade shipments remain manual, but status changes must be authorized, auditable, and visible without changing digital access rules.
Output: Physical fulfillment server action, admin form, email rendering updates, queue integration, and tests.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-04-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-06-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-UI-SPEC.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@src/payments/admin-actions.ts
@src/components/admin/orders/order-detail.tsx
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing physical transition and notification contracts</name>
  <files>tests/unit/fulfillment/physical.test.ts, tests/e2e/admin-fulfillment.spec.ts</files>
  <behavior>
    - Valid status flow succeeds; backwards, impossible, stale, and non-admin transitions fail.
    - Shipped accepts missing carrier/tracking, validates `https://` tracking URL when present, and normalizes fields.
    - Shipped queues one required shipping email; delivered email behavior is optional and explicit.
  </behavior>
  <action>Add unit and Playwright contracts before implementation. Cover admin status update, shipped without tracking, shipped with tracking URL, invalid tracking URL, delivered state, stale state, non-admin denial, and outbox email enqueue.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/physical.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list</automated></verify>
  <done>Contracts fail only because physical action/form/email wiring is absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement admin physical action and form</name>
  <files>src/fulfillment/physical.ts, src/fulfillment/email-outbox.ts, src/emails/transactional.ts, src/components/admin/fulfillment/physical-fulfillment-form.tsx, src/components/admin/orders/order-detail.tsx, src/components/admin/orders/order-queue.tsx, tests/unit/fulfillment/physical.test.ts, tests/e2e/admin-fulfillment.spec.ts</files>
  <behavior>
    - Server action requires `requireAdmin`, loads current state, validates next state, rejects stale submissions, and calls the 05-01 physical RPC.
    - Shipped enqueues `shipping_update` through the 05-04 outbox and includes customer-safe tracking facts only.
    - Admin UI shows current status, valid next-state controls, optional tracking fields, helper copy, result state, and disabled pending controls.
  </behavior>
  <action>Implement `updatePhysicalFulfillmentAction` with Zod validation and typed result codes. Extend email rendering/outbox payload support for shipped and delivered notices. Build `PhysicalFulfillmentForm` in admin order detail and add compact queue indicators or filters as needed. Admin note metadata must pass the unsafe metadata guard and must not render on customer pages.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/physical.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list && npm run test:security && npm run typecheck</automated></verify>
  <done>Admins can update manual physical status and optional tracking, shipped queues email, stale or invalid transitions fail safely, and admin UI records the outcome.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | physical transition | mitigate | Allowed flow, current-state load, expected-state RPC checks, stale tests. |
| T-05-09-02 | Elevation of Privilege | physical action | mitigate | `requireAdmin`, database admin check, non-admin tests. |
| T-05-09-03 | Information Disclosure | tracking/admin notes | mitigate | Customer-safe tracking fields only; admin note remains admin-only. |
| T-05-09-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run physical unit, admin browser listing, security, and typecheck. The SUMMARY must record shipped-without-tracking evidence and shipped email outbox dedupe behavior.
</verification>

<success_criteria>
Admins can advance physical fulfillment and tracking through the locked manual flow, with required shipped email enqueue and stale-state protection.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-09-SUMMARY.md` when done.</output>