---
phase: 05-fulfillment-and-purchase-access
plan: "06"
type: execute
wave: 4
depends_on: ["05-02", "05-05"]
files_modified:
  - src/fulfillment/entitlements.ts
  - src/fulfillment/admin-entitlement-actions.ts
  - src/components/admin/fulfillment/entitlement-actions.tsx
  - src/components/admin/fulfillment/entitlement-audit-list.tsx
  - src/components/admin/orders/order-detail.tsx
  - src/payments/queries.ts
  - tests/unit/fulfillment/downloads.test.ts
  - tests/e2e/admin-fulfillment.spec.ts
  - tests/security/fulfillment-boundaries.test.mjs
autonomous: true
requirements: [DIG-07]
requirements_addressed: [DIG-07]
user_setup: []
must_haves:
  truths:
    - "D-12/DIG-07 admin can resend, revoke, and reissue digital access with audit rows."
    - "Resolved research uses explicit revoked/reissued entitlement transitions and append-only audit events."
    - "Reissue creates fresh token/link/outbox work and never mutates expired or public links."
    - "Stale admin actions fail safely when entitlement state changed after render."
  artifacts:
    - path: "src/fulfillment/entitlements.ts"
      provides: "RPC wrappers for entitlement revoke and reissue"
    - path: "src/fulfillment/admin-entitlement-actions.ts"
      provides: "admin server actions for revoke/reissue"
    - path: "src/components/admin/fulfillment/entitlement-actions.tsx"
      provides: "admin UI controls with confirmations and stale-state feedback"
  key_links:
    - from: "src/fulfillment/admin-entitlement-actions.ts"
      to: "public.revoke_digital_entitlement/public.reissue_digital_entitlement"
      via: "typed RPC wrappers"
      pattern: "reissueDigitalEntitlement"
---

<objective>
Implement admin digital access revoke and reissue with audit, stale-state handling, and UI controls.

Purpose: DIG-07 requires operational controls, not only tests. Admins need safe access correction without public links, token reuse, or stale overwrites.
Output: Entitlement service wrappers, admin server actions, UI components, audit display, and focused tests.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-02-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-05-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-UI-SPEC.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@src/payments/admin-actions.ts
@src/components/admin/orders/order-detail.tsx
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing revoke/reissue contracts</name>
  <files>tests/unit/fulfillment/downloads.test.ts, tests/e2e/admin-fulfillment.spec.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - Revoke active entitlement removes download eligibility immediately and appends audit.
    - Reissue revoked entitlement creates fresh active access, fresh token/link/outbox work, and audit.
    - Revoke/reissue reject stale expected status/version and non-admin requests.
  </behavior>
  <action>Add unit, Playwright, and security contracts for RPC wrapper mapping, stale state, non-admin denial, revoke denial of download, reissue fresh-token behavior, audit result codes, admin revoke dialog, reissue confirmation, stale inline error, audit list update, customer denial after revoke, and fresh availability after reissue.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/downloads.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list && npm run test:security</automated></verify>
  <done>Contracts fail only because revoke/reissue wrappers, actions, and UI are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement wrappers, actions, UI, and audit display</name>
  <files>src/fulfillment/entitlements.ts, src/fulfillment/admin-entitlement-actions.ts, src/components/admin/fulfillment/entitlement-actions.tsx, src/components/admin/fulfillment/entitlement-audit-list.tsx, src/components/admin/orders/order-detail.tsx, src/payments/queries.ts, tests/unit/fulfillment/downloads.test.ts, tests/e2e/admin-fulfillment.spec.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - `revokeDigitalEntitlement` and `reissueDigitalEntitlement` validate input, call 05-01 RPCs, and return discriminated results.
    - Server actions require `requireAdmin`, expected status/version, optional sanitized note, and path revalidation.
    - Admin detail shows status, audit summary, resend/revoke/reissue actions, destructive revoke confirmation, reissue confirmation, and stale-state recovery.
  </behavior>
  <action>Implement typed wrappers and server actions. Reissue must generate fresh token material, store only its hash through the RPC, enqueue a new download email request, and append audit; it must not mutate expired app links or expose durable/public URLs. Do not accept raw token or object path input from the browser. Build admin components per 05-UI-SPEC with persistent inline results and customer-safe data boundaries.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/downloads.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list && npm run test:security && npm run typecheck</automated></verify>
  <done>Admins can revoke and reissue digital access through server-authorized actions; each action is audited, stale-safe, and reflected in download eligibility without public or stale links.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06-01 | Tampering | revoke/reissue wrappers | mitigate | Expected-state inputs, stale rejection, append-only audit checks. |
| T-05-06-02 | Elevation of Privilege | entitlement admin actions | mitigate | `requireAdmin`, DB admin checks, non-admin tests. |
| T-05-06-03 | Information Disclosure | entitlement admin UI | mitigate | No raw token, signed URL, object path, bucket, or internal audit ID rendering. |
| T-05-06-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run focused download/admin/security/typecheck commands. The SUMMARY must record revoke audit evidence, reissue fresh-token evidence, and stale-state denial evidence.
</verification>

<success_criteria>
DIG-07 has real implementation coverage for revoke/reissue RPC wrappers, admin UI/actions, audit rows/events, stale-state handling, fresh token/link behavior, and tests.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-06-SUMMARY.md` when done.</output>