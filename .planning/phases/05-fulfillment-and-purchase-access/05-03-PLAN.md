---
phase: 05-fulfillment-and-purchase-access
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [DIG-03, OPS-01]
requirements_addressed: [DIG-03, OPS-01]
user_setup: []
must_haves:
  truths:
    - "The `resend` package is human-approved before install because research flagged the latest package as SUS."
    - "Email rendering remains plain TypeScript localized HTML/text helpers per resolved research decision."
    - "Outbox execution uses a Vercel Cron-compatible protected route plus admin-triggered retry, with no Supabase Cron dependency."
  artifacts:
    - path: ".planning/phases/05-fulfillment-and-purchase-access/05-03-SUMMARY.md"
      provides: "package legitimacy approval or rejection before email worker implementation"
  key_links:
    - from: "05-03 approval"
      to: "05-04 package install"
      via: "SUMMARY records approved resend version"
      pattern: "resend"
---

<objective>
Verify the email provider package and locked email-worker approach before installation.

Purpose: Package-manager changes are gated because research marked `resend@6.14.0` as suspicious due only to recent publish timing.
Output: Human approval record or approved replacement package version for the email worker plan.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-RESEARCH.md
@.planning/phases/05-fulfillment-and-purchase-access/05-CONTEXT.md
</read_first>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `resend` package legitimacy before install</name>
  <files>package.json, package-lock.json</files>
  <what-built>Research identified `resend@6.14.0` as the standard provider SDK and flagged it `SUS` because the latest publish is recent.</what-built>
  <action>Run the package metadata command and inspect the npm package/source repository as needed. Confirm the exact approved version in the SUMMARY. Do not install `resend` until the human approves this package or provides a replacement version.</action>
  <verify>
    <automated>npm view resend@6.14.0 version time dist-tags repository.url scripts.postinstall</automated>
    <human-check>Approve `resend@6.14.0`, reject it, or provide the approved replacement version.</human-check>
  </verify>
  <done>The SUMMARY records the package decision and the exact version Plan 05-04 may install.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-SC | Tampering | `resend` install | mitigate | Blocking human legitimacy checkpoint before package installation. |
</threat_model>

<verification>
The checkpoint must complete before Plan 05-04. The SUMMARY must include the `npm view` output and human approval/rejection.
</verification>

<success_criteria>
Email worker implementation has an approved package decision and cannot silently add a suspicious dependency.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-03-SUMMARY.md` when done.</output>