---
phase: 05-fulfillment-and-purchase-access
plan: "07"
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - src/fulfillment/account-queries.ts
  - src/app/[locale]/account/orders/page.tsx
  - src/app/[locale]/account/patterns/page.tsx
  - src/components/fulfillment/account-order-history.tsx
  - src/components/fulfillment/pattern-library.tsx
  - src/components/fulfillment/pattern-library-card.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/fulfillment/account-access.test.ts
  - tests/e2e/account-purchases.spec.ts
autonomous: true
requirements: [DIG-05, ACC-02]
requirements_addressed: [DIG-05, ACC-02]
user_setup: []
must_haves:
  truths:
    - "D-02/DIG-05 signed-in pattern library groups repeated purchases while preserving history."
    - "ACC-02 signed-in customers see only their own orders, payments, downloads, and tracking summaries."
    - "D-05 library download actions use the app download route, not public PDF URLs."
  artifacts:
    - path: "src/fulfillment/account-queries.ts"
      provides: "owner-scoped order history and pattern library projections"
    - path: "src/app/[locale]/account/patterns/page.tsx"
      provides: "localized signed-in pattern library route"
  key_links:
    - from: "src/app/[locale]/account/patterns/page.tsx"
      to: "src/fulfillment/account-queries.ts"
      via: "server-rendered owner query"
      pattern: "getCustomerPatternLibrary"
---

<objective>
Build signed-in account order history and grouped pattern library access.

Purpose: Customers need durable redownload access from their account without seeing another customer's purchase data.
Output: Owner-scoped query helpers, localized account routes, library/order components, copy, and tests.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-02-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-UI-SPEC.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@src/app/[locale]/account/page.tsx
@src/app/[locale]/account/account-overview.tsx
@src/auth/guards.ts
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing account and pattern library contracts</name>
  <files>tests/unit/fulfillment/account-access.test.ts, tests/e2e/account-purchases.spec.ts</files>
  <behavior>
    - Owner sees own order history; non-owner and anonymous users are denied without enumeration.
    - Repeated purchases group by pattern while individual order and entitlement history remains accessible.
    - Download affordances delegate to `/api/downloads` and never render storage object details.
  </behavior>
  <action>Create unit and browser contracts before implementation. Include owner-only queries, repeated-purchase grouping, empty library, unauthorized account access, fresh download action shape, and localized English/Vietnamese route coverage.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/account-access.test.ts && npm run test:e2e -- tests/e2e/account-purchases.spec.ts --list</automated></verify>
  <done>Contracts fail only because account queries, routes, and components are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement owner-scoped account routes and components</name>
  <files>src/fulfillment/account-queries.ts, src/app/[locale]/account/orders/page.tsx, src/app/[locale]/account/patterns/page.tsx, src/components/fulfillment/account-order-history.tsx, src/components/fulfillment/pattern-library.tsx, src/components/fulfillment/pattern-library-card.tsx, src/messages/en.json, src/messages/vi.json, tests/unit/fulfillment/account-access.test.ts, tests/e2e/account-purchases.spec.ts</files>
  <behavior>
    - Order history lists current-user orders with payment, digital access, physical status, tracking summary, and localized detail links.
    - Pattern library groups by pattern/product with latest purchase date, purchase count, active entitlement state, and download action.
  </behavior>
  <action>Implement injectable account query helpers using owner-scoped Supabase server clients and 05-01 projections/RLS. Add dynamic localized routes following the account overview pattern: validate locale, call `setRequestLocale`, require signed-in user, then render server-authorized data. Build components with stable responsive layouts and no raw entitlement IDs, token values, object paths, bucket names, signed URLs, or service-role clients.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/account-access.test.ts && npm run test:e2e -- tests/e2e/account-purchases.spec.ts --list && npm run typecheck</automated></verify>
  <done>Signed-in customers can view only their own order history and grouped pattern library, and download actions remain app-route mediated.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07-01 | Elevation of Privilege | account queries | mitigate | Owner predicates, RLS-backed projections, cross-user tests. |
| T-05-07-02 | Information Disclosure | pattern library UI | mitigate | No raw tokens, entitlement IDs, object paths, buckets, or signed URLs. |
| T-05-07-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run focused account unit/browser/typecheck commands. The SUMMARY must record owner-only denial evidence and repeated-purchase grouping evidence.
</verification>

<success_criteria>
Signed-in customers can reopen their own order history and grouped PDF library without cross-user access or private PDF leakage.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-07-SUMMARY.md` when done.</output>