---
phase: 05-fulfillment-and-purchase-access
plan: "04"
type: execute
wave: 2
depends_on: ["05-01", "05-03"]
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - src/lib/env/server.ts
  - src/emails/transactional.ts
  - src/fulfillment/email-outbox.ts
  - src/app/api/fulfillment/email-outbox/route.ts
  - tests/unit/fulfillment/email-outbox.test.ts
  - tests/e2e/admin-fulfillment.spec.ts
autonomous: true
requirements: [DIG-03, OPS-01]
requirements_addressed: [DIG-03, OPS-01]
user_setup:
  - service: resend
    why: "Transactional download, guest access, claim, and shipping emails"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys"
      - name: RESEND_FROM_EMAIL
        source: "Resend Dashboard -> Domains or verified sender"
      - name: TRANSACTIONAL_EMAIL_WORKER_SECRET
        source: "Generate a deployment secret for Vercel Cron and manual worker calls"
    dashboard_config:
      - task: "Verify sender domain or sender identity before real delivery"
        location: "Resend Dashboard -> Domains"
must_haves:
  truths:
    - "D-03/DIG-03 paid PDF emails contain app links with 24-hour tokens, not attachments or public URLs."
    - "D-10/OPS-01 provider failure cannot erase outbox intent or corrupt paid transitions."
    - "Resolved research uses a Vercel Cron-compatible protected route plus admin/manual retry, not Supabase Cron."
    - "Resolved research uses plain TypeScript localized HTML/text email helpers, not React email components."
  artifacts:
    - path: "src/emails/transactional.ts"
      provides: "localized HTML/text email rendering helpers"
    - path: "src/fulfillment/email-outbox.ts"
      provides: "outbox claim, send, attempt, retry, and backoff service"
    - path: "src/app/api/fulfillment/email-outbox/route.ts"
      provides: "protected worker endpoint"
  key_links:
    - from: "src/app/api/fulfillment/email-outbox/route.ts"
      to: "src/fulfillment/email-outbox.ts"
      via: "worker secret authorizes due-row processing"
      pattern: "processTransactionalEmailBatch"
---

<objective>
Implement transactional email rendering and the protected outbox worker.

Purpose: Paid access and shipping emails must be durable, retryable, localized, and decoupled from payment/fulfillment transactions.
Output: Approved Resend package install, server-only config, plain HTML/text renderer, outbox worker service, protected route, and tests.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-03-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-RESEARCH.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@src/lib/env/server.ts
@src/app/api/webhooks/paypal/route.ts
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing renderer and worker contracts</name>
  <files>tests/unit/fulfillment/email-outbox.test.ts, tests/e2e/admin-fulfillment.spec.ts</files>
  <behavior>
    - Download, guest reopen, claim, shipped, and delivered emails render localized text/html app links without attachments or public PDF URLs.
    - Worker claims due rows atomically, records attempts, schedules transient backoff, and preserves permanent failure state.
    - Worker route rejects missing/wrong secret and supports injected sender tests without real Resend credentials.
  </behavior>
  <action>Create tests before implementation with an injected sender. Assert English/Vietnamese subject/html/text output, 24-hour expiry copy, provider idempotency keys, sanitized failure categories, bounded batch processing, and protected route denial.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/email-outbox.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list</automated></verify>
  <done>Contracts fail only because email rendering, worker processing, and route protection are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Install approved Resend package and build worker</name>
  <files>package.json, package-lock.json, .env.example, src/lib/env/server.ts, src/emails/transactional.ts, src/fulfillment/email-outbox.ts, src/app/api/fulfillment/email-outbox/route.ts, tests/unit/fulfillment/email-outbox.test.ts, tests/e2e/admin-fulfillment.spec.ts</files>
  <behavior>
    - The package version approved in 05-03 is installed and lockfile updated.
    - Missing config returns typed unconfigured results and never logs secrets, tokens, signed URLs, raw provider responses, or full recipient payloads.
    - The protected route can be called by Vercel Cron or admin retry using `TRANSACTIONAL_EMAIL_WORKER_SECRET`.
  </behavior>
  <action>Install only the 05-03 approved package version. Extend server-only env parsing and `.env.example`. Implement plain TypeScript render helpers returning `subject`, `html`, and `text`. Implement outbox claim/send/attempt/backoff functions with injected sender support and production `resend.emails.send`. Create the worker route with body limits, secret check, batch-size limit, no public GET data leakage, and typed processed/retry/failed counts.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/email-outbox.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list && npm run test:security && npm run typecheck</automated></verify>
  <done>Outbox rows process through a protected route with mocked sender coverage, sanitized failures, localized email bodies, and no inline provider sends.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Spoofing | worker route | mitigate | Worker secret, denial tests, bounded route. |
| T-05-04-02 | Information Disclosure | email renderer/attempts | mitigate | App links only, sanitized errors, no raw tokens/signed URLs/provider payloads. |
| T-05-04-SC | Tampering | `resend` install | mitigate | Plan 05-03 package approval dependency. |
</threat_model>

<verification>
Run focused email tests, Playwright listing, security, and typecheck. The SUMMARY must record package version, route protection, and whether real Resend config was present or mocked.
</verification>

<success_criteria>
Transactional email work is rendered, claimed, sent, retried, and failed safely through a protected server route with no Supabase Cron dependency and no inline provider calls.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-04-SUMMARY.md` when done.</output>