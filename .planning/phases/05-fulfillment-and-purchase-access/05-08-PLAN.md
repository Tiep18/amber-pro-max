---
phase: 05-fulfillment-and-purchase-access
plan: "08"
type: execute
wave: 4
depends_on: ["05-02", "05-04", "05-07"]
files_modified:
  - src/fulfillment/order-claim.ts
  - src/fulfillment/guest-access.ts
  - src/app/[locale]/guest-order/page.tsx
  - src/app/[locale]/orders/[orderNumber]/claim/page.tsx
  - src/components/fulfillment/guest-reopen-form.tsx
  - src/components/fulfillment/order-claim-panel.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/fulfillment/account-access.test.ts
  - tests/e2e/account-purchases.spec.ts
autonomous: true
requirements: [DIG-06, ACC-05]
requirements_addressed: [DIG-06, ACC-05]
user_setup: []
must_haves:
  truths:
    - "D-06/DIG-06 guest reopen uses a 24-hour expiring email magic token and generic responses."
    - "D-08/ACC-05 order claim requires signed-in same-email account plus email token proof."
    - "D-09 successful claim revokes old guest order/download access tokens."
  artifacts:
    - path: "src/fulfillment/order-claim.ts"
      provides: "guest reopen request, claim email request, and claim transaction actions"
    - path: "src/components/fulfillment/guest-reopen-form.tsx"
      provides: "generic guest reopen UI"
    - path: "src/components/fulfillment/order-claim-panel.tsx"
      provides: "same-email claim UI"
  key_links:
    - from: "src/fulfillment/order-claim.ts"
      to: "transactional_email_outbox"
      via: "guest reopen and claim emails enqueue through 05-04 outbox"
      pattern: "claimGuestOrderAction"
---

<objective>
Implement guest order reopening and same-email account claiming.

Purpose: Guest checkout remains accessible after purchase, but guest tokens expire and stop after the order is claimed into an account.
Output: Guest reopen and claim actions, localized pages/components, token revocation behavior, and tests.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-02-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-04-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-07-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-UI-SPEC.md
@src/payments/guest-access.ts
@src/auth/guards.ts
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing guest reopen and claim contracts</name>
  <files>tests/unit/fulfillment/account-access.test.ts, tests/e2e/account-purchases.spec.ts</files>
  <behavior>
    - Guest reopen always returns generic success and sends a magic link only for valid order/email pairs.
    - Claim rejects wrong-email signed-in users, invalid tokens, expired tokens, replayed tokens, already-claimed orders, and cross-order attempts.
    - Claim success attaches the order to the account, revokes outstanding guest tokens, writes audit, and removes guest-only access.
  </behavior>
  <action>Add unit and browser contracts before implementation. Include English/Vietnamese guest reopen routes, claim route states, same-email proof, old guest token denial after claim, and non-enumerating expired/invalid copy.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/account-access.test.ts && npm run test:e2e -- tests/e2e/account-purchases.spec.ts --list</automated></verify>
  <done>Contracts fail only because guest reopen and claim actions/pages are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement guest reopen, claim email, and same-email claim</name>
  <files>src/fulfillment/order-claim.ts, src/fulfillment/guest-access.ts, src/app/[locale]/guest-order/page.tsx, src/app/[locale]/orders/[orderNumber]/claim/page.tsx, src/components/fulfillment/guest-reopen-form.tsx, src/components/fulfillment/order-claim-panel.tsx, src/messages/en.json, src/messages/vi.json, tests/unit/fulfillment/account-access.test.ts, tests/e2e/account-purchases.spec.ts</files>
  <behavior>
    - Reopen and claim-email actions enqueue email only for valid scope but always return generic success.
    - Claim verifies signed-in same-email ownership, consumes token proof, updates owner, revokes guest tokens, and writes audit in one server/database transaction.
  </behavior>
  <action>Use 05-01 token tables/RPCs and 05-04 outbox. Raw tokens may exist only at creation/click time and must never be logged or stored. Add localized pages with visible labels, stable pending states, expired/invalid recovery copy, and account-bound success copy. After claim, future access must use signed-in account routes and old guest download/reopen tokens must be denied.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/account-access.test.ts && npm run test:e2e -- tests/e2e/account-purchases.spec.ts --list && npm run test:security && npm run typecheck</automated></verify>
  <done>Guests can request secure access without enumeration, same-email users can claim orders with token proof, and old guest access is revoked after claim.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08-01 | Spoofing | claim tokens | mitigate | Hash tokens, 24-hour expiry, single-use status, same-email user check. |
| T-05-08-02 | Information Disclosure | guest pages | mitigate | Generic success/error copy and no existence confirmation. |
| T-05-08-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run focused account unit/browser/security/typecheck commands. The SUMMARY must include old guest-token denial after claim and wrong-email denial evidence.
</verification>

<success_criteria>
Guest access is recoverable through expiring email links and claimable into a same-email account while old guest tokens are revoked after claim.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-08-SUMMARY.md` when done.</output>