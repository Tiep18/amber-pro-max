---
phase: 05-fulfillment-and-purchase-access
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/fulfillment/guest-access.ts
  - src/fulfillment/downloads.ts
  - src/app/api/downloads/route.ts
  - src/components/fulfillment/download-panel.tsx
  - src/components/payments/order-payment-page.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/fulfillment/downloads.test.ts
  - tests/e2e/order-downloads.spec.ts
  - tests/security/fulfillment-boundaries.test.mjs
autonomous: true
requirements: [DIG-04, DIG-06, FUL-01]
requirements_addressed: [DIG-04, DIG-06, FUL-01]
user_setup: []
must_haves:
  truths:
    - "D-04/DIG-04 every download request revalidates entitlement before signed URL creation."
    - "D-05 PDFs are never public, attached to email, or exposed through durable public URLs."
    - "D-06/D-07 guest download access uses hashed 24-hour scoped tokens."
    - "D-16/FUL-01 mixed orders can show digital ready while physical remains separate."
  artifacts:
    - path: "src/fulfillment/downloads.ts"
      provides: "server-only download authorization and signed URL adapter boundary"
    - path: "src/app/api/downloads/route.ts"
      provides: "entitlement-checked app download route"
    - path: "src/components/fulfillment/download-panel.tsx"
      provides: "localized customer download and fresh-link UI"
  key_links:
    - from: "src/app/api/downloads/route.ts"
      to: "src/fulfillment/downloads.ts"
      via: "authorizeDownloadRequest before Storage createSignedUrl"
      pattern: "authorizeDownloadRequest"
---

<objective>
Implement entitlement-checked downloads and the customer order download panel.

Purpose: Paid customers need usable PDF access, but every click must pass entitlement validation before private Storage issues a short-lived URL.
Output: Token helpers, download service, `/api/downloads`, customer download UI, localized copy, and tests.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-fulfillment-and-purchase-access/05-CONTEXT.md
@.planning/phases/05-fulfillment-and-purchase-access/05-UI-SPEC.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md
@src/payments/guest-access.ts
@src/payments/queries.ts
@src/catalog/media-actions.ts
@src/components/payments/order-payment-page.tsx
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing download and guest token contracts</name>
  <files>tests/unit/fulfillment/downloads.test.ts, tests/e2e/order-downloads.spec.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - Active signed-in entitlement succeeds; unpaid, revoked, wrong-owner, expired, stale token, and cross-order guest cases fail.
    - Only server download service may call `createSignedUrl`; client code cannot render raw token, bucket, object path, or signed URL values.
  </behavior>
  <action>Create unit, Playwright, and security assertions before implementation. Unit tests must inject fake Supabase query and Storage adapters. Browser contracts must cover signed-in owner download, guest email-link download, expired-link recovery copy, revoked entitlement denial, and cross-user denial without relying on real Storage URLs.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/downloads.test.ts && npm run test:e2e -- tests/e2e/order-downloads.spec.ts --list && npm run test:security</automated></verify>
  <done>Contracts fail only because download helpers, route, and customer panel are absent.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement secure download service, route, and panel</name>
  <files>src/fulfillment/guest-access.ts, src/fulfillment/downloads.ts, src/app/api/downloads/route.ts, src/components/fulfillment/download-panel.tsx, src/components/payments/order-payment-page.tsx, src/messages/en.json, src/messages/vi.json, tests/unit/fulfillment/downloads.test.ts, tests/e2e/order-downloads.spec.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - `authorizeDownloadRequest` validates account owner or guest token, entitlement state, order scope, token expiry, and product asset before signed URL issue.
    - The route returns generic denial and never exposes PDF paths, entitlement IDs, signed URLs, or raw token values in UI or logs.
  </behavior>
  <action>Implement token hashing helpers using the Phase 4 guest cookie pattern, then implement `authorizeDownloadRequest` and a Storage adapter in `src/fulfillment/downloads.ts`. Create `GET /api/downloads` for email/guest token links and `POST /api/downloads` for authorized fresh-link requests if needed. Use server-only Supabase clients, Zod inputs from 05-01, and Storage `createSignedUrl` only after authorization. Extend `OrderPaymentPage` with `DownloadPanel` and 05-UI-SPEC localized copy.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/downloads.test.ts && npm run test:e2e -- tests/e2e/order-downloads.spec.ts --list && npm run test:security && npm run typecheck</automated></verify>
  <done>Authorized signed-in and scoped guest users can request app-mediated downloads; unauthorized, expired, revoked, or cross-user requests fail without exposing private PDF details.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Elevation of Privilege | download authorization | mitigate | Owner/token/order/entitlement predicates and negative tests. |
| T-05-02-02 | Information Disclosure | `/api/downloads` and panel | mitigate | Generic denial and scanner allow-list for signed URL boundary. |
| T-05-02-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run focused unit, browser listing, security, and typecheck commands. The SUMMARY must identify the only allowed `createSignedUrl` boundary and show denial evidence for revoked, expired, wrong-owner, and wrong-guest cases.
</verification>

<success_criteria>
Download access works from paid order and guest link flows while PDFs remain private and every signed URL is issued only after active entitlement authorization.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-02-SUMMARY.md` when done.</output>