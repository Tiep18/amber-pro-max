---
phase: 05-fulfillment-and-purchase-access
plan: "05"
type: execute
wave: 3
depends_on: ["05-04"]
files_modified:
  - src/fulfillment/admin-email-actions.ts
  - src/components/admin/fulfillment/failed-email-queue.tsx
  - src/components/admin/orders/order-detail.tsx
  - src/components/admin/orders/order-queue.tsx
  - src/payments/queries.ts
  - tests/unit/fulfillment/email-outbox.test.ts
  - tests/e2e/admin-fulfillment.spec.ts
  - tests/security/fulfillment-boundaries.test.mjs
autonomous: true
requirements: [DIG-03, DIG-07, OPS-02]
requirements_addressed: [DIG-03, DIG-07, OPS-02]
user_setup: []
must_haves:
  truths:
    - "D-11/OPS-02 admins can inspect sanitized failed email state and trigger controlled retry."
    - "D-12/DIG-07 admin resend creates a fresh token/link, new outbox request, and audit event."
    - "D-13 failed-email queue shows order, type, attempts, sanitized error, next retry, and controlled actions."
  artifacts:
    - path: "src/fulfillment/admin-email-actions.ts"
      provides: "admin retry and download resend server actions"
    - path: "src/components/admin/fulfillment/failed-email-queue.tsx"
      provides: "compact failed transactional email queue"
  key_links:
    - from: "src/components/admin/fulfillment/failed-email-queue.tsx"
      to: "src/fulfillment/admin-email-actions.ts"
      via: "retry/resend forms"
      pattern: "retryTransactionalEmailAction"
---

<objective>
Build the admin failed-email queue plus retry and download resend operations.

Purpose: Email provider failures must be inspectable and recoverable without stale link reuse or provider detail leakage.
Output: Admin query extensions, failed email queue, retry/resend actions, sanitized UI, and tests.
</objective>

<read_first>
@.planning/phases/05-fulfillment-and-purchase-access/05-04-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-UI-SPEC.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@src/payments/admin-actions.ts
@src/components/admin/orders/order-detail.tsx
@src/components/admin/orders/order-queue.tsx
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing admin email queue and resend contracts</name>
  <files>tests/unit/fulfillment/email-outbox.test.ts, tests/e2e/admin-fulfillment.spec.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - Transient failures can be retried and permanent failures remain admin-visible with sanitized details.
    - Download resend requires admin authorization, creates a fresh token/outbox/audit tuple, and never reuses expired links.
    - Queue hides raw provider payloads, signed URLs, token values, object paths, and full provider headers.
  </behavior>
  <action>Add unit, browser, and security contracts for stale email row handling, duplicate retry no-op, non-admin denial, masked recipient display, retry success, resend success with fresh token hash, sanitized error display, and no raw provider or token leakage.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/email-outbox.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list && npm run test:security</automated></verify>
  <done>Contracts fail only because admin queue/actions are not implemented.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement queue, retry, and download resend</name>
  <files>src/fulfillment/admin-email-actions.ts, src/components/admin/fulfillment/failed-email-queue.tsx, src/components/admin/orders/order-detail.tsx, src/components/admin/orders/order-queue.tsx, src/payments/queries.ts, tests/unit/fulfillment/email-outbox.test.ts, tests/e2e/admin-fulfillment.spec.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - Retry calls 05-04 worker service for eligible failed or due rows and records sanitized state.
    - Download resend creates a new access token hash, outbox row, and audit event.
    - Admin queue/detail expose only public order number, type, masked recipient, attempt count, sanitized error, next retry, and action state.
  </behavior>
  <action>Implement admin server actions after `requireAdmin`, Zod parsing, stale row checks, and current entitlement/email state loading. Extend admin order query helpers for failed-email indicators and queue rows without raw provider payloads. Build a responsive `FailedEmailQueue` reachable from admin orders with retry/resend forms, stable pending states, persistent feedback, and path revalidation.</action>
  <verify><automated>npm run test:unit -- tests/unit/fulfillment/email-outbox.test.ts && npm run test:e2e -- tests/e2e/admin-fulfillment.spec.ts --list && npm run test:security && npm run typecheck</automated></verify>
  <done>Admins can inspect failed emails, retry eligible sends, and resend download emails with fresh token/audit evidence while non-admins and stale rows are denied safely.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Elevation of Privilege | admin email actions | mitigate | `requireAdmin`, stale checks, server actions only, non-admin tests. |
| T-05-05-02 | Information Disclosure | failed email queue | mitigate | Masked recipients, sanitized provider errors, scanner for raw payloads/tokens. |
| T-05-05-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run focused email tests, admin browser listing, security, and typecheck. The SUMMARY must show resend fresh-token/outbox/audit evidence and retry sanitized-error evidence.
</verification>

<success_criteria>
Failed transactional emails are admin-visible and recoverable, and download resend is a fresh audited operation rather than stale link reuse.
</success_criteria>

<output>Create `.planning/phases/05-fulfillment-and-purchase-access/05-05-SUMMARY.md` when done.</output>