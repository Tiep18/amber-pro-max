---
phase: 05-fulfillment-and-purchase-access
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/*_fulfillment_purchase_access.sql
  - supabase/tests/database/05_fulfillment_entitlements.test.sql
  - supabase/tests/database/05_email_outbox.test.sql
  - supabase/tests/database/05_guest_claim.test.sql
  - supabase/tests/database/05_physical_fulfillment.test.sql
  - src/types/supabase.ts
  - src/fulfillment/schemas.ts
  - tests/security/fulfillment-boundaries.test.mjs
autonomous: true
requirements: [DIG-02, DIG-03, DIG-04, DIG-05, DIG-06, DIG-07, FUL-01, FUL-02, FUL-03, ACC-02, ACC-05, OPS-01, OPS-02]
requirements_addressed: [DIG-02, DIG-03, DIG-04, DIG-05, DIG-06, DIG-07, FUL-01, FUL-02, FUL-03, ACC-02, ACC-05, OPS-01, OPS-02]
user_setup: []
must_haves:
  truths:
    - "D-01 paid digital order lines create one active entitlement only after confirmed payment."
    - "D-03, D-06, D-07 tokens are hashed, scoped, and expire after 24 hours."
    - "D-10 email intent is stored transactionally before provider sending."
    - "D-12 and DIG-07 revoke/reissue are explicit audited state transitions with stale-state checks."
    - "D-14 through D-17 physical fulfillment state and shipped email rules are represented."
  artifacts:
    - path: "supabase/migrations/*_fulfillment_purchase_access.sql"
      provides: "entitlement, token, outbox, claim, physical fulfillment, RLS, RPC, and audit foundation"
    - path: "tests/security/fulfillment-boundaries.test.mjs"
      provides: "private PDF, token, signed URL, service-role, and unsafe metadata scan"
  key_links:
    - from: "public.apply_payment_transition(jsonb)"
      to: "private.grant_paid_digital_entitlements"
      via: "paid branch under existing payment transaction locks"
      pattern: "grant_paid_digital_entitlements"
---

<objective>
Create the Phase 5 database/security foundation for entitlements, tokens, outbox, claim, physical fulfillment, and admin access operations.

Purpose: Later UI and server-action plans need one audited source of truth for secure paid access and separate physical fulfillment.
Output: Migration, generated Supabase types, shared Zod schemas, database tests, and static security tests.
</objective>

<read_first>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-fulfillment-and-purchase-access/05-CONTEXT.md
@.planning/phases/05-fulfillment-and-purchase-access/05-RESEARCH.md
@.planning/phases/05-fulfillment-and-purchase-access/05-PATTERNS.md
@supabase/migrations/20260615034000_trusted_payments_orders.sql
@tests/security/payment-boundaries.test.mjs
</read_first>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add failing database and security contracts</name>
  <files>supabase/tests/database/05_fulfillment_entitlements.test.sql, supabase/tests/database/05_email_outbox.test.sql, supabase/tests/database/05_guest_claim.test.sql, supabase/tests/database/05_physical_fulfillment.test.sql, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - D-01/DIG-02: duplicate paid transitions create one active entitlement per paid digital order line.
    - D-10/OPS-01: outbox intent is durable and provider sending is deferred.
    - D-12/DIG-07: revoke and reissue write audit rows, reject stale state, and reissue creates fresh token/outbox work.
    - D-06..D-09/ACC-05: guest reopen and claim tokens are hashed, scoped, expiring, and revoked after claim.
    - D-14..D-17/FUL-01..FUL-03: physical status, optional tracking, shipped email, and split state are enforced.
  </behavior>
  <action>Create pgTAP and static security tests before SQL. Cover unpaid orders, duplicate provider events, cross-owner reads, raw token persistence, stale revoke/reissue expected state, unsafe audit metadata, expired token, wrong-email claim, shipped without tracking, invalid tracking URL, backwards physical transition, public PDF URL exposure, browser `createSignedUrl`, and client service-role imports.</action>
  <verify><automated>npm run db:test && npm run test:security</automated></verify>
  <done>Tests fail only because Phase 5 migration/types/schemas are absent, and each failure maps to the listed requirements or D-01 through D-17.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement schema, RLS, RPCs, types, and schemas</name>
  <files>supabase/migrations/*_fulfillment_purchase_access.sql, supabase/tests/database/05_fulfillment_entitlements.test.sql, supabase/tests/database/05_email_outbox.test.sql, supabase/tests/database/05_guest_claim.test.sql, supabase/tests/database/05_physical_fulfillment.test.sql, src/types/supabase.ts, src/fulfillment/schemas.ts, tests/security/fulfillment-boundaries.test.mjs</files>
  <behavior>
    - Entitlements preserve history while enforcing one active entitlement per paid digital order line.
    - Admin revoke/reissue RPCs require admin authority, expected state/version, fresh token hash on reissue, and append-only audit.
    - Token/outbox/claim/physical tables store only safe metadata and apply owner/admin RLS.
  </behavior>
  <action>Use `supabase migration new fulfillment_purchase_access`. Add `digital_entitlements`, `digital_access_tokens`, guest reopen and claim tokens, `transactional_email_outbox`, email attempts, physical fulfillment events, and fulfillment audit events. Enable RLS, use owner/admin predicates, revoke public execute on privileged RPCs, use fixed search paths, check `private.is_admin()` inside admin RPCs, store token hashes only, reject unsafe metadata containing tokens, signed URLs, object paths, provider payloads, or secrets, extend `apply_payment_transition` paid branch to call the fulfillment helper, generate `src/types/supabase.ts`, and add Zod schemas/results in `src/fulfillment/schemas.ts`.</action>
  <verify><automated>npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts && npm run test:security && npm run typecheck</automated></verify>
  <done>Database reset/lint/tests/types pass; paid transitions grant once; revoke/reissue is audited and stale-safe; token material is hashed at rest; private PDF access is not exposed through public/client surfaces.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | paid entitlement helper | mitigate | Transactional paid-branch hook, unique active entitlement key, duplicate transition tests. |
| T-05-01-02 | Information Disclosure | token/PDF metadata | mitigate | Hash tokens, private bucket, RLS, static scans for raw URLs/object paths. |
| T-05-01-03 | Elevation of Privilege | admin RPCs | mitigate | `private.is_admin()`, fixed search path, revoked public execute, admin tests. |
| T-05-01-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run the database/security gate in the tasks. The SUMMARY must record migration filename, RPC names, type generation status, and revoke/reissue stale-state evidence.
</verification>

<success_criteria>
Phase 5 has one RLS-protected, audited database foundation for paid entitlements, private download tokens, durable email outbox, guest claiming, physical fulfillment, and admin revoke/reissue actions.
</success_criteria>

<output>
Create `.planning/phases/05-fulfillment-and-purchase-access/05-01-SUMMARY.md` when done.
</output>