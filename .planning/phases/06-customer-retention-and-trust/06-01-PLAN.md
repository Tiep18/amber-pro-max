---
phase: 06-customer-retention-and-trust
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/*_customer_retention_trust.sql
  - supabase/tests/database/06_customer_retention.test.sql
  - src/types/supabase.ts
  - src/account/addresses.ts
  - src/account/address-actions.ts
  - src/app/[locale]/account/addresses/address-book-page.tsx
  - src/app/[locale]/account/addresses/page.tsx
  - src/app/[locale]/tai-khoan/dia-chi/page.tsx
  - src/components/account/address-book.tsx
  - src/components/account/address-form.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/account/addresses.test.ts
  - tests/e2e/account-retention.spec.ts
autonomous: true
requirements: [ACC-03]
must_haves:
  truths:
    - "Signed-in customer can save, edit, delete, and identify one default shipping address per D-01, D-02, and D-04."
    - "Saved-address rows are owner-scoped convenience data and cannot mutate prior order shipping snapshots per D-01 and D-04."
  artifacts:
    - path: "supabase/migrations/*_customer_retention_trust.sql"
      provides: "Customer shipping address table, constraints, RLS, grants, and indexes"
    - path: "src/account/addresses.ts"
      provides: "Owner-scoped saved-address queries and mapping helpers"
      exports: ["getCustomerShippingAddresses"]
    - path: "src/account/address-actions.ts"
      provides: "Server actions for create, update, delete, and default selection"
    - path: "src/components/account/address-book.tsx"
      provides: "Localized saved-address list and row actions"
  key_links:
    - from: "src/account/address-actions.ts"
      to: "customer_shipping_addresses"
      via: "server-owned authenticated user id"
      pattern: "requireUser|customer_shipping_addresses"
    - from: "supabase/migrations/*_customer_retention_trust.sql"
      to: "checkout_orders.shipping_address"
      via: "no FK from orders to saved addresses"
      pattern: "customer_shipping_addresses"
---

## Phase Goal

**As a** returning customer, **I want to** manage saved shipping addresses, **so that** repeat checkout starts from accurate account-owned data.

<objective>
Implement the saved-address account management slice for ACC-03.

Purpose: Saved addresses are convenience records for future checkout, while order shipping snapshots remain immutable evidence.
Output: Shared Phase 6 migration section, address query/actions, localized account pages/components, and RED-to-green address tests for D-01, D-02, and D-04.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-customer-retention-and-trust/06-CONTEXT.md
@.planning/phases/06-customer-retention-and-trust/06-RESEARCH.md
@.planning/phases/06-customer-retention-and-trust/06-PATTERNS.md
@.planning/phases/06-customer-retention-and-trust/06-VALIDATION.md
@.planning/phases/06-customer-retention-and-trust/06-UI-SPEC.md
@.planning/phases/04-trusted-payments-and-orders/04-03-SUMMARY.md
@.planning/phases/05-fulfillment-and-purchase-access/05-07-SUMMARY.md
@src/auth/guards.ts
@src/checkout/shipping-address.ts
@src/app/[locale]/account/account-overview.tsx
@src/app/[locale]/account/orders/account-orders-page.tsx
</context>

<migration_strategy>
Use one Phase 6 migration named with the `*_customer_retention_trust.sql` suffix from `06-PATTERNS.md`. This aligns the checker warning instead of using separate `*_customer_addresses_wishlist.sql`, `*_product_reviews_moderation.sql`, and `*_newsletter_consent.sql` migration names. Later Phase 6 plans append their domain sections to the same migration before execution completes.
</migration_strategy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add saved-address contracts</name>
  <files>supabase/tests/database/06_customer_retention.test.sql, tests/unit/account/addresses.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - Test ACC-03 and D-01: saved addresses are copied later, not linked as order evidence.
    - Test D-02: each customer has one default shipping address.
    - Test D-04: edit/delete affects only saved records, not prior order snapshots.
  </behavior>
  <action>Create RED database, unit, and Playwright contracts for saved addresses. Database tests must assert `customer_shipping_addresses`, owner FK, one-default partial unique index, RLS owner policies, revoked direct anon access, and no `checkout_orders` FK to saved-address rows. Unit tests must cover parsing/mapping and action result states. Browser contracts must cover English and Vietnamese account address routes and list/create/edit/delete/default flows with authenticated fixture hooks to be activated in Plan 06-10.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/account/addresses.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
  </verify>
  <done>Saved-address test files exist, cite D-01/D-02/D-04, and fail or list for missing implementation without passing vacuously.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement saved-address schema, services, and account UI</name>
  <files>supabase/migrations/*_customer_retention_trust.sql, src/types/supabase.ts, src/account/addresses.ts, src/account/address-actions.ts, src/app/[locale]/account/addresses/address-book-page.tsx, src/app/[locale]/account/addresses/page.tsx, src/app/[locale]/tai-khoan/dia-chi/page.tsx, src/components/account/address-book.tsx, src/components/account/address-form.tsx, src/messages/en.json, src/messages/vi.json, supabase/tests/database/06_customer_retention.test.sql, tests/unit/account/addresses.test.ts</files>
  <behavior>
    - Signed-in customer can create, update, delete, and set default saved addresses.
    - Server actions derive owner from `requireUser`; browser input never supplies owner id.
    - Account UI follows `06-UI-SPEC.md`: one top-level card, visible labels, default badge, delete consequence copy.
  </behavior>
  <action>Create `customer_shipping_addresses` in the shared `*_customer_retention_trust.sql` migration per D-01, D-02, and D-04. Reuse `shippingAddressSchema` field semantics, enforce one default per customer, enable RLS, revoke broad grants, and avoid any order-table dependency on mutable address rows. Implement query/action helpers with safe result unions and `requireUser`. Build localized account routes `/en/account/addresses` and `/vi/tai-khoan/dia-chi` using existing account page patterns and localized messages. Regenerate Supabase types after migration changes.</action>
  <verify>
    <automated>npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts</automated>
    <automated>npm run test:unit -- tests/unit/account/addresses.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Customer saved-address management works through server-owned authorization and account UI, with immutable-order-snapshot safety covered by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to address actions | Untrusted form data crosses into customer-owned saved-address mutations. |
| Saved address to order evidence | Mutable convenience data must not become historical shipping evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Elevation of Privilege | `customer_shipping_addresses`, `address-actions.ts` | mitigate | Use `requireUser`, server-owned `user.id`, RLS owner predicates, and cross-user pgTAP checks. |
| T-06-01-02 | Tampering | Default address and prior order snapshots | mitigate | Enforce one-default partial unique index and no saved-address FK from immutable checkout orders per D-01/D-02/D-04. |
| T-06-01-SC | Tampering | npm package surface | accept | No package install occurs; use existing Supabase, Zod, Next.js, and React stack. |
</threat_model>

<verification>
Plan gate: `npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts && npm run test:unit -- tests/unit/account/addresses.test.ts && npm run typecheck`.
</verification>

<success_criteria>
ACC-03 saved-address account management is complete when signed-in customers can manage address records in both locales, one default is enforced, owner boundaries hold, and prior orders remain unaffected by saved-address edits or deletes.
</success_criteria>

<output>
Create `.planning/phases/06-customer-retention-and-trust/06-01-SUMMARY.md` when done.
</output>
