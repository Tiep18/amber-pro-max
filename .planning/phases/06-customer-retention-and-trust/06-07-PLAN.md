---
phase: 06-customer-retention-and-trust
plan: "07"
type: execute
wave: 7
depends_on: ["06-06"]
files_modified:
  - supabase/migrations/*_customer_retention_trust.sql
  - supabase/tests/database/06_customer_retention.test.sql
  - src/types/supabase.ts
  - src/newsletter/consent.ts
  - src/newsletter/actions.ts
  - src/components/newsletter/subscribe-form.tsx
  - src/components/site-footer.tsx
  - src/i18n/routing.ts
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/newsletter/consent.test.ts
  - tests/e2e/newsletter.spec.ts
autonomous: true
requirements: [NEWS-01, NEWS-02]
must_haves:
  truths:
    - "Visitor can explicitly subscribe in Vietnamese or English without creating an account per D-13 and NEWS-01."
    - "Subscription state is keyed by normalized email with latest locale/market preference and append-only consent events per D-13."
    - "Consent evidence stores redacted or hashed request metadata, not raw PII-heavy logs per D-16."
  artifacts:
    - path: "supabase/migrations/*_customer_retention_trust.sql"
      provides: "Newsletter subscriber and consent event tables/functions"
    - path: "src/newsletter/consent.ts"
      provides: "Subscribe state machine, email normalization, and metadata redaction"
    - path: "src/newsletter/actions.ts"
      provides: "Unauthenticated explicit subscribe action"
    - path: "src/components/newsletter/subscribe-form.tsx"
      provides: "Localized public subscribe form"
  key_links:
    - from: "src/components/site-footer.tsx"
      to: "src/components/newsletter/subscribe-form.tsx"
      via: "localized footer subscribe form"
      pattern: "SubscribeForm"
    - from: "src/newsletter/actions.ts"
      to: "newsletter_subscribers"
      via: "validated unauthenticated subscribe"
      pattern: "subscribeNewsletterAction|newsletter_subscribers"
---

## Phase Goal

**As a** visitor, **I want to** explicitly subscribe in my language, **so that** newsletter consent is clear and account-independent.

<objective>
Implement localized newsletter subscribe and consent history for NEWS-01 and the consent-record portion of NEWS-02.

Purpose: Newsletter subscription must work for visitors and signed-in customers without requiring an account or retaining unnecessary raw request metadata.
Output: Subscriber/consent schema, subscribe service/action, footer form, localized copy, and tests for D-13 and D-16.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-customer-retention-and-trust/06-CONTEXT.md
@.planning/phases/06-customer-retention-and-trust/06-RESEARCH.md
@.planning/phases/06-customer-retention-and-trust/06-PATTERNS.md
@.planning/phases/06-customer-retention-and-trust/06-UI-SPEC.md
@.planning/phases/05-fulfillment-and-purchase-access/05-04-SUMMARY.md
@src/components/site-footer.tsx
@src/checkout/actions.ts
@src/fulfillment/downloads.ts
</context>

<migration_strategy>
Append newsletter subscriber and consent-event tables to the shared `*_customer_retention_trust.sql` migration from Plan 06-01.
</migration_strategy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add subscribe and consent contracts</name>
  <files>supabase/tests/database/06_customer_retention.test.sql, tests/unit/newsletter/consent.test.ts, tests/e2e/newsletter.spec.ts</files>
  <behavior>
    - Explicit subscribe works in English and Vietnamese without account requirement.
    - Subscriber state is normalized by email and records latest locale/market.
    - Consent events retain source/time and redacted/hash metadata only.
  </behavior>
  <action>Add RED database, unit, and browser contracts for NEWS-01 and D-13/D-16. Database tests must assert `newsletter_subscribers`, `newsletter_consent_events`, direct grant restrictions, required consent source/timestamp fields, and absence of raw IP or raw user-agent fields. Unit tests must cover email normalization, subscribe/resubscribe transitions, latest locale/market updates, redacted metadata shaping, and generic safe result states. Browser contracts must cover footer subscribe in both locales using Plan 06-10 fixtures where needed.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/newsletter/consent.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/newsletter.spec.ts --list</automated>
  </verify>
  <done>Newsletter subscribe/consent contracts exist and are traceable to NEWS-01, D-13, and D-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement explicit subscribe and consent history</name>
  <files>supabase/migrations/*_customer_retention_trust.sql, src/types/supabase.ts, src/newsletter/consent.ts, src/newsletter/actions.ts, src/components/newsletter/subscribe-form.tsx, src/components/site-footer.tsx, src/i18n/routing.ts, src/messages/en.json, src/messages/vi.json, supabase/tests/database/06_customer_retention.test.sql, tests/unit/newsletter/consent.test.ts, tests/e2e/newsletter.spec.ts</files>
  <behavior>
    - Public subscribe form has visible Email label, explicit noun-bearing CTA, and localized success/error states.
    - Subscribe validates email/locale/market/source and appends consent history.
    - Stored metadata avoids raw IP, raw user-agent, raw headers, and raw token material.
  </behavior>
  <action>Implement `newsletter_subscribers` and `newsletter_consent_events` in the shared migration. Subscribe must normalize email as primary identity, store latest locale and market preference, append a consent event with source/timestamp, and shape request metadata through redaction or hashing. Implement `subscribeNewsletterAction` without `requireUser`; validate all input with Zod and return generic safe states. Add a compact footer `SubscribeForm` with visible label and noun-bearing CTA such as `Subscribe to newsletter` plus equivalent Vietnamese copy. Do not add pre-checked consent boxes or admin override paths.</action>
  <verify>
    <automated>npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts</automated>
    <automated>npm run test:unit -- tests/unit/newsletter/consent.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/newsletter.spec.ts --list</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>NEWS-01 subscribe works in both locales and durable consent history satisfies D-13 and D-16.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor to subscribe action | Anonymous email, locale, market, and request metadata cross into consent records. |
| Consent records to audit/admin readers | Stored metadata must be useful without raw PII-heavy logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07-01 | Spoofing | `subscribeNewsletterAction` | mitigate | Validate email/locale/market/source and record explicit submit event; no account requirement per D-13. |
| T-06-07-02 | Information Disclosure | Consent metadata | mitigate | Store redacted/hash metadata and reject raw IP/user-agent/header dumps per D-16. |
| T-06-07-SC | Tampering | npm package surface | accept | No package install occurs. |
</threat_model>

<verification>
Plan gate: `npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts && npm run test:unit -- tests/unit/newsletter/consent.test.ts && npm run test:e2e -- tests/e2e/newsletter.spec.ts --list && npm run typecheck`.
</verification>

<success_criteria>
NEWS-01 and consent-history portions of NEWS-02 are complete when visitors can explicitly subscribe in both locales, email-keyed subscriber state is updated, and consent evidence stores minimized metadata.
</success_criteria>

<output>
Create `.planning/phases/06-customer-retention-and-trust/06-07-SUMMARY.md` when done.
</output>
