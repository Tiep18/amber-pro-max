---
phase: 06-customer-retention-and-trust
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/account/addresses.ts
  - src/components/checkout/saved-address-selector.tsx
  - src/components/checkout/checkout-page.tsx
  - src/i18n/routing.ts
  - src/proxy.ts
  - src/auth/redirect.ts
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/account/addresses.test.ts
  - tests/unit/checkout/shipping-address-ui.test.ts
  - tests/unit/checkout/quote-diff.test.ts
  - tests/e2e/account-retention.spec.ts
autonomous: true
requirements: [ACC-03]
must_haves:
  truths:
    - "Signed-in customer can choose a saved address during checkout and copy it into the active shipping form per D-01."
    - "Saved-address selection immediately refreshes the quote and shows the existing material-change preview before accepted quote changes per D-03."
  artifacts:
    - path: "src/components/checkout/saved-address-selector.tsx"
      provides: "Checkout saved-address picker integrated with quote revalidation"
    - path: "src/components/checkout/checkout-page.tsx"
      provides: "State wiring between saved-address selection and existing checkout flow"
    - path: "tests/e2e/account-retention.spec.ts"
      provides: "Browser contract for signed-in checkout address reuse"
  key_links:
    - from: "src/components/checkout/saved-address-selector.tsx"
      to: "src/checkout/actions.ts"
      via: "refreshCheckoutQuoteAction"
      pattern: "refreshCheckoutQuoteAction"
    - from: "src/components/checkout/checkout-page.tsx"
      to: "QuoteDiffDialog"
      via: "existing material-change confirmation flow"
      pattern: "QuoteDiffDialog|materialChanges"
---

## Phase Goal

**As a** returning customer, **I want to** reuse a saved address at checkout, **so that** checkout stays fast while market and shipping changes remain explicit.

<objective>
Wire saved-address selection into checkout revalidation for ACC-03.

Purpose: D-03 requires saved-address reuse to go through the same server-authoritative quote refresh and material-change confirmation used by Phase 3 checkout.
Output: Checkout selector, state wiring, localized route/redirect support, and automated tests for D-01 and D-03.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-customer-retention-and-trust/06-CONTEXT.md
@.planning/phases/06-customer-retention-and-trust/06-RESEARCH.md
@.planning/phases/06-customer-retention-and-trust/06-PATTERNS.md
@.planning/phases/06-customer-retention-and-trust/06-UI-SPEC.md
@.planning/phases/06-customer-retention-and-trust/06-01-SUMMARY.md
@src/components/checkout/checkout-page.tsx
@src/components/checkout/destination-form.tsx
@src/checkout/actions.ts
@src/checkout/shipping-address.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend saved-address checkout tests</name>
  <files>tests/unit/account/addresses.test.ts, tests/unit/checkout/shipping-address-ui.test.ts, tests/unit/checkout/quote-diff.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - Selecting a saved address copies normalized fields into checkout state.
    - Selection calls `refreshCheckoutQuoteAction` with existing accepted quote and selected country.
    - Material changes open the existing quote-diff confirmation before checkout continues.
  </behavior>
  <action>Add failing tests for the D-03 checkout path. Unit coverage should isolate the copied address shape and quote refresh inputs. Browser coverage should use authenticated fixture hooks from Plan 06-10 and assert that a country-changing saved address triggers the material-change preview instead of silently replacing the accepted quote.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/account/addresses.test.ts tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/quote-diff.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
  </verify>
  <done>Saved-address checkout tests exist and fail or list until selector wiring is implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement checkout selector and redirect support</name>
  <files>src/account/addresses.ts, src/components/checkout/saved-address-selector.tsx, src/components/checkout/checkout-page.tsx, src/i18n/routing.ts, src/proxy.ts, src/auth/redirect.ts, src/messages/en.json, src/messages/vi.json, tests/unit/account/addresses.test.ts, tests/unit/checkout/shipping-address-ui.test.ts, tests/unit/checkout/quote-diff.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - Selector appears only for signed-in customers with saved addresses in a physical checkout context.
    - Choosing an address copies the saved fields into the existing shipping form state.
    - Quote refresh and `QuoteDiffDialog` behavior are reused, not forked.
  </behavior>
  <action>Implement `SavedAddressSelector` as a compact checkout section that receives server-loaded saved addresses and a callback into `CheckoutPage`. It must copy the selected address into existing shipping state, call the existing quote refresh flow, and preserve the current material-change dialog before updating accepted quote. Add localized labels and safe sign-in/checkout redirect paths as needed. Do not change immutable order snapshot behavior or create a new quote API.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/account/addresses.test.ts tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/quote-diff.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Checkout saved-address reuse satisfies D-01 and D-03 while preserving Phase 3 material-change confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Account data to checkout UI | Customer-owned saved data is copied into untrusted browser state. |
| Checkout UI to quote action | Address-derived destination data crosses into authoritative quote recalculation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | `SavedAddressSelector`, `CheckoutPage` | mitigate | Reuse `refreshCheckoutQuoteAction` and `QuoteDiffDialog` so selected saved addresses cannot bypass server revalidation per D-03. |
| T-06-02-02 | Spoofing | Saved address ownership in checkout | mitigate | Load saved addresses through Plan 06-01 owner-scoped query; selector never accepts arbitrary address owner ids. |
| T-06-02-SC | Tampering | npm package surface | accept | No package install occurs. |
</threat_model>

<verification>
Plan gate: `npm run test:unit -- tests/unit/account/addresses.test.ts tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/quote-diff.test.ts && npm run test:e2e -- tests/e2e/account-retention.spec.ts --list && npm run typecheck`.
</verification>

<success_criteria>
ACC-03 checkout reuse is complete when saved-address selection copies address fields into checkout, immediately refreshes quote facts, and requires confirmation for material market, currency, price, shipping, or eligibility changes.
</success_criteria>

<output>
Create `.planning/phases/06-customer-retention-and-trust/06-02-SUMMARY.md` when done.
</output>
