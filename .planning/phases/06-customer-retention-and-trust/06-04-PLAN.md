---
phase: 06-customer-retention-and-trust
plan: "04"
type: execute
wave: 4
depends_on: ["06-03"]
files_modified:
  - src/account/wishlist-actions.ts
  - src/components/catalog/wishlist-heart.tsx
  - src/components/catalog/product-card.tsx
  - src/app/[locale]/product/[productSlug]/page.tsx
  - src/auth/redirect.ts
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/account/wishlist.test.ts
  - tests/e2e/account-retention.spec.ts
autonomous: true
requirements: [ACC-04]
must_haves:
  truths:
    - "Signed-in customers can add or remove wishlist entries from product cards and product detail pages per D-07."
    - "Guests using a heart are routed to localized sign-in and returned to the product or wishlist context per D-08."
    - "Phase 6 does not create guest wishlist state or merge behavior per D-08."
  artifacts:
    - path: "src/components/catalog/wishlist-heart.tsx"
      provides: "Accessible product-card/detail wishlist heart control"
    - path: "src/components/catalog/product-card.tsx"
      provides: "Catalog card heart integration without shifting card facts"
    - path: "src/app/[locale]/product/[productSlug]/page.tsx"
      provides: "Product detail heart integration near purchase CTA"
  key_links:
    - from: "src/components/catalog/wishlist-heart.tsx"
      to: "src/account/wishlist-actions.ts"
      via: "Server Action form/action"
      pattern: "addWishlistItemAction|removeWishlistItemAction"
    - from: "src/components/catalog/wishlist-heart.tsx"
      to: "src/auth/redirect.ts"
      via: "localized guest sign-in return"
      pattern: "next|sign"
---

## Phase Goal

**As a** shopper, **I want to** save products from discovery surfaces, **so that** wishlist management is available where purchase interest starts.

<objective>
Implement product-card and product-detail wishlist heart controls for ACC-04.

Purpose: D-07 and D-08 require wishlist management on catalog/product surfaces while preserving guest checkout boundaries.
Output: Reusable heart control, catalog/detail integration, guest sign-in return, and browser/unit coverage.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-customer-retention-and-trust/06-CONTEXT.md
@.planning/phases/06-customer-retention-and-trust/06-RESEARCH.md
@.planning/phases/06-customer-retention-and-trust/06-PATTERNS.md
@.planning/phases/06-customer-retention-and-trust/06-UI-SPEC.md
@.planning/phases/06-customer-retention-and-trust/06-03-SUMMARY.md
@src/components/catalog/product-card.tsx
@src/app/[locale]/product/[productSlug]/page.tsx
@src/auth/redirect.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wishlist heart behavior contracts</name>
  <files>tests/unit/account/wishlist.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - Signed-in product card/detail hearts call add/remove actions and reflect selected state.
    - Guest heart redirects to localized sign-in with safe return path.
    - No guest wishlist persistence or merge path exists.
  </behavior>
  <action>Extend ACC-04 tests for D-07 and D-08. Unit tests must cover add/remove action result mapping and guest redirect URL shaping. Browser contracts must cover product card and detail heart controls, selected state, 44px target, `aria-pressed`, and localized guest return behavior using Plan 06-10 fixtures.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/account/wishlist.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
  </verify>
  <done>Wishlist heart tests exist and explicitly reject guest wishlist persistence or merge behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement catalog and product detail heart controls</name>
  <files>src/account/wishlist-actions.ts, src/components/catalog/wishlist-heart.tsx, src/components/catalog/product-card.tsx, src/app/[locale]/product/[productSlug]/page.tsx, src/auth/redirect.ts, src/messages/en.json, src/messages/vi.json, tests/unit/account/wishlist.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - Heart control is accessible, localized, stable in layout, and does not compete with primary buy CTA.
    - Signed-in customers add/remove product-level wishlist entries.
    - Guests are prompted to sign in with a validated relative `next`.
  </behavior>
  <action>Implement `WishlistHeart` with existing lucide heart icon, `aria-pressed`, accessible names, pending state, and 44px hit area. Integrate it into `ProductCard` image/card boundary and the product detail title/CTA group without shifting title, price, badges, or primary purchase action. Use `addWishlistItemAction` and `removeWishlistItemAction` from Plan 06-03 for signed-in users. For guests, redirect to localized sign-in with safe return context; do not create guest wishlist storage or cart-like merge UI.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/account/wishlist.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Wishlist product-surface controls satisfy D-07 and D-08 with no guest wishlist state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public product surfaces to wishlist actions | Product ids from public pages cross into authenticated account mutation. |
| Guest heart to auth redirect | Anonymous intent crosses into localized sign-in return handling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Elevation of Privilege | `WishlistHeart`, `wishlist-actions.ts` | mitigate | Mutations call server actions that derive owner from `requireUser`; product id is the only browser-supplied identifier. |
| T-06-04-02 | Spoofing | Guest sign-in return | mitigate | Use existing safe redirect helpers and relative `next` paths; no guest wishlist records or merge path per D-08. |
| T-06-04-SC | Tampering | npm package surface | accept | No package install occurs. |
</threat_model>

<verification>
Plan gate: `npm run test:unit -- tests/unit/account/wishlist.test.ts && npm run test:e2e -- tests/e2e/account-retention.spec.ts --list && npm run typecheck`.
</verification>

<success_criteria>
ACC-04 product-surface wishlist behavior is complete when signed-in customers can add/remove products from cards and detail pages, guests are sent through localized sign-in return, and no guest wishlist or merge state exists.
</success_criteria>

<output>
Create `.planning/phases/06-customer-retention-and-trust/06-04-SUMMARY.md` when done.
</output>
