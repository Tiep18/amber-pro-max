---
phase: 06-customer-retention-and-trust
plan: "03"
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - supabase/migrations/*_customer_retention_trust.sql
  - supabase/tests/database/06_customer_retention.test.sql
  - src/types/supabase.ts
  - src/account/wishlist.ts
  - src/account/wishlist-actions.ts
  - src/app/[locale]/account/wishlist/page.tsx
  - src/app/[locale]/tai-khoan/yeu-thich/page.tsx
  - src/components/account/wishlist-page.tsx
  - src/i18n/routing.ts
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/account/wishlist.test.ts
  - tests/e2e/account-retention.spec.ts
autonomous: true
requirements: [ACC-04]
must_haves:
  truths:
    - "Signed-in customer can view and remove product-level wishlist entries from account per D-05 and D-07."
    - "Wishlist rows reload current catalog facts, market availability, price, stock, and variant state when rendered per D-05 and D-06."
    - "Products unavailable in the active market stay visible but cannot be checked out directly per D-06."
  artifacts:
    - path: "supabase/migrations/*_customer_retention_trust.sql"
      provides: "Wishlist table, product-level uniqueness, RLS, grants, and indexes"
    - path: "src/account/wishlist.ts"
      provides: "Owner-scoped wishlist current-catalog hydration"
      exports: ["getCustomerWishlist"]
    - path: "src/account/wishlist-actions.ts"
      provides: "Server actions for product-level wishlist remove and shared mutation helpers"
    - path: "src/components/account/wishlist-page.tsx"
      provides: "Localized account wishlist display"
  key_links:
    - from: "src/account/wishlist.ts"
      to: "public catalog projections"
      via: "current product facts query"
      pattern: "catalog|product"
    - from: "src/account/wishlist-actions.ts"
      to: "wishlist_items"
      via: "server-owned authenticated user id"
      pattern: "requireUser|wishlist_items"
---

## Phase Goal

**As a** returning customer, **I want to** revisit saved products with current store facts, **so that** my wishlist stays useful across market and inventory changes.

<objective>
Implement wishlist storage and account rendering for ACC-04.

Purpose: Wishlist must be an account feature that stores product references only and hydrates current catalog state when rendered.
Output: Wishlist schema, RLS, query/actions, localized account wishlist page, and tests for D-05, D-06, and account-side D-07.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-customer-retention-and-trust/06-CONTEXT.md
@.planning/phases/06-customer-retention-and-trust/06-RESEARCH.md
@.planning/phases/06-customer-retention-and-trust/06-PATTERNS.md
@.planning/phases/06-customer-retention-and-trust/06-UI-SPEC.md
@.planning/phases/06-customer-retention-and-trust/06-02-SUMMARY.md
@src/fulfillment/account-queries.ts
@src/components/fulfillment/pattern-library.tsx
@src/catalog/queries.ts
</context>

<migration_strategy>
Append wishlist tables and indexes to the shared `*_customer_retention_trust.sql` migration created by Plan 06-01, preserving the `06-PATTERNS.md` migration naming convention.
</migration_strategy>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wishlist account contracts</name>
  <files>supabase/tests/database/06_customer_retention.test.sql, tests/unit/account/wishlist.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - Wishlist stores owner id, product id, and timestamps only; no price/title/stock/variant snapshots.
    - Account wishlist hydrates current catalog facts for active locale and market.
    - Unavailable-market products remain visible with disabled checkout action.
  </behavior>
  <action>Add RED contracts for ACC-04 and D-05 through D-07 account behavior. Database tests must assert `wishlist_items`, product-level uniqueness per owner/product, owner RLS, and absence of commercial snapshot columns. Unit tests must cover hydration mapping, unavailable-market state, remove result mapping, and no direct checkout action for ineligible products. Browser contracts must cover localized account wishlist display using authenticated fixture hooks from Plan 06-10.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/account/wishlist.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
  </verify>
  <done>Wishlist account contracts exist and are traceable to ACC-04 and D-05 through D-07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement wishlist schema, actions, and account page</name>
  <files>supabase/migrations/*_customer_retention_trust.sql, src/types/supabase.ts, src/account/wishlist.ts, src/account/wishlist-actions.ts, src/app/[locale]/account/wishlist/page.tsx, src/app/[locale]/tai-khoan/yeu-thich/page.tsx, src/components/account/wishlist-page.tsx, src/i18n/routing.ts, src/messages/en.json, src/messages/vi.json, supabase/tests/database/06_customer_retention.test.sql, tests/unit/account/wishlist.test.ts, tests/e2e/account-retention.spec.ts</files>
  <behavior>
    - `wishlist_items` stores product references only and is owner scoped.
    - Account page renders current localized product facts, price, stock, market eligibility, and variant status.
    - Remove action is authenticated, non-blocking, and does not touch cart state.
  </behavior>
  <action>Append `wishlist_items` to the shared migration per D-05 and D-06. Do not add `price_minor`, `currency_code`, localized title, stock, variant snapshot, or checkout snapshot columns. Implement `getCustomerWishlist` so it joins or queries through existing catalog-safe projections for active locale and market. Build localized account wishlist routes and rows/cards using `06-UI-SPEC.md`: stable image aspect ratio, current facts, unavailable status pill, disabled checkout action, and labeled remove control. Regenerate Supabase types after migration changes.</action>
  <verify>
    <automated>npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts</automated>
    <automated>npm run test:unit -- tests/unit/account/wishlist.test.ts</automated>
    <automated>npm run test:e2e -- tests/e2e/account-retention.spec.ts --list</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Account wishlist stores product references only, renders current catalog facts, and prevents direct checkout for unavailable-market products.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser to wishlist actions | Untrusted product ids cross into account-owned wishlist mutations. |
| Wishlist rows to catalog display | Stored references are hydrated into market-aware public product facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Elevation of Privilege | `wishlist_items`, `wishlist-actions.ts` | mitigate | Use `requireUser`, owner RLS, product-level unique index, and database ownership tests. |
| T-06-03-02 | Information Disclosure | `wishlist_items`, `wishlist-page.tsx` | mitigate | Store only product references and hydrate current catalog facts; reject snapshot columns per D-05. |
| T-06-03-03 | Tampering | Unavailable-market wishlist item actions | mitigate | Render unavailable products with disabled checkout action while keeping remove available per D-06. |
| T-06-03-SC | Tampering | npm package surface | accept | No package install occurs. |
</threat_model>

<verification>
Plan gate: `npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && git diff --exit-code src/types/supabase.ts && npm run test:unit -- tests/unit/account/wishlist.test.ts && npm run test:e2e -- tests/e2e/account-retention.spec.ts --list && npm run typecheck`.
</verification>

<success_criteria>
ACC-04 account wishlist is complete when signed-in customers can see and remove product-level wishlist entries with current market-aware catalog facts and unavailable products cannot be directly checked out.
</success_criteria>

<output>
Create `.planning/phases/06-customer-retention-and-trust/06-03-SUMMARY.md` when done.
</output>
