---
phase: 08-shipping-profile-fallbacks-destination-zones-and-us-region-s
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260712080100_shipping_profile_fallbacks_regions.sql
  - supabase/tests/database/08_shipping_schema.test.sql
  - supabase/tests/database/08_shipping_allocation_schema.test.sql
  - supabase/tests/rehearsals/08_shipping_forward_repair.sql
autonomous: false
requirements: [SHIP-07, SHIP-08, SHIP-10, SHIP-12]
must_haves:
  truths:
    - "Existing exact-country rows retain the same profile, country, currency, active state, and integer fees after migration; no profile or rule is rewritten into a default or fallback (D-03, D-04)."
    - "The database permits zero active store defaults before explicit admin selection and prevents more than one active default; it never auto-selects a profile (D-03)."
    - "A profile has at most one fallback per currency, exact and fallback rule shapes cannot be ambiguous, and normalized region adjustments support surcharge or replace (D-02, D-05, D-06)."
    - "Shipping configuration and allocation evidence are private under RLS; checkout will use constrained functions rather than direct table reads (D-09)."
    - "The approved non-production Supabase project accepts the additive migration before any dependent plan executes."
    - "A disposable non-production failure/rollback/reapply and forward-repair rehearsal preserves exact-country row checksums plus the expected exact/fallback constraints and indexes."
  artifacts:
    - path: "supabase/migrations/20260712080100_shipping_profile_fallbacks_regions.sql"
      provides: "Additive defaults, fallback rule shape, region adjustments, immutable allocation storage, constraints, indexes, RLS, grants, and atomic default replacement"
      contains: "shipping_region_adjustments"
    - path: "supabase/tests/database/08_shipping_schema.test.sql"
      provides: "Compatibility, uniqueness, normalization, RLS, and grant contracts"
      contains: "no existing profile is selected as default"
    - path: "supabase/tests/database/08_shipping_allocation_schema.test.sql"
      provides: "Allocation table shape, immutability, and ownership-policy contracts"
    - path: "supabase/tests/rehearsals/08_shipping_forward_repair.sql"
      provides: "Non-production forced-failure rollback, clean reapply, and forward-only constraint repair assertions"
  key_links:
    - from: "public.shipping_store_defaults.shipping_profile_id"
      to: "public.shipping_profiles.id"
      via: "restricting foreign key plus active-default trigger/RPC validation"
      pattern: "shipping_store_defaults.*shipping_profile_id"
    - from: "public.shipping_region_adjustments.shipping_rule_id"
      to: "public.shipping_rules.id"
      via: "currency-inheriting parent rule and normalized country/region checks"
      pattern: "shipping_region_adjustments.*shipping_rule_id"
    - from: "public.checkout_order_shipping_allocations.order_line_id"
      to: "public.checkout_order_lines.id"
      via: "one immutable evidence row per submitted physical order line"
      pattern: "checkout_order_shipping_allocations.*order_line_id"
---

# Phase 08 Plan 01: Additive shipping schema and compatibility gate

<objective>
Create the additive Supabase foundation for explicit store defaults, exact/fallback destination rules, generic region adjustments, and immutable shipping-allocation evidence without changing existing exact rows or selecting a default.

Purpose: Establish database invariants and least-privilege boundaries before resolver, API, admin, or checkout behavior depends on them.
Output: One additive migration, Wave 0 schema pgTAP contracts, a forward-repair rehearsal, and recorded evidence that the migration was pushed to an explicitly approved non-production project. Generated-type ownership remains in Plan 08-03 so every source file has one owner.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-CONTEXT.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-RESEARCH.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-PATTERNS.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-VALIDATION.md
@supabase/migrations/20260615030000_shipping_profiles.sql
@supabase/migrations/20260615032000_checkout_orders_reservations.sql
@supabase/migrations/20260618093000_checkout_shipping_address_snapshot.sql
@supabase/tests/database/03_checkout_quote.test.sql
@supabase/tests/database/03_checkout_model.test.sql

<interfaces>
Existing objects that must remain compatible: public.shipping_profiles(id,name,description,active), public.shipping_rules(id,profile_id,country_code,currency_code,first_item_fee_minor,additional_item_fee_minor,active), public.product_shipping_profiles(product_id,profile_id), public.variant_shipping_profiles(variant_id,profile_id), public.checkout_orders, and public.checkout_order_lines. Existing exact rules are unique by profile_id/country_code/currency_code and money remains non-negative integer minor units in VND or USD.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - write failing database compatibility and invariant contracts</name>
  <read_first>
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-VALIDATION.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-PATTERNS.md
@supabase/migrations/20260615030000_shipping_profiles.sql
@supabase/tests/database/03_checkout_quote.test.sql
@supabase/tests/database/03_checkout_model.test.sql
  </read_first>
  <files>supabase/tests/database/08_shipping_schema.test.sql, supabase/tests/database/08_shipping_allocation_schema.test.sql, supabase/tests/rehearsals/08_shipping_forward_repair.sql</files>
  <behavior>
    - SHIP-07 / D-03: migration starts with zero defaults, accepts one active default only when explicitly inserted, rejects a second active default, and rejects an inactive profile as the active default.
    - SHIP-08 / D-02 / D-04: pre-migration exact fixtures keep identical IDs, country codes, currencies, fees, and active flags; exact and fallback shapes are mutually exclusive; a second fallback for one profile/currency is rejected; no fallback is synthesized.
    - SHIP-10 / D-05 / D-06: region country/code values are uppercase two-letter codes, mode is only surcharge or replace, amounts are non-negative integer minor units, and duplicate active adjustment membership for one parent rule/country/region is rejected.
    - SHIP-12 / D-09: allocation evidence has complete profile/rule/region/base/final/quantity/allocated-total columns and update/delete attempts cannot mutate accepted evidence.
    - T-08-01/T-08-02: anon/authenticated callers cannot read or mutate base configuration/allocation tables directly; admin policies and service-role grants remain explicit; callable functions have fixed search_path and least-privilege grants.
    - Migration repair: a forced exception after applying the migration body rolls the transaction back without changing exact-row checksums or legacy constraints; the clean reapply succeeds; a forward-only repair restores a deliberately removed Phase 8 index/check without changing exact rows.
  </behavior>
  <action>Create both schema-focused pgTAP files with deterministic UUID fixtures. In 08_shipping_schema.test.sql, insert exact US/USD and VN/VND rows before exercising migration-era additions, snapshot every original semantic value, and assert those values remain byte-for-byte equal after new default/fallback/region rows are exercised. Include has_table/has_column/index/policy/function assertions, throws_ok SQLSTATE checks for every duplicate or malformed shape, and explicit tests that shipping_store_defaults begins empty. In 08_shipping_allocation_schema.test.sql, assert the allocation evidence columns and immutable trigger contract; leave resolver population and submit-population behavior to the separately owned Plan 08-02 and 08-03 test files, but do not skip schema or tamper checks. Create supabase/tests/rehearsals/08_shipping_forward_repair.sql for a disposable non-production database: capture ordered exact-rule rows plus pg_get_constraintdef/pg_get_indexdef checksums, apply the migration body in one transaction followed by a deliberate exception, assert rollback left data/catalog checksums unchanged and no migration-history row, apply the canonical migration cleanly, deliberately remove one Phase 8 check/index inside the disposable rehearsal, apply forward-only idempotent repair statements, and assert the exact rows plus all intended constraints/indexes are restored. The rehearsal must never target production, never delete migration history, and never use a destructive down migration. Run RED against the current schema and confirm failures are caused by missing Phase 8 objects, not fixture or syntax errors. Cite D-02 through D-06 and T-08-01/T-08-02 adjacent to assertions.</action>
  <verify>
    <automated>npm run db:reset && npm run db:test</automated>
  </verify>
  <done>Both pgTAP files parse, deterministic compatibility fixtures exist, and the current-schema run fails only because the Phase 8 schema/invariants are absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement additive schema, RLS/grants, compatibility, and generated types</name>
  <read_first>
@supabase/tests/database/08_shipping_schema.test.sql
@supabase/tests/database/08_shipping_allocation_schema.test.sql
@supabase/migrations/20260615030000_shipping_profiles.sql
@supabase/migrations/20260615032000_checkout_orders_reservations.sql
@supabase/migrations/20260618093000_checkout_shipping_address_snapshot.sql
@src/types/supabase.ts
  </read_first>
  <files>supabase/migrations/20260712080100_shipping_profile_fallbacks_regions.sql, supabase/tests/database/08_shipping_schema.test.sql, supabase/tests/database/08_shipping_allocation_schema.test.sql, supabase/tests/rehearsals/08_shipping_forward_repair.sql</files>
  <action>Implement one forward-only additive migration. Preserve product_shipping_profiles and variant_shipping_profiles ownership exactly per D-01 and preserve all existing shipping_rules semantic values per D-04. Add shipping_store_defaults with a partial unique index allowing zero or one active row, an FK to shipping_profiles, and an atomic public.admin_set_shipping_store_default(p_profile_id uuid) function that requires private.is_admin(), locks the singleton scope, rejects inactive profiles, and never chooses a profile implicitly per D-03. Extend shipping_rules with match_kind constrained to exact_country|fallback while backfilling existing rows only to exact_country; make country_code nullable only for fallback and enforce exact requires ^[A-Z]{2}$ while fallback requires null. Replace the old broad unique constraint with partial unique indexes for exact (profile_id,country_code,currency_code) and fallback (profile_id,currency_code), retaining active lookup indexes. Add shipping_region_adjustments keyed to one shipping_rule with country_code, region_code, mode surcharge|replace, first_item_fee_minor, additional_item_fee_minor, active, timestamps, normalized checks, and one active adjustment per rule/country/region per D-05/D-06. Add checkout_order_shipping_allocations with order/order-line links, source_tier variant|product|store_default, profile ID/name snapshot, rule ID/match_kind, destination/currency, base fees, optional region adjustment evidence, final fees, quantity, first-item winner units, and allocated_shipping_minor; use an immutable before update/delete trigger and constraints that prevent incomplete or negative evidence per D-09. Enable RLS on every new table, grant authenticated admin CRUD only behind private.is_admin(), grant service_role explicitly, expose no anon table privileges, harden functions with fixed search_path, and add revoke/grant statements. Declare the stable public.get_checkout_shipping_quote_v2(jsonb,text,text,text) JSON contract as a fail-closed resolver_not_ready wrapper with explicit revoke/grant and fixed search_path; Plan 08-02 replaces only the hardened resolver/wrapper bodies without changing the public signature, and Plan 08-03 exclusively regenerates src/types/supabase.ts after all Phase 8 database interfaces exist. Keep the migration transaction-safe and make every repair statement in the rehearsal forward-only/idempotent. Do not add payment, FX, carrier, postal-zone, or package-splitting objects.</action>
  <verify>
    <automated>npm run db:reset && npm run db:lint && npm run db:test && npm run test:security</automated>
  </verify>
  <done>The additive migration passes lint and pgTAP, original exact fixtures remain unchanged, zero defaults remain valid until explicit selection, duplicate/ambiguous configuration fails, allocation evidence is immutable, and RLS/grants are least privilege.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: [BLOCKING] Approve and verify non-production Supabase db push</name>
  <what-built>The additive Phase 8 migration, compatibility tests, RLS/grants, allocation evidence schema, and generated types are locally green. This gate exists because remote project identity and a mutating schema push cannot be inferred safely.</what-built>
  <how-to-verify>
1. The executor must present `supabase projects list`, `supabase status`, the currently linked project ref, `supabase migration list --linked`, and `supabase db push --dry-run --linked` output (or the installed CLI's non-mutating equivalent). The target must be explicitly identified as non-production; a production or ambiguous target blocks the plan.
2. Confirm the linked non-production project ref by replying `approved non-production <project-ref>`. Do not approve a ref that differs from the intended test/staging project.
3. Before the canonical push, use a disposable branch/database derived from the approved non-production environment to run `supabase/tests/rehearsals/08_shipping_forward_repair.sql`: capture exact-row/catalog checksums, force a transactional migration failure and prove rollback, reapply cleanly, then perform the forward-only repair drill and prove the same exact rows plus intended constraints/indexes survive. Production, shared staging data, migration-history deletion, and down migrations are forbidden.
4. After the rehearsal passes, the executor runs `supabase db push --linked`, then `supabase migration list --linked`, `npm run db:test`, and exact/fallback/region/default smoke queries against that same ref. Record the approved ref, pre/failure/reapply/repair checksums, and command evidence in 08-01-SUMMARY.md; never record keys or secrets. Plan 08-03 owns the single generated-type refresh.
5. Any destructive diff, wrong target, failed/irreproducible repair rehearsal, policy/grant mismatch, changed exact fixture, missing constraint/index, or HIGH/CRITICAL security finding blocks all dependent plans.
  </how-to-verify>
  <resume-signal>Reply `approved non-production <project-ref>` to authorize the push, or describe the target/migration issue.</resume-signal>
</task>

</tasks>

<source_audit>
| Source | ID/item | Plan coverage | Status |
|--------|---------|---------------|--------|
| GOAL | Resilient shipping configuration with explicit/default/fallback/region policy | Additive invariant foundation | COVERED |
| REQ | SHIP-07 | Explicit zero-or-one active store default and atomic replacement | COVERED |
| REQ | SHIP-08 | Exact/fallback shape and uniqueness per profile/currency | COVERED |
| REQ | SHIP-10 | Generic normalized surcharge/replace region storage | COVERED |
| REQ | SHIP-12 | Immutable allocation evidence schema | COVERED |
| CONTEXT | D-01..D-06, D-09 | Ownership, parcel/rule split, no auto-default, exact compatibility, region modes, server-private evidence | COVERED |
| RESEARCH | Additive migration, no fake country, partial uniqueness, RLS/grants, failed/reapply forward-repair rehearsal, remote push | Tasks 1-3; generated types are exclusively owned by 08-03 | COVERED |
| EXCLUDED | Payment/FX, providers, carriers, postal remote zones, package splitting | No artifact or task implements these deferred/out-of-scope items | EXCLUDED |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/server action -> Postgres | Untrusted IDs, codes, modes, and fee values cross into privileged shipping configuration. |
| Checkout caller -> Data API | Anonymous/authenticated callers must not bypass constrained projections to read private shipping tables. |
| Migration executor -> linked Supabase project | A mutating push can affect the wrong environment unless project identity is explicitly confirmed. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | Defaults, fallbacks, region rules | mitigate | Database checks, partial unique indexes, normalized codes, non-negative integer fees, FK integrity, atomic default replacement, and pgTAP negative cases prevent ambiguous configuration. |
| T-08-02 | Information Disclosure / Elevation | Shipping tables, RLS, grants, functions | mitigate | Enable RLS, revoke direct anon access, require private.is_admin() for authenticated writes, grant only constrained function execution, fix search_path, and test role access. |
| T-08-01-PUSH | Tampering / Elevation | Remote schema deployment | mitigate | Blocking human confirmation of a non-production project ref precedes db push; dry-run and post-push migration/policy checks are mandatory. |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No package install or new dependency is introduced. |

ASVS L1 gate: every authorization, RLS, input-shape, integrity, and error-redaction test must pass. Any HIGH/CRITICAL finding, wrong-environment evidence, or failed T-08-01/T-08-02 mitigation blocks completion.
</threat_model>

<verification>
Local gate: `npm run db:reset && npm run db:lint && npm run db:test && npm run test:security`.

Remote gate: approved non-production project identity, disposable forced-failure rollback plus clean reapply and forward-only repair rehearsal with unchanged exact-row/catalog checksums, clean dry-run, successful `supabase db push --linked`, matching migration status, remote smoke cases, and regenerated types recorded in the summary.
</verification>

<success_criteria>
- Existing exact-country rows are semantically unchanged and no default/fallback is auto-created.
- Default, fallback, region, and immutable allocation constraints reject every ambiguous or malformed fixture.
- RLS, grants, function hardening, db lint, pgTAP, and security tests pass; generated types are refreshed once by the sole owner in 08-03.
- The blocking non-production push and post-push checks succeed against the approved project ref before Plan 08-02 starts.
- The non-production rollback/reapply and forward-repair rehearsal proves exact-country rows and intended constraints/indexes survive failure and repair without a down migration.
</success_criteria>

<output>
Create `.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-01-SUMMARY.md` with local and non-production push evidence when done.
</output>
