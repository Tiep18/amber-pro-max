---
phase: 08-shipping-profile-fallbacks-destination-zones-and-us-region-s
plan: "07"
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - src/checkout/shipping-address.ts
  - src/checkout/shipping-address-ui.ts
  - tests/unit/checkout/shipping-address-ui.test.ts
autonomous: true
requirements: [SHIP-11, SHIP-13]
must_haves:
  truths:
    - "Country-only physical quote preview accepts a normalized two-letter country without requiring recipient, street, US state, or postal fields (D-07, D-08)."
    - "Final US physical submit requires one controlled normalized state/territory code and a nonblank valid postal code; non-US and digital-only final validation remain unchanged (D-08)."
    - "The canonical US option set contains exactly 50 states, DC, AS, GU, MP, PR, and VI, with unique two-letter uppercase codes and stable localized label keys."
    - "Free-form state names, lowercase/unrecognized codes, malformed country, and invalid postal input return stable field issue codes rather than silently normalizing unsupported values."
    - "Address parsing never accepts shipping prices, profile/rule IDs, adjustment modes, or quote evidence from the browser (D-09)."
  artifacts:
    - path: "src/checkout/shipping-address.ts"
      provides: "Owned shipping-address schema and preview/final validation modes"
      exports: ["shippingAddressSchema", "ShippingAddress", "validateShippingDestination"]
    - path: "src/checkout/shipping-address-ui.ts"
      provides: "Canonical country and US state/territory options plus field issue mapping"
      exports: ["US_SHIPPING_REGION_OPTIONS", "getShippingCountryOptions", "validateCheckoutShippingAddress"]
    - path: "tests/unit/checkout/shipping-address-ui.test.ts"
      provides: "Country-only preview, US final, option-list, normalization, and regression coverage"
  key_links:
    - from: "validateCheckoutShippingAddress"
      to: "shippingAddressSchema"
      via: "explicit preview or final mode, never duplicated field rules"
      pattern: "mode.*preview|final"
    - from: "US_SHIPPING_REGION_OPTIONS"
      to: "final US validation"
      via: "one canonical code allowlist"
      pattern: "US_SHIPPING_REGION_OPTIONS"
---

# Phase 08 Plan 07: Address schemas and normalized US destinations

<objective>
Define and prove the checkout address contracts for country-only quote preview, canonical US state/territory options, and strict final US physical submission.

Purpose: Separate destination validity from asynchronous quote state so all later checkout orchestration and UI consume one normalized, testable boundary.
Output: Shipping address schemas/options/issue codes and focused unit coverage; no checkout component or localization-message changes.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-CONTEXT.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-PATTERNS.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-UI-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-VALIDATION.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-03-SUMMARY.md
@src/checkout/shipping-address.ts
@src/checkout/shipping-address-ui.ts
@tests/unit/checkout/shipping-address-ui.test.ts

<interfaces>
Existing exports are shippingAddressSchema, ShippingAddress, getShippingCountryOptions, and validateCheckoutShippingAddress. Plan 08-03 owns submitCheckoutInputSchema and database final enforcement; this plan owns reusable address-domain validation and must match the Plan 08-03 result codes without modifying its files. UI translation strings and rendering are owned by 08-09.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - specify preview, final, US option, and invalid-input behavior</name>
  <read_first>
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-UI-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-VALIDATION.md
@src/checkout/shipping-address.ts
@src/checkout/shipping-address-ui.ts
@tests/unit/checkout/shipping-address-ui.test.ts
  </read_first>
  <files>tests/unit/checkout/shipping-address-ui.test.ts</files>
  <behavior>
    - SHIP-11 / D-07: preview mode accepts a valid country alone and rejects malformed country before any quote request.
    - SHIP-13 / D-08: final physical US mode rejects missing/unknown region and blank/malformed postal; valid controlled region+postal succeeds; preview US does not require either.
    - The option list contains exactly 56 unique uppercase codes: 50 states, DC, AS, GU, MP, PR, VI; every entry has a stable label key and deterministic ordering.
    - Lowercase, full state names, whitespace-only, duplicate, or unrecognized region values are rejected rather than treated as authoritative controlled values.
    - Non-US final behavior, digital-only bypass, address formatting, and existing country options remain green.
    - T-08-05: unknown shipping evidence fields are stripped/rejected and no amount/profile/rule/adjustment input participates in validation.
  </behavior>
  <action>Extend the existing unit file with table-driven preview/final cases, a complete expected-code set, duplicate detection, deterministic ordering, US postal boundaries, non-US/digital regressions, and unknown-field rejection. Assert stable issue codes rather than localized copy. Run RED before schema/option changes.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/checkout/shipping-address-ui.test.ts</automated>
  </verify>
  <done>Unit contracts fail only for missing preview/final separation, canonical US options, or stricter normalized input behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement address-domain schemas, options, and issue mapping</name>
  <read_first>
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-03-SUMMARY.md
@src/checkout/shipping-address.ts
@src/checkout/shipping-address-ui.ts
@tests/unit/checkout/shipping-address-ui.test.ts
  </read_first>
  <files>src/checkout/shipping-address.ts, src/checkout/shipping-address-ui.ts, tests/unit/checkout/shipping-address-ui.test.ts</files>
  <action>Keep ShippingAddress as the owned normalized shape and add explicit preview versus final validation mode. Preview requires only normalized destination country needed by the quote boundary. Final mode retains existing recipient/address requirements and, only for physical US orders, requires a value from the canonical 56-code allowlist plus a nonblank postal value satisfying the established safe length/character contract per D-08. Do not require region/postal for first US quote, non-US region, or digital-only checkout. Export US_SHIPPING_REGION_OPTIONS with deterministic codes and stable label keys for Plan 08-09 localization; use the same set for validation so UI and server affordance cannot drift. Return stable field issue codes and reject unknown fields/price evidence. Preserve address formatting and existing country behavior. Do not modify submit schema, quote lifecycle, components, or messages.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/submit-checkout.test.ts && npm run lint && npm run typecheck && npm run test:security</automated>
  </verify>
  <done>Country-only preview, canonical US options, strict final US physical validation, non-US/digital regressions, type/lint, and security checks pass with stable issue codes.</done>
</task>

</tasks>

<source_audit>
| Source | ID/item | Plan coverage | Status |
|--------|---------|---------------|--------|
| GOAL | Predictable destination-aware checkout | One normalized address contract | COVERED |
| REQ | SHIP-11 | Country-only quote preview input | COVERED |
| REQ | SHIP-13 | Normalized US state/territory and postal at final submit | COVERED |
| CONTEXT | D-07, D-08, D-09 | Requote destination, preview-vs-final completeness, server authority | COVERED |
| RESEARCH | Controlled US list, normalized boundaries, country-first quote | Tasks 1-2 | COVERED |
| UI-SPEC | Controlled options and progressive field contract, excluding rendering/copy owned by 08-09 | Domain support | COVERED |
| EXCLUDED | Quote races, component UI, messages, payments, carriers, postal-zone pricing | Owned later or out of scope | EXCLUDED |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser address draft -> quote/final schemas | Country, region, postal, and unknown fields are untrusted. |
| Controlled option list -> final submit | UI affordance and server validation must share one canonical set. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Tampering | Address and quote intent validation | mitigate | Explicit preview/final schemas, canonical code allowlist, unknown-field rejection, bounded postal input, no browser price/config evidence. |
| T-08-SC | Tampering | Dependency surface | accept | Zod and existing code only; no install. |

ASVS L1 gate: normalized allowlists, bounded input, and server-authoritative submit regression must pass. Any HIGH/CRITICAL finding blocks completion.
</threat_model>

<verification>
`npm run test:unit -- tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/submit-checkout.test.ts && npm run lint && npm run typecheck && npm run test:security`
</verification>

<success_criteria>
- Country-only preview and strict final US physical validation differ exactly per D-08.
- The canonical deterministic 56-code set is the sole US region authority.
- Invalid/free-form/unknown inputs produce stable issues and cannot inject shipping evidence.
- Existing non-US, digital-only, address-formatting, and submit regressions stay green.
</success_criteria>

<output>
Create `.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-07-SUMMARY.md` when done.
</output>
