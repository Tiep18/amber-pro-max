---
phase: 08-shipping-profile-fallbacks-destination-zones-and-us-region-s
plan: "04"
type: execute
wave: 4
depends_on: ["08-03"]
files_modified:
  - src/checkout/admin-shipping-actions.ts
  - src/catalog/actions.ts
  - src/catalog/variant-actions.ts
  - tests/unit/checkout/admin-commerce-actions.test.ts
autonomous: true
requirements: [SHIP-07, SHIP-08, SHIP-10]
must_haves:
  truths:
    - "Every profile/default/rule/region/assignment mutation calls requireAdmin before creating a privileged client or reading/writing data."
    - "Profile fields are validated separately from destination rules; exact and Other countries shapes, currency, integer money, active state, and normalized US codes are server-owned (D-02, D-05, D-06)."
    - "First default selection and replacement call one atomic RPC, reject inactive profiles, permit zero defaults before explicit selection, and never auto-select (D-03)."
    - "Product and variant assignment actions preserve the existing ownership model: explicit UUID creates/replaces the owner row and store_default removes only that explicit row, leaving variant precedence intact (D-01)."
    - "Expected constraint/stale/reference failures map to allowlisted result codes; raw database details, notes, and unrelated records never reach admin UI."
  artifacts:
    - path: "src/checkout/admin-shipping-actions.ts"
      provides: "Authorized and validated profile/default/rule/region server actions"
      exports: ["saveShippingProfileAction", "setStoreDefaultShippingProfileAction", "saveShippingRuleAction", "saveShippingRegionAdjustmentAction"]
    - path: "src/catalog/actions.ts"
      provides: "Authorized product shipping-profile assignment action"
    - path: "src/catalog/variant-actions.ts"
      provides: "Authorized variant override assignment action"
    - path: "tests/unit/checkout/admin-commerce-actions.test.ts"
      provides: "Authorization ordering, normalization, RPC argument, ownership, and redaction coverage"
  key_links:
    - from: "setStoreDefaultShippingProfileAction"
      to: "public.admin_set_shipping_store_default"
      via: "requireAdmin followed by one atomic RPC"
      pattern: "rpc\\('admin_set_shipping_store_default'"
    - from: "saveShippingRuleAction"
      to: "shipping_rules"
      via: "exact_country with normalized country or fallback with null country"
      pattern: "match_kind"
    - from: "catalog assignment actions"
      to: "product_shipping_profiles / variant_shipping_profiles"
      via: "UUID upsert or deletion of only the explicit owner row"
      pattern: "shippingProfileId"
---

# Phase 08 Plan 04: Authorized shipping administration actions

<objective>
Implement only the protected server-action and RPC boundary for parcel profiles, explicit defaults, exact/fallback rules, region adjustments, and product/variant assignments.

Purpose: Centralize authorization, validation, atomic mutations, ownership semantics, and sanitized failures before either admin UI plan consumes them.
Output: Focused shipping/catalog server actions and unit/security evidence; no admin or checkout UI changes.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-CONTEXT.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-RESEARCH.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-PATTERNS.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-VALIDATION.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-03-SUMMARY.md
@src/checkout/admin-shipping-actions.ts
@src/catalog/actions.ts
@src/catalog/variant-actions.ts
@tests/unit/checkout/admin-commerce-actions.test.ts

<interfaces>
Follow the established boundary exactly: await requireAdmin() before createSupabaseServerClient or any query, Zod safeParse with unknown-field rejection, integer minor-unit conversion, runMonitoredAction, sanitized discriminated results, and targeted revalidatePath/revalidateTag. Database authority is admin_set_shipping_store_default plus RLS/constraints from Plans 08-01..03. Existing product and variant actions must retain all catalog, pricing, inventory, and publish behavior.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 - specify authorization, validation, ownership, and redaction</name>
  <read_first>
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-SPEC.md
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-VALIDATION.md
@src/checkout/admin-shipping-actions.ts
@src/catalog/actions.ts
@src/catalog/variant-actions.ts
@tests/unit/checkout/admin-commerce-actions.test.ts
  </read_first>
  <files>tests/unit/checkout/admin-commerce-actions.test.ts</files>
  <behavior>
    - SHIP-07 / D-03 / T-08-08: requireAdmin precedes every client/query/mutation; default selection/replacement invokes admin_set_shipping_store_default exactly once; inactive/default-delete/duplicate/stale failures are sanitized.
    - SHIP-08 / D-02: profile save accepts parcel fields only; exact_country requires normalized country; fallback requires null country; duplicate exact/fallback and invalid currency/money return allowlisted field codes.
    - SHIP-10 / D-05/D-06: region association, country, normalized US region membership, surcharge|replace mode, and separate first/additional non-negative amounts are validated before mutation.
    - D-01: product and variant UUID assignment upserts only the matching ownership row; store_default removes only that explicit row; variant override precedence is not replaced by a second boolean model.
    - Raw Postgres message/detail/hint, internal notes, and unrelated IDs are absent from every returned or monitored public fact object.
  </behavior>
  <action>Extend the existing hoisted mocks for requireAdmin, server client chains, RPCs, monitored failures, path/tag revalidation, and constraint responses. Assert call ordering, exact normalized RPC/table arguments, no mutation after invalid input, and no raw detail leakage. Add independent product and variant assignment cases without coupling them to UI components. Run RED before modifying actions.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/checkout/admin-commerce-actions.test.ts</automated>
  </verify>
  <done>Tests fail only for the missing Phase 8 action contracts and encode all required authorization, validation, ownership, atomicity, and redaction behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement profile, default, rule, and region actions</name>
  <read_first>
@.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-03-SUMMARY.md
@src/checkout/admin-shipping-actions.ts
@tests/unit/checkout/admin-commerce-actions.test.ts
  </read_first>
  <files>src/checkout/admin-shipping-actions.ts, tests/unit/checkout/admin-commerce-actions.test.ts</files>
  <action>Refactor profile input so name, description, active state, and existing parcel dimensions/weight are independent from destination rules per D-02. Export owned Zod schemas and sanitized action unions for profile save/deactivate/delete, set default, exact/fallback rule save/toggle/delete, and region adjustment save/toggle/delete. Every export calls requireAdmin before privileged access, rejects unknown fields, converts VND/USD display values to non-negative integer minor units, normalizes codes, and records only allowlisted monitored facts. Default mutation calls admin_set_shipping_store_default once, rejects inactive input, and never selects on create/load per D-03. Exact rules use a two-letter country; fallback uses null; no fake country code. Region actions persist generic country/region storage but accept only the Phase 8 US state/territory allowlist from admin input, with surcharge|replace and separate first/additional values per D-05/D-06. Map uniqueness, FK, current-default deletion, stale version, and referenced-record errors to stable codes. Revalidate /admin/shipping only after successful mutation. Do not create components or alter pricing/payment behavior.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/checkout/admin-commerce-actions.test.ts && npm run typecheck && npm run test:security</automated>
  </verify>
  <done>Profile/default/rule/region actions are authorized, normalized, atomic where required, and sanitized; unit/type/security checks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement product and variant assignment actions</name>
  <read_first>
@src/catalog/actions.ts
@src/catalog/variant-actions.ts
@src/checkout/admin-shipping-actions.ts
@tests/unit/checkout/admin-commerce-actions.test.ts
  </read_first>
  <files>src/catalog/actions.ts, src/catalog/variant-actions.ts, tests/unit/checkout/admin-commerce-actions.test.ts</files>
  <action>Add explicit product and variant shipping-profile assignment actions using the same requireAdmin-before-access, Zod, monitored redaction, and revalidation pattern. Accept either an active profile UUID or the sentinel store_default. UUID assignment creates/replaces only product_shipping_profiles or variant_shipping_profiles for that owner; store_default deletes only that explicit row so product inheritance or store-default inheritance remains visible. Never copy product assignment onto variants and never remove a variant override when changing a product assignment per D-01. Reject inactive/unknown profiles and stale owners with allowlisted codes. Revalidate affected catalog edit routes/tags plus /admin/shipping after success, without changing existing draft, price, inventory, publish, or archive behavior.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/checkout/admin-commerce-actions.test.ts && npm run lint && npm run typecheck && npm run test:security</automated>
  </verify>
  <done>Product/variant assignment actions preserve explicit ownership and inheritance exactly, expose no raw failures, and all focused/static/security checks pass.</done>
</task>

</tasks>

<source_audit>
| Source | ID/item | Plan coverage | Status |
|--------|---------|---------------|--------|
| GOAL | Admin can configure resilient shipping without direct database work | Protected mutation boundary consumed by 08-05/08-06 | COVERED |
| REQ | SHIP-07 | Explicit atomic default selection/replacement | COVERED |
| REQ | SHIP-08 | Exact/fallback rule validation and mutation | COVERED |
| REQ | SHIP-10 | Normalized surcharge/replace mutation | COVERED |
| CONTEXT | D-01..D-06 | Existing ownership, parcel/rule separation, no auto-default, exact compatibility, US-first generic region modes | COVERED |
| RESEARCH | requireAdmin, RLS/RPC authority, integer money, normalized input, sanitized failures | Tasks 1-3 | COVERED |
| EXCLUDED | UI, checkout lifecycle, payments, FX, carriers, postal zones, package splitting | No task modifies these surfaces | EXCLUDED |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin browser -> server actions | IDs, modes, codes, assignment scope, and money strings are untrusted. |
| Server actions -> privileged tables/RPCs | Authorization must precede every read/mutation and RLS remains authoritative. |
| Database/monitoring failure -> admin result | Raw SQL/internal configuration data must not leak. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Spoofing / Tampering / Information Disclosure | Admin shipping actions | mitigate | requireAdmin-before-access, Zod allowlists, integer money, normalized codes, atomic RPCs, RLS, monitored redaction, and exact unit assertions. |
| T-08-SC | Tampering | Dependency surface | accept | No package install or dependency is introduced. |

ASVS L1 gate: access control, validation, secure defaults, atomicity, and safe error handling must pass. Any HIGH/CRITICAL finding blocks completion.
</threat_model>

<verification>
`npm run test:unit -- tests/unit/checkout/admin-commerce-actions.test.ts && npm run lint && npm run typecheck && npm run test:security`
</verification>

<success_criteria>
- All admin mutations authorize before access and return only validated, sanitized unions.
- Default replacement is one atomic RPC and no action auto-selects a profile.
- Exact/fallback/region constraints are represented exactly at the server boundary.
- Product/variant assignment actions preserve D-01 ownership and inheritance without changing unrelated catalog behavior.
</success_criteria>

<output>
Create `.planning/phases/08-shipping-profile-fallbacks-destination-zones-and-us-region-s/08-04-SUMMARY.md` when done.
</output>
