---
phase: 04-trusted-payments-and-orders
plan: "03"
type: execute
wave: 3
depends_on:
  - "04-02"
files_modified:
  - supabase/migrations/20260618030000_checkout_shipping_address_snapshots.sql
  - src/types/supabase.ts
  - src/checkout/submit-checkout.ts
  - src/checkout/actions.ts
  - src/payments/queries.ts
  - tests/unit/payments/order-queries.test.ts
  - supabase/tests/database/04_checkout_shipping_address.test.sql
autonomous: true
requirements:
  - ORD-01
  - ORD-02
  - ORD-03
  - PAY-07
  - SEC-03
must_haves:
  truths:
    - "Physical and mixed orders store an immutable shipping-address snapshot per D-27."
    - "Digital-only orders store no shipping-address snapshot per D-23."
    - "Customer and admin projections can read the snapshot through scoped server-owned views per D-28 and D-29."
  artifacts:
    - path: "supabase/migrations/20260618030000_checkout_shipping_address_snapshots.sql"
      provides: "Address snapshot columns, constraints, immutable trigger, submit_checkout replacement, and projection updates"
    - path: "src/payments/queries.ts"
      provides: "Typed customer/admin order projection mapping including line summary and address snapshot"
    - path: "src/types/supabase.ts"
      provides: "Generated Supabase types after schema change"
  key_links:
    - from: "src/checkout/submit-checkout.ts"
      to: "public.submit_checkout(jsonb)"
      via: "shippingAddress payload"
      pattern: "shippingAddress"
    - from: "public.get_order_payment_status"
      to: "src/payments/queries.ts"
      via: "address snapshot projection"
      pattern: "shippingAddressSnapshot"
---

<objective>
Persist the address as order evidence at the same boundary that creates the payable order.

Purpose: Address data is only trustworthy if it is validated server-side and stored with the order before PayPal orders or VietQR instructions are created. This plan makes the snapshot immutable and visible through scoped projections.
Output: Additive migration, generated types, submit wrapper updates, and typed query projections for customer/admin order detail.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/03-mixed-cart-and-checkout/03-CONTEXT.md
@src/checkout/submit-checkout.ts
@src/checkout/actions.ts
@src/payments/queries.ts
@supabase/migrations/20260615032000_checkout_orders_reservations.sql
@supabase/migrations/20260615034000_trusted_payments_orders.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Store immutable shipping-address snapshots in submit_checkout</name>
  <files>supabase/migrations/20260618030000_checkout_shipping_address_snapshots.sql, src/types/supabase.ts, src/checkout/submit-checkout.ts, src/checkout/actions.ts, supabase/tests/database/04_checkout_shipping_address.test.sql</files>
  <behavior>
    - submit_checkout rejects physical/mixed orders without a complete normalized address snapshot.
    - submit_checkout accepts digital-only orders without address data and stores null address fields.
    - Stored snapshots include recipient name, phone, ISO country code, region, locality, address lines, postal code, and normalized display facts.
    - The snapshot cannot be updated or replaced after order creation.
  </behavior>
  <action>Create the additive migration declared in the file list. Add `shipping_address_snapshot jsonb`, `shipping_country_code text`, and any supporting generated/stored fields needed for query speed, with constraints and a trigger that prevents changes after insert. Replace `public.submit_checkout(jsonb)` in the new migration so it derives physical/mixed requirement from payable quote lines, validates the normalized snapshot from the server-parsed payload, rejects country mismatches between `shippingAddress.countryCode` and quote shipping country, stores no address for digital-only orders, and preserves the existing idempotency, reservation, exception-grant, and payment-shell behavior. Regenerate `src/types/supabase.ts` after the schema reset.</action>
  <verify>
    <automated>npm run db:reset</automated>
    <automated>npm run db:test</automated>
    <automated>npm run db:types</automated>
    <automated>npm run test:unit -- tests/unit/checkout/submit-checkout.test.ts tests/unit/checkout/shipping-address.test.ts</automated>
  </verify>
  <done>Database and wrapper tests prove D-22, D-23, D-24, and D-27 at the order creation boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose address snapshot through scoped order projections</name>
  <files>supabase/migrations/20260618030000_checkout_shipping_address_snapshots.sql, src/payments/queries.ts, tests/unit/payments/order-queries.test.ts</files>
  <behavior>
    - Customer status projection returns the address snapshot only for an authorized owner/guest/admin order.
    - Admin order projection returns the full snapshot and immutable line summaries for operational review.
    - Base payment/event/audit tables remain inaccessible to customers.
  </behavior>
  <action>Update `order_payment_statuses`, `get_order_payment_status`, and admin detail/timeline helpers to include customer-safe order lines and the normalized shipping-address snapshot while preserving owner, guest-cookie hash, and admin authorization rules. Extend `src/payments/queries.ts` types and mappers with `shippingAddressSnapshot`, `requiresShipping`, and order line summary fields. Keep provider events and raw audit data admin-only; the customer projection must not expose internal order/payment UUIDs beyond already approved public order number facts.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/order-queries.test.ts</automated>
    <automated>npm run db:test</automated>
    <automated>npm run test:security</automated>
  </verify>
  <done>Customer/admin query code can render D-28 and D-29 without bypassing RLS or leaking another customer's address.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| submit_checkout RPC | Browser payload becomes permanent order evidence and inventory reservation. |
| projection RPC/views | Address and line snapshots become visible to authorized customer/admin screens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Tampering | shipping_address_snapshot | mitigate | Validate in submit_checkout, reject country mismatches, and enforce immutable trigger. |
| T-04-08 | Information Disclosure | customer projection | mitigate | Return snapshots only through owner/guest/admin scoped RPC, not direct table access. |
| T-04-09 | Repudiation | address evidence | mitigate | Store normalized snapshot on order and include it in audit-visible order evidence. |
| T-04-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run database reset, database tests, type generation, focused unit tests, and security tests. Confirm `git diff src/types/supabase.ts` contains only generated type changes from the migration.
</verification>

<success_criteria>
Every physical or mixed payable order carries a server-validated immutable address snapshot, digital-only orders do not carry address data, and customer/admin projections expose the snapshot only through authorized reads.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-03-SUMMARY.md` when done.
</output>
