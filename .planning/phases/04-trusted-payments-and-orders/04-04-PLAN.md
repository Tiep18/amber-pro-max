---
phase: 04-trusted-payments-and-orders
plan: "04"
type: execute
wave: 4
depends_on:
  - "04-03"
files_modified:
  - supabase/migrations/20260615034000_trusted_payments_orders.sql
  - src/payments/schemas.ts
  - src/payments/types.ts
  - src/payments/transitions.ts
  - tests/integration/payment-concurrency.mjs
  - tests/security/payment-boundaries.test.mjs
  - supabase/tests/database/04_payment_model.test.sql
  - supabase/tests/database/04_payment_transitions.test.sql
  - supabase/tests/database/04_payment_rls_audit.test.sql
autonomous: true
requirements:
  - INV-04
  - INV-05
  - ORD-02
  - PAY-04
  - PAY-07
  - PAY-08
  - SEC-03
must_haves:
  truths:
    - "All paid, failed, cancelled, rejected, expired, duplicate, and late events flow through one idempotent command per D-01, D-15, and D-16."
    - "Inventory is finalized exactly once on paid and released or expired with retained reservation records per D-16 through D-18."
    - "Audit records cover money, stock, and paid-gate transitions without logging every view or click per D-19 and D-20."
  artifacts:
    - path: "supabase/migrations/20260615034000_trusted_payments_orders.sql"
      provides: "Payment model, transition RPC, inventory effects, audit, RLS, and expiry command"
    - path: "src/payments/transitions.ts"
      provides: "Typed adapter for apply_payment_transition"
    - path: "tests/integration/payment-concurrency.mjs"
      provides: "Race verification for exact-once payment and inventory outcomes"
  key_links:
    - from: "src/payments/transitions.ts"
      to: "public.apply_payment_transition(jsonb)"
      via: "RPC adapter"
      pattern: "apply_payment_transition"
    - from: "public.apply_payment_transition(jsonb)"
      to: "checkout_inventory_reservations"
      via: "finalize/release/expire updates"
      pattern: "status = 'consumed'|release_reason"
---

<objective>
Preserve and harden the shared payment state machine after adding address snapshots.

Purpose: The address correction must not weaken the payment authority that already protects order, inventory, paid gate, refunds visibility, and audit evidence.
Output: Payment model and transition code verified against the full D-01 through D-21 decision set and ready for PayPal/VietQR provider slices.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md
@.planning/phases/04-trusted-payments-and-orders/04-PATTERNS.md
@.planning/phases/04-trusted-payments-and-orders/04-UI-SPEC.md
@supabase/migrations/20260615034000_trusted_payments_orders.sql
@src/payments/schemas.ts
@src/payments/transitions.ts
@tests/integration/payment-concurrency.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-verify payment state families and audit invariants</name>
  <files>supabase/migrations/20260615034000_trusted_payments_orders.sql, src/payments/schemas.ts, src/payments/types.ts, src/payments/transitions.ts, supabase/tests/database/04_payment_model.test.sql, supabase/tests/database/04_payment_rls_audit.test.sql</files>
  <behavior>
    - Payment records and events remain the source of truth for paid transitions per D-01.
    - Customer statuses stay simple while admin status/evidence remains detailed per D-02 through D-04 and D-21.
    - Paid gate opens later fulfillment eligibility without creating entitlements, emails, or shipment tasks per D-05 and D-07.
    - Audit records are append-only for important money, stock, and access-right transitions per D-19 and D-20.
  </behavior>
  <action>Audit and adjust the existing payment migration, schemas, types, and adapter only where Plan 01 contracts expose drift. Keep separate order, payment, digital fulfillment, and physical fulfillment states; include `partially_refunded` and `refunded` visibility without adding refund initiation; keep `apply_payment_transition` service-role/admin controlled and safe-json validated; ensure no Phase 5 entitlement, email, download, or shipment records are created. If address snapshot facts are added to audit metadata, store only normalized non-secret evidence and avoid raw provider payloads.</action>
  <verify>
    <automated>npm run db:reset</automated>
    <automated>npm run db:test</automated>
    <automated>npm run test:unit -- tests/unit/payments/status-mapping.test.ts</automated>
    <automated>npm run test:security</automated>
  </verify>
  <done>Database and TypeScript contracts prove D-01 through D-05, D-19 through D-21, PAY-07, PAY-08, and SEC-03 remain intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove exact-once inventory outcomes under races</name>
  <files>supabase/migrations/20260615034000_trusted_payments_orders.sql, supabase/tests/database/04_payment_transitions.test.sql, tests/integration/payment-concurrency.mjs, tests/security/payment-boundaries.test.mjs</files>
  <behavior>
    - Duplicate paid transitions consume active reservations once and never decrement inventory twice per D-16.
    - Failed, cancelled, rejected, and expired transitions release or expire reservations without deleting them per D-06 through D-08 and D-17.
    - Released or expired reservations record timestamp, reason, and transition/source per D-18.
    - Late paid events after terminal release become review-required and keep fulfillment locked.
  </behavior>
  <action>Extend pgTAP and real local Supabase concurrency scenarios for paid/paid, expiry/paid, fail/paid, VietQR confirm/confirm, and confirm/reject races. Keep deterministic lock order across orders, payments, reservations, and inventory. Ensure terminal failed, cancelled, rejected, and expired orders cannot be retried on the same order per D-08; customer must create a new checkout with fresh price, stock, shipping, and discount. Preserve retained reservation rows for dispute/audit history.</action>
  <verify>
    <automated>npm run db:test</automated>
    <automated>node tests/integration/payment-concurrency.mjs</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Concurrency and transition tests prove INV-04, INV-05, PAY-04, and SEC-03 with no duplicate stock consumption or silent reservation deletion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| provider/admin/cron -> transition RPC | High-trust facts can change money, stock, paid gate, and audit state. |
| transition RPC -> inventory records | Payment state can decrement or release limited inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | apply_payment_transition | mitigate | Unique transition keys, event dedupe, deterministic row locks, and strict status matrix. |
| T-04-11 | Repudiation | commerce_audit_events | mitigate | Append-only audit for state-changing events with actor/source and sanitized facts. |
| T-04-12 | Denial of Service | expiry command | mitigate | Bounded expiry batches and idempotent deadline checks on every transition path. |
| T-04-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run DB reset/tests, focused unit tests, security suite, concurrency script, and typecheck. Any change that bypasses `applyPaymentTransition` for terminal payment/order/inventory mutation fails this plan.
</verification>

<success_criteria>
Payment, inventory, and audit authority remains a single idempotent transaction boundary, with exact-once finalization/release behavior and no fulfillment before confirmed paid state.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-04-SUMMARY.md` when done.
</output>
