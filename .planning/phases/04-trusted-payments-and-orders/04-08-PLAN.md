---
phase: 04-trusted-payments-and-orders
plan: "08"
type: execute
wave: 6
depends_on:
  - "04-05"
  - "04-06"
  - "04-07"
files_modified:
  - src/app/[locale]/orders/[orderNumber]/page.tsx
  - src/app/[locale]/don-hang/[orderNumber]/page.tsx
  - src/components/payments/order-payment-page.tsx
  - src/components/payments/payment-state-panel.tsx
  - src/components/payments/paypal-buttons.tsx
  - src/components/payments/vietqr-instructions.tsx
  - src/components/payments/order-address-summary.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/e2e/order-status.spec.ts
autonomous: true
requirements:
  - ORD-01
  - ORD-02
  - PAY-01
  - PAY-05
  - PAY-07
  - PAY-08
must_haves:
  truths:
    - "Customer order detail shows order number, status, immutable lines, totals, and payment method per ORD-01 and D-02."
    - "Physical and mixed customer order detail shows the immutable shipping-address snapshot per D-28."
    - "Every non-paid or review state shows fulfillment locked, and paid only opens eligibility without creating Phase 5 fulfillment artifacts per D-05 and D-07."
  artifacts:
    - path: "src/components/payments/order-payment-page.tsx"
      provides: "Localized customer order detail using server projections"
    - path: "src/components/payments/order-address-summary.tsx"
      provides: "Customer-safe address snapshot rendering"
    - path: "tests/e2e/order-status.spec.ts"
      provides: "Customer status, address visibility, PayPal verifying, and VietQR journeys"
  key_links:
    - from: "src/components/payments/order-payment-page.tsx"
      to: "src/payments/queries.ts"
      via: "getAuthorizedOrderPayment"
      pattern: "shippingAddressSnapshot"
    - from: "src/components/payments/paypal-buttons.tsx"
      to: "src/app/api/paypal/orders"
      via: "server-owned create/capture routes"
      pattern: "orderNumber"
---

<objective>
Render the customer-facing order and payment status page with the saved shipment destination.

Purpose: Customers need a calm, trustworthy order page that never claims payment before verification and lets physical/mixed buyers confirm the destination attached to the order.
Output: Localized customer order pages/components and browser coverage for payment statuses, fulfillment gate, order summary, and address visibility.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-UI-SPEC.md
@src/components/payments/order-payment-page.tsx
@src/components/payments/payment-state-panel.tsx
@src/components/payments/vietqr-instructions.tsx
@src/components/payments/paypal-buttons.tsx
@src/messages/en.json
@src/messages/vi.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Show localized order summary, status, and address snapshot</name>
  <files>src/app/[locale]/orders/[orderNumber]/page.tsx, src/app/[locale]/don-hang/[orderNumber]/page.tsx, src/components/payments/order-payment-page.tsx, src/components/payments/payment-state-panel.tsx, src/components/payments/order-address-summary.tsx, src/messages/en.json, src/messages/vi.json</files>
  <behavior>
    - Authorized owner or guest sees order number, status heading, immutable line summaries, totals, payment method, deadline, and fulfillment gate.
    - Physical/mixed order page shows recipient, phone, country, region, locality, address lines, and postal code from the snapshot.
    - Digital-only order page does not render an empty shipping-address section.
    - Unauthorized access shows the generic non-enumerating error.
  </behavior>
  <action>Render from `getAuthorizedOrderPayment` projection only; do not query base payment, event, audit, or order tables directly from customer UI. Add a reusable customer-safe `OrderAddressSummary` for physical/mixed orders and line/totals rendering from immutable snapshots. Keep localized routes under `/en/orders/[orderNumber]` and `/vi/don-hang/[orderNumber]`. Map status copy to the simple customer statuses from D-02, including paid, verifying, failed, cancelled, rejected, expired, partially refunded, refunded, and review-required. Do not display provider event names, merchant IDs, internal UUIDs, audit metadata, or raw address data beyond the customer's own saved destination.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/order-status.spec.ts --list</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Customer pages satisfy ORD-01, ORD-02, PAY-08, D-02, D-05, D-28, and the non-enumerating access boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve PayPal verifying and VietQR pending journeys</name>
  <files>src/components/payments/order-payment-page.tsx, src/components/payments/paypal-buttons.tsx, src/components/payments/vietqr-instructions.tsx, src/messages/en.json, src/messages/vi.json, tests/e2e/order-status.spec.ts</files>
  <behavior>
    - PayPal return or uncertain capture renders verifying with recheck/refresh and never paid before transition per D-09 and D-10.
    - VietQR pending page shows exact VND amount, reference, bank data, QR, deadline, copy controls, and no customer self-confirm button per D-12 and D-13.
    - Failed, cancelled, rejected, and expired states do not offer retry on the same order per D-08.
    - Fulfillment lock remains visible for non-paid/review states and paid only shows eligibility.
  </behavior>
  <action>Keep the PayPal button a client control that calls server create/capture routes and redirects or refreshes into server-projected status. Keep bounded recheck behavior and avoid infinite polling. Render VietQR instructions from `getVietQrInstructions` with keyboard-accessible copy buttons and exact deadline formatting. For terminal unpaid states, point the primary action to a fresh checkout/cart path rather than retrying the same order. Keep Phase 5 download, email, and shipment controls absent.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/order-status.spec.ts --list</automated>
    <automated>npm run test:unit -- tests/unit/payments/status-mapping.test.ts tests/unit/payments/vietqr.test.ts tests/unit/payments/paypal-buttons.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Customer payment page satisfies D-05 through D-10, D-12 through D-14, PAY-01, PAY-05, PAY-07, and PAY-08 without adding out-of-phase fulfillment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer order page -> projection RPC | Customer can see only authorized order, address, status, and safe payment facts. |
| customer UI -> payment routes | PayPal controls can request server routes but cannot set paid state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-22 | Information Disclosure | order detail | mitigate | Owner/guest/admin scoped projection and generic unauthorized page. |
| T-04-23 | Spoofing | PayPal return UI | mitigate | Server-projected verifying state until verified transition succeeds. |
| T-04-24 | Tampering | same-order retry | mitigate | Terminal unpaid states route to new checkout, not same order payment retry. |
| T-04-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run listed E2E listing, focused unit tests, and typecheck. During execution, turn relevant skipped browser contracts green only when local seeded fixtures are available.
</verification>

<success_criteria>
Authorized customers can inspect payment state, fulfillment gate, order lines, totals, and the saved shipping address for physical/mixed orders without any client-owned paid-state or fulfillment behavior.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-08-SUMMARY.md` when done.
</output>
