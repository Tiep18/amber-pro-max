---
phase: 04-trusted-payments-and-orders
plan: "08"
type: execute
wave: 5
depends_on:
  - 04-05
  - 04-07
files_modified:
  - tests/e2e/order-status.spec.ts
  - tests/e2e/admin-vietqr.spec.ts
  - src/components/payments/vietqr-instructions.tsx
  - src/components/payments/payment-state-panel.tsx
  - src/components/payments/order-payment-page.tsx
  - src/messages/en.json
  - src/messages/vi.json
autonomous: true
requirements:
  - ORD-01
  - PAY-05
  - PAY-06
  - PAY-07
  - PAY-08
must_haves:
  truths:
    - "Vietnam customer sees exact VND amount, immutable reference, deadline and manual-transfer instructions only from server snapshots."
    - "VietQR UI never lets the customer self-confirm payment."
    - "Wrong amount/reference states lead to admin rejection copy and a closed paid/fulfillment gate."
    - "All status, deadline and evidence copy exists in Vietnamese and English and does not rely on color alone."
  artifacts:
    - path: "src/components/payments/vietqr-instructions.tsx"
      provides: "Customer VietQR instruction display"
    - path: "tests/e2e/order-status.spec.ts"
      provides: "Customer order/payment status browser coverage"
    - path: "tests/e2e/admin-vietqr.spec.ts"
      provides: "VietQR admin action journey coverage"
  key_links:
    - from: "vietqr-instructions"
      to: "src/payments/vietqr/instructions.ts"
      via: "server snapshot projection"
      pattern: "exact amount/reference/deadline"
---

<objective>
Build the customer-facing VietQR instruction and status experience on the localized order page.

Purpose: Let Vietnam customers complete manual bank transfer with exact instructions while preserving the server-only paid gate.
Output: VietQR instruction component, localized copy and focused browser journey coverage.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-UI-SPEC.md
@.planning/phases/04-trusted-payments-and-orders/04-05-SUMMARY.md
@.planning/phases/04-trusted-payments-and-orders/04-07-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render exact VietQR instructions and customer status copy</name>
  <read_first>src/components/payments/vietqr-instructions.tsx, src/components/payments/payment-state-panel.tsx, src/components/payments/order-payment-page.tsx, src/components/checkout/checkout-page.tsx, src/messages/en.json, src/messages/vi.json, .planning/phases/04-trusted-payments-and-orders/04-UI-SPEC.md</read_first>
  <files>src/components/payments/vietqr-instructions.tsx, src/components/payments/payment-state-panel.tsx, src/components/payments/order-payment-page.tsx, src/messages/en.json, src/messages/vi.json</files>
  <action>
Implement `VietQrInstructions` with fixed 44px controls, masked bank/account display, exact VND amount, immutable transfer reference, deadline timer, copy action, reduced-motion QR placeholder/loading state, and fulfillment-lock messaging. Add localized messages for awaiting transfer, verifying admin review, rejected, expired, review-required and paid states. The component accepts only server projection fields and has no customer confirmation action.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['src/components/payments/vietqr-instructions.tsx','src/messages/en.json','src/messages/vi.json']){const s=fs.readFileSync(f,'utf8'); if(!/VietQR|vietqr|transfer/i.test(s)) throw new Error(f+' missing VietQR copy')} "</automated>
  </verify>
  <acceptance_criteria>
Source assertion: `src/components/payments/vietqr-instructions.tsx` contains no confirm/payment-success mutation call and renders amount, reference and deadline from props. Copy assertion: both locale JSON files contain VietQR awaiting, rejected and expired messages.
  </acceptance_criteria>
  <done>Customer VietQR order page displays exact instructions and keeps fulfillment locked before paid.</done>
</task>

<task type="auto">
  <name>Task 2: Cover customer VietQR and responsive order journeys</name>
  <read_first>tests/e2e/order-status.spec.ts, tests/e2e/admin-vietqr.spec.ts, playwright.config.ts, .planning/phases/04-trusted-payments-and-orders/04-UI-SPEC.md</read_first>
  <files>tests/e2e/order-status.spec.ts, tests/e2e/admin-vietqr.spec.ts</files>
  <action>
Complete Playwright scenarios for `/vi/don-hang/{orderNumber}` and `/en/orders/{orderNumber}` with VietQR awaiting, verifying, rejected, expired and paid-gate-closed states. Add mobile viewport checks at 375px and desktop checks at 1280px for no horizontal overflow, visible reference/amount/deadline and no color-only status. Keep implementation-dependent backend setup skipped with explicit reason until previous summaries provide seeded helpers.
  </action>
  <verify>
    <automated>npx playwright test --list tests/e2e/order-status.spec.ts tests/e2e/admin-vietqr.spec.ts</automated>
  </verify>
  <acceptance_criteria>
CLI assertion: Playwright lists VietQR customer and admin scenarios without syntax/config errors. Source assertion: specs include both `/vi/don-hang/` and `/en/orders/` route expectations and a 375px viewport.
  </acceptance_criteria>
  <done>Browser specs enumerate customer VietQR journeys and responsive checks.</done>
</task>

</tasks>

## Artifacts this phase produces

- `VietQrInstructions` customer component.
- Localized VietQR status and instruction copy.
- Customer/admin VietQR browser journey coverage.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| server projection -> customer browser | Customer sees instructions but cannot prove transfer. |
| localized copy -> payment status | Copy must not imply paid/fulfillable before authorized confirmation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-20 | Spoofing | VietQR customer UI | mitigate | No customer confirmation action; server-only status projection. |
| T-04-21 | Information disclosure | bank/account display | mitigate | Mask display fields and avoid raw receipt/evidence exposure. |
| T-04-22 | Tampering | status interpretation | mitigate | Text and icon labels accompany semantic color; paid gate stays closed until server status is paid. |
| T-04-SC | Tampering | package supply chain | accept | No new package; render existing UI primitives only. |
</threat_model>

<verification>
Task-local verification uses source assertions and Playwright list; full browser execution runs in Plan 04-10.
</verification>

<success_criteria>
- Vietnam customer can see exact transfer instructions.
- Customer cannot self-confirm or bypass pending payment.
- Mobile and desktop order layouts remain scannable and localized.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-08-SUMMARY.md` when done.
</output>
