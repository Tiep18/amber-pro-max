---
phase: 04-trusted-payments-and-orders
plan: "06"
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified:
  - src/payments/paypal/verification.ts
  - src/payments/paypal/mapping.ts
  - src/payments/paypal/logging.ts
  - src/app/api/webhooks/paypal/route.ts
  - tests/fixtures/payments/paypal-events.ts
  - tests/unit/payments/paypal-webhook.test.ts
  - tests/unit/payments/paypal-logging.test.ts
  - tests/security/payment-boundaries.test.mjs
autonomous: true
requirements:
  - INV-04
  - INV-05
  - PAY-03
  - PAY-04
  - PAY-07
  - PAY-08
  - SEC-03
must_haves:
  truths:
    - "PayPal webhook is the primary confirmation path and verifies authenticity before paid state per D-09 and D-11."
    - "Duplicate, delayed, rejected, mismatch, refund-visibility, and late-after-expiry events are idempotent per D-04, D-06, D-15, and D-16."
    - "Webhook evidence and logs are sanitized and auditable per D-03, D-19, D-20, and D-21."
  artifacts:
    - path: "src/app/api/webhooks/paypal/route.ts"
      provides: "Raw-body PayPal webhook receiver and transition delegator"
    - path: "src/payments/paypal/verification.ts"
      provides: "Signature verification and event reconciliation"
    - path: "tests/unit/payments/paypal-webhook.test.ts"
      provides: "Webhook verification, mapping, duplicate, mismatch, and late event coverage"
  key_links:
    - from: "src/app/api/webhooks/paypal/route.ts"
      to: "src/payments/transitions.ts"
      via: "applyPaymentTransition"
      pattern: "applyPaymentTransition"
    - from: "src/payments/paypal/verification.ts"
      to: "tests/fixtures/payments/paypal-events.ts"
      via: "sanitized provider fixtures"
      pattern: "PAYMENT.CAPTURE"
---

<objective>
Verify PayPal webhooks and route every accepted event through the shared transition command.

Purpose: PayPal callbacks and browser returns are not proof of payment. This plan keeps asynchronous webhook confirmation authoritative while preserving idempotency, audit, and refund visibility.
Output: Webhook verification/reconciliation modules, sanitized logging, webhook route, fixtures, and focused tests.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md
@src/app/api/webhooks/paypal/route.ts
@src/payments/paypal/verification.ts
@src/payments/paypal/mapping.ts
@src/payments/paypal/logging.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify raw webhook signatures and reconcile provider facts</name>
  <files>src/payments/paypal/verification.ts, src/payments/paypal/mapping.ts, tests/fixtures/payments/paypal-events.ts, tests/unit/payments/paypal-webhook.test.ts</files>
  <behavior>
    - Raw body digest and required PayPal transmission headers are verified against the configured webhook ID.
    - Accepted capture events validate provider order mapping, merchant/receiver identity, amount, and currency.
    - Forged signature, wrong merchant, wrong amount, wrong currency, and wrong local order mapping never open the paid gate.
    - Refund events update visibility only and do not initiate refunds.
  </behavior>
  <action>Keep verification transport injectable for unit tests and never call live PayPal in automated tests. Validate event names and Payments v2 capture facts against local `PayPalOrderSource`. Map completed captures to paid transitions, failed/declined/cancelled provider facts to terminal transitions, late completed money after local expiry/release to review-required, and refund visibility to read-only `partially_refunded` or `refunded` status. Use sanitized deterministic fixtures with no live seller data or secrets.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/paypal-webhook.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Webhook unit tests prove PAY-03, PAY-04, PAY-08, D-09, D-11, and D-15 without live provider calls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist webhook receipts and sanitize evidence</name>
  <files>src/app/api/webhooks/paypal/route.ts, src/payments/paypal/logging.ts, tests/unit/payments/paypal-logging.test.ts, tests/security/payment-boundaries.test.mjs</files>
  <behavior>
    - Duplicate webhook deliveries increment duplicate history without repeating payment or inventory effects.
    - Accepted transition events call applyPaymentTransition exactly once per provider event key.
    - Rejected or suspicious events are recorded as sanitized evidence when safe to do so.
    - Logs, UI evidence, and audit metadata omit raw body, signatures, payer PII, credentials, and full address data.
  </behavior>
  <action>Ensure `POST /api/webhooks/paypal` reads the raw body once, enforces body size and required headers, checks duplicate provider event IDs before expensive work, records delivery count and last received time, and delegates paid/fail/review effects to `applyPaymentTransition`. For no-op or refund visibility paths, persist sanitized event facts only. Extend logging tests so address/shipping fields from provider payloads are redacted or reduced to approved destination facts and never expose customer shipping address text in logs.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/paypal-webhook.test.ts tests/unit/payments/paypal-logging.test.ts</automated>
    <automated>npm run test:security</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Webhook route satisfies D-03, D-09, D-11, D-15, D-19 through D-21, PAY-03, PAY-04, PAY-07, PAY-08, and SEC-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PayPal -> webhook route | Untrusted internet requests claim payment events. |
| webhook route -> payment transition | Verified provider facts can affect paid gate and inventory. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-16 | Spoofing | PayPal webhook | mitigate | Official signature postback, raw-body digest, required transmission headers, configured webhook ID. |
| T-04-17 | Tampering | provider facts | mitigate | Validate order mapping, merchant, amount, currency, event id, and local deadline before transition. |
| T-04-18 | Information Disclosure | webhook logs/evidence | mitigate | Sanitized facts and digest-like evidence only; no raw payload/signature/PII/secret dumps. |
| T-04-SC | Tampering | package installs | accept | No package installs. |
</threat_model>

<verification>
Run PayPal webhook and logging unit tests, payment security tests, and typecheck. Sandbox HTTPS webhook delivery remains a Plan 10 human/provider checkpoint.
</verification>

<success_criteria>
Verified PayPal webhook events can move an order through paid or terminal payment states idempotently, while forged, duplicate, mismatched, late, or refund events never create unsafe paid or inventory effects.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-06-SUMMARY.md` when done.
</output>
