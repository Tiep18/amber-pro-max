---
phase: 04-trusted-payments-and-orders
plan: "10"
type: execute
wave: 7
depends_on:
  - 04-06
  - 04-08
  - 04-09
files_modified:
  - .planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md
  - .env.example
  - package.json
  - tests/integration/payment-concurrency.mjs
  - tests/security/payment-boundaries.test.mjs
autonomous: false
requirements:
  - INV-04
  - INV-05
  - ORD-01
  - ORD-02
  - ORD-03
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - PAY-05
  - PAY-06
  - PAY-07
  - PAY-08
  - SEC-03
must_haves:
  truths:
    - "Schema push happens after all Supabase schema changes and before final verification."
    - "Managed PayPal sandbox and VietQR seller configuration are explicit checkpoints; secrets are never invented."
    - "Final evidence proves duplicate/out-of-order payment handling, inventory exact-once behavior, access control and UI lifecycle coverage."
  artifacts:
    - path: ".planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md"
      provides: "Executed validation sign-off evidence"
    - path: "tests/integration/payment-concurrency.mjs"
      provides: "Repeated race/idempotency final scenario"
    - path: "tests/security/payment-boundaries.test.mjs"
      provides: "Final payment boundary assertion set"
  key_links:
    - from: "supabase db push"
      to: "supabase/migrations/20260615034000_trusted_payments_orders.sql"
      via: "managed schema deployment"
      pattern: "trusted_payments_orders"
---

<objective>
Run Phase 4 schema, provider, security, browser and CI gates after all implementation slices are complete.

Purpose: Prevent false-positive completion where local code passes but schema, provider and lifecycle checks have not been proven.
Output: Final validation sign-off, managed schema push evidence, sandbox/manual checkpoints and CI-ready Phase 4 verification.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md
@.planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md
@.planning/phases/04-trusted-payments-and-orders/04-01-SUMMARY.md
@.planning/phases/04-trusted-payments-and-orders/04-09-SUMMARY.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Push schema and verify managed payment environment checkpoints</name>
  <read_first>supabase/migrations/20260615034000_trusted_payments_orders.sql, .env.example, .planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md, .planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md</read_first>
  <files>.env.example, .planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md</files>
  <action>
Run `[BLOCKING] supabase db push` after all schema edits are complete. If the environment is non-interactive, use `SUPABASE_ACCESS_TOKEN` and project linking as required by the Supabase CLI. Verify pg_cron extension/job availability or record the managed-environment blocker while retaining read-path deadline checks. Confirm PayPal sandbox env values and webhook ID are seller-provided, run one sandbox create/capture with a verified webhook to the deployed HTTPS endpoint, and confirm VietQR bank configuration is seller-approved before production instruction display. Record results in the validation sign-off; do not store secret values.
  </action>
  <verify>
    <automated>supabase db push</automated>
  </verify>
  <acceptance_criteria>
CLI assertion: `supabase db push` exits 0 or the summary records an explicit managed-credential blocker. Documentation assertion: validation sign-off records PayPal sandbox webhook and VietQR seller-configuration checkpoint status without secret values.
  </acceptance_criteria>
  <done>Managed schema and provider checkpoints are proven or explicitly blocked for seller input.</done>
</task>

<task type="auto">
  <name>Task 2: Run final automated lifecycle, security and UI verification</name>
  <read_first>package.json, tests/integration/payment-concurrency.mjs, tests/security/payment-boundaries.test.mjs, tests/e2e/order-status.spec.ts, tests/e2e/admin-orders.spec.ts, tests/e2e/admin-vietqr.spec.ts, .planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md</read_first>
  <files>package.json, tests/integration/payment-concurrency.mjs, tests/security/payment-boundaries.test.mjs, .planning/phases/04-trusted-payments-and-orders/04-VALIDATION.md</files>
  <action>
Run the complete Phase 4 automated gate sequence: targeted unit tests for payments, pgTAP payment model/transitions/RLS tests, repeated concurrency scenarios for duplicate paid, recheck-plus-webhook, expiry-versus-capture and two-admin VietQR confirm, Node security boundary checks, Playwright order/admin journeys, typecheck and `npm run ci`. Update `04-VALIDATION.md` statuses only with commands actually run and green. If a provider/manual checkpoint remains blocked by missing seller credentials, keep the final summary explicit and do not mark production readiness.
  </action>
  <verify>
    <automated>npm run ci</automated>
  </verify>
  <acceptance_criteria>
CLI assertion: `npm run ci` exits 0. Source assertion: `04-VALIDATION.md` contains green/passed status for automated task rows and separately names any manual provider checkpoint that is not complete.
  </acceptance_criteria>
  <done>Phase 4 automated verification is complete and manual provider readiness is explicit.</done>
</task>

</tasks>

## Artifacts this phase produces

- Managed schema push and provider readiness checkpoint evidence.
- Final automated payment lifecycle/security/UI validation record.
- Phase 4 verification-ready summary.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| local migration -> managed database | Types/build can pass without schema deployed. |
| sandbox/manual provider -> production readiness | Provider credentials and bank accounts are seller-owned facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-26 | Tampering | schema deployment | mitigate | Blocking `supabase db push` before final verification. |
| T-04-27 | Spoofing | provider readiness | mitigate | Real sandbox webhook and seller-approved VietQR checkpoint before live use. |
| T-04-28 | Repudiation | final verification | mitigate | Validation sign-off records exact commands and manual blockers. |
| T-04-SC | Tampering | package supply chain | accept | No dependency install; final CI uses existing scripts. |
</threat_model>

<verification>
This plan is the phase gate: `supabase db push`, `npm run ci`, payment concurrency, security, Playwright and provider/manual checkpoints.
</verification>

<success_criteria>
- Database schema is pushed or blocked with an explicit credential/environment reason.
- Automated lifecycle tests and CI pass before Phase 4 is marked complete.
- Production PayPal/VietQR readiness is never implied without seller-approved provider facts.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-10-SUMMARY.md` when done.
</output>
