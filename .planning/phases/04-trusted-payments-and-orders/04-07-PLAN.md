---
phase: 04-trusted-payments-and-orders
plan: "07"
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - tests/unit/payments/vietqr.test.ts
  - src/payments/vietqr/instructions.ts
  - src/payments/vietqr/evidence.ts
  - src/payments/admin-actions.ts
  - src/payments/queries.ts
autonomous: true
requirements:
  - PAY-05
  - PAY-06
  - PAY-07
  - SEC-03
must_haves:
  truths:
    - "VietQR instructions exist only for vn+VND+vietqr_intent and snapshot exact integer amount/reference/deadline."
    - "D-12: VietQR customer transfer happens outside the app; only an authorized admin can confirm or reject after checking the bank account."
    - "Quick Link adapter uses typed seller configuration; fixtures are test-only."
    - "Admin confirm requires exact reference/amount/time; mismatch can only be rejected."
    - "Confirm/reject and double-submit use the same idempotent transition command as PayPal."
  artifacts:
    - path: "src/payments/vietqr/instructions.ts"
      provides: "Typed Quick Link instruction snapshot adapter"
      exports: ["getVietQrInstructions"]
    - path: "src/payments/vietqr/evidence.ts"
      provides: "Evidence schemas and exact comparison"
    - path: "src/payments/admin-actions.ts"
      provides: "Authorized confirm/reject Server Actions"
  key_links:
    - from: "VietQR instruction loader"
      to: "payment evidence snapshot"
      via: "idempotent shared transition source"
      pattern: "vietqr_instruction"
    - from: "admin actions"
      to: "applyPaymentTransition"
      via: "requireAdmin plus DB-side authority"
      pattern: "vietqr_admin"
---

<objective>
Implement VietQR instruction generation and authorized evidence-based confirm/reject backend behavior.

Purpose: Support exact manual Vietnam bank transfer without customer self-confirmation or direct admin table updates.
Output: Quick Link adapter, evidence validation, admin actions, projections and focused unit coverage.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md
@.planning/phases/04-trusted-payments-and-orders/04-PATTERNS.md
@.planning/phases/04-trusted-payments-and-orders/04-02-SUMMARY.md
@.planning/phases/04-trusted-payments-and-orders/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate stable exact VietQR instruction snapshots</name>
  <files>tests/unit/payments/vietqr.test.ts, src/payments/vietqr/instructions.ts</files>
  <read_first>tests/unit/payments/vietqr.test.ts, src/payments/vietqr/instructions.ts, .planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md, .planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md, .planning/phases/04-trusted-payments-and-orders/04-PATTERNS.md</read_first>
  <behavior>
    - Wrong market/currency/method or terminal/expired order cannot create instructions.
    - Amount is exact positive integer VND; reference is immutable ASCII order number.
    - Bank/account/template values come from typed env and are snapshotted once.
    - Quick Link URL encodes amount, reference and account name; public projection masks account number.
  </behavior>
  <action>
Complete Wave 0 unit cases first. Implement server-owned VietQR Quick Link adapter with the resolved typed bank configuration and no external package. Generate exact QR URL, display-safe bank/account data, unique order reference and timezone-aware deadline. Persist the instruction snapshot idempotently through the shared transition source `vietqr_instruction` without changing paid state. Keep fixture bank values inside tests only; production code must fail with typed unconfigured state until seller-approved values exist.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/payments/vietqr.test.ts</automated>
  </verify>
  <acceptance_criteria>
Source assertion: every file listed in <files> exists or is intentionally created by this task. Command assertion: the automated verify command exits 0. Behavior assertion: Eligible and ineligible instruction, encoding, masking and snapshot cases pass.
  </acceptance_criteria>
  <done>Eligible and ineligible instruction, encoding, masking and snapshot cases pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authorized VietQR evidence confirmation and rejection</name>
  <files>tests/unit/payments/vietqr.test.ts, src/payments/vietqr/evidence.ts, src/payments/admin-actions.ts, src/payments/queries.ts</files>
  <read_first>tests/unit/payments/vietqr.test.ts, src/payments/vietqr/evidence.ts, src/payments/admin-actions.ts, src/payments/queries.ts, .planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md, .planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md, .planning/phases/04-trusted-payments-and-orders/04-PATTERNS.md</read_first>
  <behavior>
    - Confirm requires bank reference, exact integer received amount, ISO received time and idempotency key.
    - Wrong amount/reference cannot confirm even when client validation is bypassed.
    - Reject requires bounded reason; `other` requires note and releases inventory immediately.
    - Non-admin, stale and duplicate actions cannot create repeated/regressive effects.
  </behavior>
  <action>
Implement Zod evidence schemas and pure comparison helpers. Add `confirmVietQrPaymentAction` and `rejectVietQrPaymentAction`: call `requireAdmin()` before parsing/clients, load expected facts, validate deadline/current state, and call `applyPaymentTransition` exactly once. Confirmation stores D-13 bank reference/amount/time plus optional bounded note and private receipt path; rejection stores reason and invokes immediate release/non-payable transition per D-14. The database independently checks admin authority and expected facts. Extend admin query projection with evidence/action availability; never use direct `.update()` for payment/order/reservation/inventory.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/payments/vietqr.test.ts</automated>
  </verify>
  <acceptance_criteria>
Source assertion: every file listed in <files> exists or is intentionally created by this task. Command assertion: the automated verify command exits 0. Behavior assertion: Exact confirm, mismatch reject, non-admin, stale and duplicate backend cases pass.
  </acceptance_criteria>
  <done>Exact confirm, mismatch reject, non-admin, stale and duplicate backend cases pass.</done>
</task>

</tasks>

## Artifacts this phase produces

- Exact VietQR Quick Link instruction snapshot adapter.
- Evidence validation and authorized confirm/reject Server Actions.
- Admin evidence projection and focused unit tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| server config -> customer instructions | Seller bank data must be approved, stable and safely projected. |
| admin browser -> Server Action | Evidence and decision input remain untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-04-17 | Tampering | amount/reference | mitigate | Server snapshot, integer money and DB-side expected/received comparison. |
| T-04-18 | Elevation of privilege / replay | admin actions | mitigate | `requireAdmin`, DB admin check, action idempotency and shared transition uniqueness. |
| T-04-19 | Information disclosure | bank/receipt evidence | mitigate | Masked customer projection, protected admin projection and private receipt path only. |
| T-04-SC | Tampering | package supply chain | accept | Official Quick Link URL and existing dependencies only. |
</threat_model>

<verification>
Both tasks use only `vietqr.test.ts`; browser/admin workflows and full DB effects run in Plans 04-08 and 04-10.
</verification>

<success_criteria>
- Vietnam order receives exact stable instructions without paid mutation.
- Only authorized admin can confirm exact full payment or reject mismatch.
- Confirm/reject remains idempotent and atomic with inventory/audit through the shared command.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-07-SUMMARY.md` when done.
</output>
