---
phase: 04-trusted-payments-and-orders
plan: "07"
type: execute
wave: 5
depends_on:
  - "04-04"
files_modified:
  - src/payments/vietqr/instructions.ts
  - src/payments/vietqr/evidence.ts
  - src/payments/admin-actions.ts
  - src/payments/queries.ts
  - src/payments/schemas.ts
  - tests/unit/payments/vietqr.test.ts
  - tests/security/payment-boundaries.test.mjs
autonomous: true
user_setup:
  - service: vietqr
    why: "Real VietQR instructions require seller-approved receiving bank details."
    env_vars:
      - name: VIETQR_BANK_ID
        source: "Seller-approved VietQR bank identifier"
      - name: VIETQR_ACCOUNT_NO
        source: "Seller receiving bank account number"
      - name: VIETQR_ACCOUNT_NAME
        source: "Seller receiving account name"
      - name: VIETQR_TEMPLATE
        source: "Seller-approved VietQR template"
requirements:
  - ORD-03
  - PAY-05
  - PAY-06
  - PAY-07
  - PAY-08
  - SEC-03
must_haves:
  truths:
    - "Vietnam VND orders receive exact VietQR instructions with amount, reference, and deadline per D-12 and D-13."
    - "VietQR payment remains pending until authorized admin confirms or rejects bank evidence per D-12 through D-15."
    - "VietQR instruction and admin evidence use the server-owned order/address snapshot as shipment destination context per D-30."
  artifacts:
    - path: "src/payments/vietqr/instructions.ts"
      provides: "Server-owned VietQR instruction snapshot builder"
    - path: "src/payments/vietqr/evidence.ts"
      provides: "Evidence validation and confirm/reject transition builders"
    - path: "src/payments/admin-actions.ts"
      provides: "Authorized admin VietQR confirm/reject Server Actions"
  key_links:
    - from: "src/payments/vietqr/instructions.ts"
      to: "src/payments/transitions.ts"
      via: "vietqr_instruction transition"
      pattern: "vietqr_instruction"
    - from: "src/payments/admin-actions.ts"
      to: "public.apply_payment_transition(jsonb)"
      via: "confirm/reject actions"
      pattern: "applyPaymentTransition"
---

<objective>
Generate exact VietQR instructions and let only authorized admins confirm or reject bank evidence.

Purpose: VietQR is a manual payment instruction, not a customer-confirmed payment. This slice keeps the seller's bank values server-owned, stores instruction/evidence facts for audit, and routes terminal decisions through the same payment transition command as PayPal.
Output: VietQR instruction adapter, evidence validators, admin actions, query fields, transition schema alignment, and focused tests.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trusted-payments-and-orders/04-CONTEXT.md
@.planning/phases/04-trusted-payments-and-orders/04-RESEARCH.md
@src/payments/vietqr/instructions.ts
@src/payments/vietqr/evidence.ts
@src/payments/admin-actions.ts
@src/payments/queries.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate auditable server-owned VietQR instructions</name>
  <files>src/payments/vietqr/instructions.ts, src/payments/queries.ts, src/payments/schemas.ts, tests/unit/payments/vietqr.test.ts</files>
  <behavior>
    - Instructions are available only for VN market, VND currency, VietQR intent, positive amount, active deadline orders.
    - Exact amount, ASCII transfer reference, bank config, masked account display, and deadline are derived on the server.
    - Instruction snapshot records a pending audit transition without opening the paid gate or changing inventory.
    - Physical/mixed instruction facts include only approved destination context from the immutable address snapshot.
  </behavior>
  <action>Extend `getVietQrInstructions` to accept order projection fields including `requiresShipping` and `shippingAddressSnapshot`. Build QR URL, transfer reference, masked account display, deadline, and sanitized instruction facts from server-owned config and order snapshot only. Include destination country/address digest or normalized destination summary where useful for admin audit, but do not place the full shipping address in public logs or QR fields. Keep customer self-confirmation absent and treat missing seller bank config as typed unconfigured state.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/vietqr.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>VietQR instruction tests satisfy PAY-05, D-12, D-13, D-30, and keep the paid gate closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Confirm or reject VietQR through one audited transition</name>
  <files>src/payments/vietqr/evidence.ts, src/payments/admin-actions.ts, src/payments/queries.ts, tests/unit/payments/vietqr.test.ts, tests/security/payment-boundaries.test.mjs</files>
  <behavior>
    - Confirm requires bank reference, received amount, received date/time, and exact match to expected order facts per D-13.
    - Wrong amount or reference requires rejection, release of inventory, audit record, and non-payable same order per D-14.
    - Admin double-submit and stale tabs are idempotent or stale, not duplicate paid effects per D-15 and D-16.
    - Non-admin cannot confirm or reject and no direct table mutation path exists.
  </behavior>
  <action>Keep `requireAdmin()` before privileged client creation and before applying any decision. Validate evidence and rejection input with Zod, compare exact received facts to expected payment facts, require a rejection reason for mismatches, and call `applyPaymentTransition` for confirm/reject. Ensure the database command independently validates paid VietQR evidence so bypassing the Server Action cannot mark paid with wrong reference or amount. Preserve action result codes for duplicate, stale, invalid, rejected, confirmed, and error states without raw SQL output.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/vietqr.test.ts</automated>
    <automated>npm run test:security</automated>
    <automated>npm run db:test</automated>
  </verify>
  <done>VietQR admin actions satisfy PAY-06, PAY-07, PAY-08, SEC-03, D-12 through D-16, and D-30.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server config -> customer VietQR page | Seller bank details become customer payment instructions. |
| admin form -> transition RPC | Admin-submitted bank evidence can mark payment paid or rejected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-19 | Tampering | VietQR amount/reference | mitigate | Exact amount/reference/deadline from server order; wrong facts cannot confirm paid. |
| T-04-20 | Elevation of Privilege | admin actions | mitigate | requireAdmin plus DB-side transition validation; no direct terminal table updates. |
| T-04-21 | Repudiation | manual bank decision | mitigate | Evidence fields, actor/source, transition ledger, and audit rows are recorded. |
| T-04-SC | Tampering | package installs | accept | No package installs; QR Quick Link uses platform URL APIs. |
</threat_model>

<verification>
Run focused VietQR unit tests, security suite, and DB tests. Real seller bank evidence remains a Plan 10 human/provider readiness checkpoint.
</verification>

<success_criteria>
VietQR orders show exact pending instructions from server-owned facts, and only authorized admin evidence can confirm or reject payment through the idempotent transition path.
</success_criteria>

<output>
Create `.planning/phases/04-trusted-payments-and-orders/04-07-SUMMARY.md` when done.
</output>
