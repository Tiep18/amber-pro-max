---
phase: 09-independent-locale-and-market-commerce-projection-with-seo-s
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/unit/i18n/routing.test.ts
  - tests/unit/catalog/market.test.ts
  - tests/unit/storefront-context-lifecycle.test.ts
  - tests/e2e/localization.spec.ts
autonomous: true
requirements: [MKT-01, MKT-05, OPS-04]
must_haves:
  truths:
    - "Characterization proves locale and market are independent across vi+vn, vi+intl, en+vn, and en+intl (D-01)."
    - "Direct and unprefixed entry obey URL, valid NEXT_LOCALE, Accept-Language best fit, then vi; market obeys valid ACTIVE_MARKET, trusted country suggestion, then intl (D-02, D-03)."
    - "Locale changes preserve market and dynamic equivalent routes while only route-allowlisted query state survives (D-05)."
    - "The browser contract starts resolving, fails closed on errors, ignores stale generations, and revalidates on focus, visibility, and invalidation-only cross-tab signals (D-12, D-16, D-23)."
    - "Wave 0 records current risks before runtime behavior changes and leaves implementation-dependent cases explicitly annotated for promotion (D-26)."
  artifacts:
    - path: "tests/unit/storefront-context-lifecycle.test.ts"
      provides: "Lifecycle, generation, abort, rollback, focus, visibility, and cross-tab contracts"
    - path: "tests/unit/i18n/routing.test.ts"
      provides: "Locale precedence, equivalent-route, and query-allowlist contracts"
    - path: "tests/unit/catalog/market.test.ts"
      provides: "Strict market precedence, invalid-cookie, and mutation-result contracts"
    - path: "tests/e2e/localization.spec.ts"
      provides: "Direct/unprefixed entry and locale-preserves-market browser matrix"
  key_links:
    - from: "tests/unit/storefront-context-lifecycle.test.ts"
      to: "src/components/storefront-context.tsx"
      via: "future pure lifecycle exports exercised before provider wiring"
      pattern: "generation|abort|focus|visibility|cross-tab"
    - from: "tests/unit/i18n/routing.test.ts"
      to: "src/i18n/routing.ts"
      via: "locked precedence and equivalent localized paths"
      pattern: "NEXT_LOCALE|Accept-Language|vi"
---

# Phase 09 Plan 01: Wave 0 resolution and storefront-context contracts

<objective>
Create executable characterization contracts for independent locale/market resolution and the race-safe storefront-context lifecycle before changing production behavior.

Purpose: Make stale assumptions and current failures visible so later resolution/context plans can turn a bounded RED/annotated matrix green without redefining scope.
Output: Updated routing/market/localization tests and a new pure lifecycle contract suite.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-CONTEXT.md
@.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-RESEARCH.md
@.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-VALIDATION.md
@src/proxy.ts
@src/i18n/routing.ts
@src/catalog/market.ts
@src/components/storefront-context.tsx

<interfaces>
Existing sources export routing/defaultLocale/isLocale/preferredLocale/getEquivalentLocalizedPath, resolveActiveMarket/safeMarketReturnPath, and StorefrontContextProvider/useStorefrontContext. Tests must describe their required Phase 09 successors without trusting locale-derived market guesses or raw cross-tab payloads.
</interfaces>
</context>

## Artifacts this phase produces

- `tests/unit/storefront-context-lifecycle.test.ts` with lifecycle cases for resolving/ready/error/retrying, monotonically increasing generation, abort, rollback, focus/visibility, and cross-tab invalidation.
- Revised routing, market, and localization contracts covering D-01–D-05 and D-23.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Characterize locale and market precedence plus safe equivalent navigation</name>
  <read_first>
@tests/unit/i18n/routing.test.ts
@tests/unit/catalog/market.test.ts
@tests/e2e/localization.spec.ts
@src/proxy.ts
@src/i18n/routing.ts
@src/catalog/market.ts
@.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-CONTEXT.md
  </read_first>
  <files>tests/unit/i18n/routing.test.ts, tests/unit/catalog/market.test.ts, tests/e2e/localization.spec.ts</files>
  <behavior>
    - D-01/D-02: direct `/vi` and `/en` win; an unprefixed visit uses a valid locale cookie, then weighted Accept-Language, then `vi` when absent/invalid.
    - D-03: valid market cookie wins over `x-vercel-ip-country`; invalid cookie recovers from VN/non-VN/missing header; IP never becomes checkout authority.
    - D-05: product/category/collection plus future technique/tag routes switch to the equivalent localized slug; catalog preserves only search/type/category/technique/tag/sort; auth preserves only a validated internal `next`; external, nested, duplicate, and backslash paths are discarded.
    - D-01: all four locale/market pairs render as supported combinations; changing one axis does not mutate the other.
  </behavior>
  <action>Replace assertions that encode missing-header -> en, combined locale/market pairs, silent invalid-market coercion, redirects, or shared `revalidatePath` behavior. Add table-driven unit cases and direct/unprefixed Playwright cases. Mark only cases blocked on Plans 09-04/09-13 with the framework's expected-failure annotation and the exact owner plan; never skip already-supported behavior. Preserve `/auth/callback` isolation and add a regression case for localized auth entry without changing auth/session source.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/i18n/routing.test.ts tests/unit/catalog/market.test.ts && npm run test:e2e -- tests/e2e/localization.spec.ts</automated>
  </verify>
  <acceptance_criteria>Every D-01–D-05 row exists with explicit expected output; current-risk cases are named and owner-annotated rather than silently skipped.</acceptance_criteria>
  <done>The resolution/navigation matrix is executable and no stale combined-axis or en-fallback assertion remains.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify race-safe storefront-context lifecycle and convergence</name>
  <read_first>
@tests/unit/checkout/quote-lifecycle.test.ts
@tests/unit/components/storefront-context-policy.test.ts
@src/components/storefront-context.tsx
@src/components/storefront-context-policy.ts
@.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-UI-SPEC.md
@.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-VALIDATION.md
  </read_first>
  <files>tests/unit/storefront-context-lifecycle.test.ts</files>
  <behavior>
    - Initial state is resolving with purchase unsafe; ready requires a server-resolved market and generation/version.
    - A then B then A intents assign increasing generations; late completions and aborted requests are no-ops.
    - Mutation failure restores the prior committed market, exposes a bounded error code, and keeps purchase unsafe until retry succeeds.
    - Focus/visible revalidation does not replace ready state when the server version/market is unchanged; changed state enters resolving and reconverges.
    - Cross-tab input contains only invalidation/version; raw market/price/quote payloads are ignored and trigger server refetch.
  </behavior>
  <action>Create the new pure contract suite following `tests/unit/checkout/quote-lifecycle.test.ts`. Cover begin/settle/fail/retry/invalidate events, full state equality for stale completions, one-in-flight abort behavior, same-version focus behavior, visibility gating, and invalidation-only broadcast. Use expected-failure annotations tied to Plan 09-05 until the pure module exists; do not import React or timers into the contract.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/storefront-context-lifecycle.test.ts tests/unit/components/storefront-context-policy.test.ts</automated>
  </verify>
  <acceptance_criteria>The suite names every lifecycle/race case in 09-VALIDATION and can be promoted without changing expected state objects.</acceptance_criteria>
  <done>Wave 0 has an implementation-independent lifecycle specification for Plan 09-05.</done>
</task>

</tasks>

<source_audit>
| Source | ID/item | Coverage | Status |
|--------|---------|----------|--------|
| GOAL | Independent language and market | Four-combination and axis-preservation contracts | COVERED |
| REQ | MKT-01, MKT-05, OPS-04 | Routing, suggestion, visibility, race verification | COVERED |
| CONTEXT | D-01..D-05, D-12, D-16, D-17, D-23, D-26 | Tasks 1-2 | COVERED |
| RESEARCH | Stale tests, locale fallback, generation/abort, auth-entry divergence | Tasks 1-2 | COVERED |
| EXCLUDED | Runtime resolution/provider/control implementation | Plans 09-04, 09-05, 09-13 | EXCLUDED |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Request URL/cookies/headers -> resolution | Preference and geo inputs are untrusted suggestions. |
| Browser events/tabs -> context state | Late responses and broadcast payloads cannot become authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Spoofing | Locale/market resolution | mitigate | Strict enums, cookie-over-geo precedence, missing-header fallback, and suggestion-only tests. |
| T-09-03 | Tampering | Context races | mitigate | Generation/abort/stale no-op and fail-closed contracts. |
| T-09-04 | Spoofing | Equivalent redirects/query | mitigate | Internal localized route mapping and per-route query allowlists. |
| T-09-06 | Tampering | Focus/cross-tab | mitigate | Invalidation-only events followed by server refetch. |
| T-09-08 | Information disclosure | Test fixtures/logging | mitigate | No raw cookie/header/customer payload snapshots; assert bounded error codes. |
| T-09-SC | Tampering | Dependencies | accept | No package install or upgrade. |
</threat_model>

<verification>
Run the two task commands, then `npm run typecheck`. Expected-failure annotations must name a later owner plan and no unrelated case may be skipped.
</verification>

<success_criteria>
- Every resolution and lifecycle row from 09-VALIDATION exists before runtime edits.
- All four locale/market combinations are explicit.
- Unsafe redirects/query propagation, stale responses, and cross-tab authority are characterized.
- No production file changes in this plan.
</success_criteria>

<output>
Create `.planning/phases/09-independent-locale-and-market-commerce-projection-with-seo-s/09-01-SUMMARY.md` when done.
</output>
