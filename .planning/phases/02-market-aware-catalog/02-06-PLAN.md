---
phase: 02-market-aware-catalog
plan: "06"
type: execute
wave: 4
depends_on: [02-01, 02-04]
files_modified:
  - supabase/migrations/20260612232000_market_catalog_queries.sql
  - supabase/tests/database/02_catalog_queries.test.sql
  - src/types/supabase.ts
  - src/catalog/queries.ts
  - tests/unit/catalog/queries.test.ts
  - tests/e2e/catalog-market.spec.ts
autonomous: true
requirements: [MKT-02, MKT-03, MKT-04, CAT-05, CAT-06, CAT-08]
must_haves:
  truths:
    - "List/search returns only published products with enabled active-market offers per D-02."
    - "Direct detail may return localized public content when unavailable but never another market's price per D-03."
    - "Every public query requires locale and market and exposes stock boolean instead of exact inventory."
    - "Variant effective price and availability follow D-08 through D-10 and D-25."
  artifacts:
    - path: "supabase/migrations/20260612232000_market_catalog_queries.sql"
      provides: "Market-filtered public catalog RPCs"
    - path: "src/catalog/queries.ts"
      provides: "Typed locale+market query helpers"
      exports: ["listCatalogProducts", "listCatalogFacets", "getCatalogProductBySlug"]
    - path: "supabase/tests/database/02_catalog_queries.test.sql"
      provides: "Database market isolation evidence"
  key_links:
    - from: "src/catalog/queries.ts"
      to: "list_catalog_products"
      via: "typed RPC with locale+market+filters"
      pattern: "list_catalog_products"
    - from: "tests/e2e/catalog-market.spec.ts"
      to: "src/catalog/queries.ts"
      via: "independent VN/INTL contexts"
      pattern: "VND|USD|ACTIVE_MARKET"
---

<objective>
Deliver the public market-isolated catalog query boundary.

Purpose: Prevent wrong-market assortment, currency, exact inventory, or private metadata from reaching public pages.
Output: SQL RPCs, typed query helpers, generated types, pgTAP/unit tests, and completed market-isolation browser cases.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-market-aware-catalog/02-CONTEXT.md
@.planning/phases/02-market-aware-catalog/02-RESEARCH.md
@.planning/phases/02-market-aware-catalog/02-VALIDATION.md
@.planning/phases/02-market-aware-catalog/02-01-SUMMARY.md
@.planning/phases/02-market-aware-catalog/02-04-SUMMARY.md
@src/lib/supabase/server.ts
</context>

## Phase Goal

**As a** store admin and shopper, **I want to** manage and browse bilingual products with market-specific availability, pricing, variants, and protected digital assets, **so that** Vietnam and international customers can browse the correct catalog before checkout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create market-filtered catalog RPCs</name>
  <files>supabase/migrations/20260612232000_market_catalog_queries.sql, supabase/tests/database/02_catalog_queries.test.sql, src/types/supabase.ts</files>
  <behavior>
    - Listing/search filters published products by requested locale and enabled market offer per D-02.
    - VN rows expose VND only and INTL rows expose USD only per D-01.
    - Category/collection/technique/tag filters compose, and collection browsing preserves manual order per D-14/D-15.
    - Detail returns localized unavailable content without wrong-market price and only indicates another eligible market per D-03.
    - Variant projection exposes explicit variants, effective price, and stock boolean per D-08 through D-10/D-25.
  </behavior>
  <action>Write pgTAP and RPC migration together. Add `list_catalog_products`, `list_catalog_facets`, `get_catalog_product_by_slug`, `get_catalog_category_by_slug`, and `get_catalog_collection_by_slug`. Use fixed search paths, revoke default execute, grant only anon/authenticated, parameterize all inputs, and never expose draft data, exact inventory, private bucket paths, or other-market prices. Regenerate types after the database gate passes.</action>
  <verify><automated>npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && npm run typecheck</automated></verify>
  <done>Database projections satisfy D-01 through D-03, D-08 through D-10, D-14, D-15, and D-25 without public data leakage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement typed query helpers and browser market isolation</name>
  <files>src/catalog/queries.ts, tests/unit/catalog/queries.test.ts, tests/e2e/catalog-market.spec.ts</files>
  <behavior>
    - Locale and market are mandatory typed inputs.
    - Search length and sort values are allowlisted; no raw SQL fragments are accepted.
    - Market-dependent requests are dynamic or explicitly keyed by both locale and market.
    - Independent browser contexts never see the other market's product or currency after switching/refreshing locale.
  </behavior>
  <action>Create typed wrappers for list, facets, taxonomy, and detail RPCs. Select practical sorts `newest`, `price_asc`, `price_desc`, and `title` per agent discretion under D-14. Add query unit tests and complete the existing market Playwright spec with seeded VN-only, INTL-only, both-market, and disabled-offer records. Write tests and implementation together so both commands finish green.</action>
  <verify><automated>npm run test:unit -- tests/unit/catalog/queries.test.ts && npm run typecheck && npm run test:e2e -- tests/e2e/catalog-market.spec.ts</automated></verify>
  <done>Typed queries and browser sessions prove locale+market isolation, safe filtering/sorting, and unavailable-detail data boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public RPC parameters to catalog tables | Locale, market, search, filter, and sort are untrusted. |
| Query result to public rendering | Wrong-market price or private metadata must not escape. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure | query/cache | mitigate | Mandatory locale+market DB predicates and isolated context tests. |
| T-02-06-02 | Tampering | filters/sort | mitigate | Typed allowlists, bounded search, parameterized RPCs. |
| T-02-06-03 | Information Disclosure | inventory/PDF | mitigate | Public projections omit exact quantities and private paths. |
</threat_model>

<verification>
Run sequential database checks, generate types, then focused query unit and complete market browser tests.
</verification>

<success_criteria>
- Public queries return only eligible active-market products/prices.
- Direct unavailable detail preserves content without price leakage.
- Exact inventory and private PDF metadata remain hidden.
</success_criteria>

<output>Create `.planning/phases/02-market-aware-catalog/02-06-SUMMARY.md` when done.</output>
