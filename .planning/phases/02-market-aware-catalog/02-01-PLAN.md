---
phase: 02-market-aware-catalog
plan: "01"
type: execute
wave: 1
depends_on: [01-08]
files_modified:
  - supabase/migrations/20260612230000_market_catalog.sql
  - supabase/tests/database/02_catalog_model.test.sql
  - supabase/tests/database/02_catalog_rls.test.sql
  - src/catalog/types.ts
  - src/types/supabase.ts
autonomous: true
requirements: [MKT-03, MKT-04, CAT-01, CAT-02, CAT-03, CAT-04, INV-01, DIG-01, SEO-01]
must_haves:
  truths:
    - "Database represents bilingual PDF patterns and physical finished products with independent VN/INTL offers per D-01, D-06, and D-13."
    - "Variants, parent-price fallback, and inventory ownership are constrained per D-07 through D-10."
    - "Draft, published, and archived states use one database publish gate for D-16 through D-18 and D-23."
    - "Catalog base tables enable RLS and do not expose draft, inventory, or private PDF metadata to anonymous/customer roles."
  artifacts:
    - path: "supabase/migrations/20260612230000_market_catalog.sql"
      provides: "Catalog schema, constraints, publish RPC, grants, and RLS"
      contains: "enable row level security"
    - path: "supabase/tests/database/02_catalog_model.test.sql"
      provides: "Constraint and publishability evidence"
    - path: "src/catalog/types.ts"
      provides: "Shared catalog domain contracts"
      exports: ["MarketCode", "CurrencyCode", "ProductType", "ProductStatus", "PublishIssueCode"]
  key_links:
    - from: "supabase/migrations/20260612230000_market_catalog.sql"
      to: "src/types/supabase.ts"
      via: "npm run db:types"
      pattern: "products|product_translations|product_market_offers"
    - from: "supabase/tests/database/02_catalog_rls.test.sql"
      to: "private.is_admin()"
      via: "role-matrix assertions"
      pattern: "private\\.is_admin"
---

<objective>
Establish the authoritative catalog data contract for Phase 2.

Purpose: Enforce the hard commerce invariants before admin or public UI consumes them.
Output: Catalog migration, pgTAP model/RLS coverage, domain types, and regenerated Supabase types.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-market-aware-catalog/02-CONTEXT.md
@.planning/phases/02-market-aware-catalog/02-RESEARCH.md
@.planning/phases/02-market-aware-catalog/02-VALIDATION.md
@.planning/phases/01-secure-bilingual-foundation/01-04-SUMMARY.md
@supabase/migrations/20260612102801_foundation_identity.sql
@supabase/tests/database/01_foundation_rls.test.sql
</context>

## Phase Goal

**As a** store admin and shopper, **I want to** manage and browse bilingual products with market-specific availability, pricing, variants, and protected digital assets, **so that** Vietnam and international customers can browse the correct catalog before checkout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the bilingual catalog and market-offer contract</name>
  <files>supabase/migrations/20260612230000_market_catalog.sql, supabase/tests/database/02_catalog_model.test.sql, src/catalog/types.ts</files>
  <behavior>
    - Product type is only `pdf_pattern` or `physical_finished`, and status is only draft/published/archived per D-06 and D-16.
    - Product/category/collection translations require unique locale-specific slugs per D-13.
    - Enabled `vn` offers require integer VND and enabled `intl` offers require integer USD per D-01; no floating-point or exchange-rate conversion exists.
    - Category, technique, tag, and collection remain independent; curated collection membership stores explicit order per D-11, D-12, D-15.
  </behavior>
  <action>Write pgTAP cases and the migration in the same task, then drive them green. Create products, translations, taxonomies and joins, ordered collection membership, product market offers, product media metadata, and digital asset metadata. Store image/PDF ownership as bucket/path metadata, never as a PDF public URL. Add indexes for locale+slug, status/type, offer market, and taxonomy joins. Export stable TypeScript unions and result contracts from `src/catalog/types.ts`.</action>
  <verify><automated>npm run db:reset && npm run db:lint && npm run db:test</automated></verify>
  <done>The migration accepts every valid D-01, D-06, D-11 through D-16, D-19, D-23 case and rejects wrong currency, duplicate localized slug, invalid type/status, or malformed taxonomy ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce variants, inventory, publishing, and catalog RLS</name>
  <files>supabase/migrations/20260612230000_market_catalog.sql, supabase/tests/database/02_catalog_model.test.sql, supabase/tests/database/02_catalog_rls.test.sql, src/types/supabase.ts</files>
  <behavior>
    - Only physical products can own variants or inventory per D-07.
    - Explicit variants have unique SKU, attributes, optional media, and optional market overrides per D-08 and D-09.
    - Inventory exists at product level for non-variant physical goods or variant level for variant goods, never both per D-10.
    - Publish issues cover both translations, slugs, SEO/social image, primary image, valid offers, private PDF metadata, and physical inventory per D-17, D-18, D-23.
    - Anonymous/customer roles cannot mutate or directly inspect protected catalog base data.
  </behavior>
  <action>Extend the migration with explicit variants, variant market overrides, inventory ownership constraints, `catalog_publish_issues(product_id)`, and atomic `publish_catalog_product(product_id)`. Use fixed `search_path`, database-owned `private.is_admin()`, RLS on every table, revoked default function execution, and narrow grants. Regenerate `src/types/supabase.ts` only after reset/lint/pgTAP pass.</action>
  <verify><automated>npm run db:reset && npm run db:lint && npm run db:test && npm run db:types && npm run typecheck</automated></verify>
  <done>Variant/inventory/publish invariants are database-enforced, the role matrix passes, and generated types match the migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Admin/session to catalog tables | Untrusted form data crosses into commerce records. |
| Catalog base tables to public projections | Drafts, exact inventory, and private assets must remain hidden. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | offers/inventory | mitigate | DB constraints, integer money, XOR inventory ownership, pgTAP negative cases. |
| T-02-01-02 | Elevation of Privilege | publish/catalog writes | mitigate | `private.is_admin()`, fixed search path, RLS, revoked defaults. |
| T-02-01-03 | Information Disclosure | draft/PDF metadata | mitigate | No direct anon/customer base-table access; later public RPCs expose projections only. |
</threat_model>

<verification>
Run Supabase commands sequentially: reset, lint, pgTAP, type generation, then TypeScript checking.
</verification>

<success_criteria>
- Valid catalog structures persist and invalid money/slug/type/variant/inventory structures fail.
- Publish cannot bypass required bilingual, offer, image, SEO, PDF, or physical data.
- All Phase 2 base tables have tested RLS.
</success_criteria>

<output>Create `.planning/phases/02-market-aware-catalog/02-01-SUMMARY.md` when done.</output>
