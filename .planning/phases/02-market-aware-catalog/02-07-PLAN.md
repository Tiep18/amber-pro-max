---
phase: 02-market-aware-catalog
plan: "07"
type: execute
wave: 5
depends_on: [02-05, 02-06]
files_modified:
  - src/i18n/routing.ts
  - src/messages/en.json
  - src/messages/vi.json
  - src/components/site-header.tsx
  - src/components/catalog/product-card.tsx
  - src/components/catalog/catalog-controls.tsx
  - src/app/[locale]/catalog/page.tsx
  - src/app/[locale]/category/[categorySlug]/page.tsx
  - src/app/[locale]/collection/[collectionSlug]/page.tsx
  - tests/e2e/catalog-discovery.spec.ts
autonomous: true
requirements: [MKT-02, MKT-03, CAT-05, CAT-06, CAT-07]
must_haves:
  truths:
    - "Shopper browses localized catalog, category, and collection pages containing only active-market products per D-02 and D-11."
    - "Search, product type, category, collection, technique, tag, and practical sorts compose through URL parameters per D-12 and D-14."
    - "Collections preserve manual display order per D-15."
    - "Cards visibly distinguish PDF patterns from finished products per D-20."
  artifacts:
    - path: "src/i18n/routing.ts"
      provides: "Translated public catalog route contract"
    - path: "src/app/[locale]/catalog/page.tsx"
      provides: "Market-aware listing/search/filter/sort page"
    - path: "tests/e2e/catalog-discovery.spec.ts"
      provides: "Green listing, taxonomy, filter, sort, and badge journeys"
  key_links:
    - from: "src/app/[locale]/catalog/page.tsx"
      to: "src/catalog/queries.ts"
      via: "server-side locale+market+URL filter query"
      pattern: "listCatalogProducts"
    - from: "src/components/catalog/product-card.tsx"
      to: "src/catalog/money.ts"
      via: "active-offer money formatting"
      pattern: "formatMoney"
---

<objective>
Deliver localized public catalog discovery and merchandising pages.

Purpose: Let shoppers find only products eligible for their active market through indexable category/collection pages and database-backed search/filter/sort.
Output: Route map, listing/category/collection pages, cards/controls, navigation, translations, and focused E2E.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-market-aware-catalog/02-CONTEXT.md
@.planning/phases/02-market-aware-catalog/02-RESEARCH.md
@.planning/phases/02-market-aware-catalog/02-VALIDATION.md
@.planning/phases/02-market-aware-catalog/02-05-SUMMARY.md
@.planning/phases/02-market-aware-catalog/02-06-SUMMARY.md
@src/i18n/routing.ts
@src/i18n/navigation.ts
</context>

## Phase Goal

**As a** store admin and shopper, **I want to** manage and browse bilingual products with market-specific availability, pricing, variants, and protected digital assets, **so that** Vietnam and international customers can browse the correct catalog before checkout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Establish localized catalog routes and market-aware listing</name>
  <files>src/i18n/routing.ts, src/messages/en.json, src/messages/vi.json, src/components/site-header.tsx, src/components/catalog/product-card.tsx, src/app/[locale]/catalog/page.tsx, tests/e2e/catalog-discovery.spec.ts</files>
  <behavior>
    - Route names follow the existing Phase 1 `next-intl` translated-pathname pattern, with separate locale-specific dynamic content slugs.
    - Header exposes the shop route on desktop/mobile.
    - Listing renders only active-market cards with correct currency and visible product type badge.
    - Empty and loading states remain localized and accessible.
  </behavior>
  <action>Per the resolved research decision, this plan owns the final static route labels. Add internal `/catalog`, `/category/[categorySlug]`, `/collection/[collectionSlug]`, and `/product/[productSlug]` contracts with concise Vietnamese/English pathnames consistent with existing routing helpers. Build server-rendered listing using active market plus locale; never fetch all products and filter in the client. Product cards show primary public image, localized title, formatted active-market price, D-20 type badge, and localized detail link. Build tests and implementation together.</action>
  <verify><automated>npm run typecheck && npm run build && npm run test:e2e -- tests/e2e/catalog-discovery.spec.ts --grep "listing|market|currency|badge"</automated></verify>
  <done>Both locales expose a routed catalog whose cards contain only eligible products and correct active-market prices/type labels.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add search, filters, sorting, category, and collection pages</name>
  <files>src/components/catalog/catalog-controls.tsx, src/app/[locale]/catalog/page.tsx, src/app/[locale]/category/[categorySlug]/page.tsx, src/app/[locale]/collection/[collectionSlug]/page.tsx, tests/e2e/catalog-discovery.spec.ts</files>
  <behavior>
    - Text, type, category, collection, technique, tag, and sort URL parameters compose per D-14.
    - Category and collection pages are public and indexable per D-11.
    - Technique/tag work as catalog filters without standalone index pages per D-12.
    - Curated collection results retain admin display order per D-15.
  </behavior>
  <action>Build GET-based controls that preserve locale and market cookie while encoding filters in the URL. Use the Plan 02-06 server query helpers. Support `newest`, `price_asc`, `price_desc`, and `title`; do not add fuzzy search or a search service. Category/collection pages load localized translation and eligible products; wrong locale/unpublished slugs return notFound. Complete Playwright coverage for composed filters, each sort, collection order, category navigation, technique/tag filter views, and empty results.</action>
  <verify><automated>npm run typecheck && npm run build && npm run test:e2e -- tests/e2e/catalog-discovery.spec.ts</automated></verify>
  <done>Shopper can browse, search, filter, and sort the eligible localized assortment; category/collection pages are public and technique/tag remain filters only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL search parameters to query helpers | Search/filter/sort inputs are shopper-controlled. |
| Public projection to cards | Wrong-market price or private metadata must not render. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering | URL controls | mitigate | Typed allowlists and bounded query helper inputs. |
| T-02-07-02 | Information Disclosure | product cards | mitigate | Consume market-filtered projections only. |
| T-02-07-03 | Spoofing | localized links | mitigate | Generate links from typed route map and stored locale slugs. |
</threat_model>

<verification>
Run typecheck/build and the complete green discovery Playwright spec with VN/INTL and vi/en fixtures.
</verification>

<success_criteria>
- Listing/category/collection routes work in Vietnamese and English.
- Search/filter/sort never returns another market's offer.
- Technique/tag are filters only.
- Product type is clear on every card.
</success_criteria>

<output>Create `.planning/phases/02-market-aware-catalog/02-07-SUMMARY.md` when done.</output>
