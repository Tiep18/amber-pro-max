---
phase: 02-market-aware-catalog
plan: "04"
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/catalog/variant-schemas.ts
  - src/catalog/variant-actions.ts
  - src/catalog/variant-pricing.ts
  - src/app/admin/catalog/[productId]/variants/page.tsx
  - src/components/admin/catalog/variant-editor.tsx
  - tests/unit/catalog/variants.test.ts
  - tests/e2e/admin-variants.spec.ts
autonomous: true
requirements: [MKT-04, CAT-02, CAT-08, INV-01]
must_haves:
  truths:
    - "Only physical products have variants or inventory per D-07."
    - "Admin explicitly creates variants with SKU, attributes, optional image, inventory, and optional market overrides per D-08."
    - "Effective price uses variant override then parent fallback per D-09."
    - "Inventory is editable at exactly one valid level per D-10."
  artifacts:
    - path: "src/catalog/variant-actions.ts"
      provides: "Admin-only variant, override, and inventory mutations"
    - path: "src/components/admin/catalog/variant-editor.tsx"
      provides: "Explicit physical variant and inventory editor"
    - path: "tests/e2e/admin-variants.spec.ts"
      provides: "Green product-level and variant-level inventory journeys"
  key_links:
    - from: "src/components/admin/catalog/variant-editor.tsx"
      to: "src/catalog/variant-actions.ts"
      via: "validated server action submissions"
      pattern: "saveVariant|adjustInventory"
    - from: "src/catalog/variant-pricing.ts"
      to: "product/variant market offers"
      via: "override then parent fallback"
      pattern: "override|parent"
---

<objective>
Deliver explicit physical variants, market price overrides, and correct inventory editing.

Purpose: Let admin prepare physical products that public pages can later present without inferred combinations or ambiguous stock.
Output: Variant/inventory actions, editor, pricing helper, and focused unit/browser coverage.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-market-aware-catalog/02-CONTEXT.md
@.planning/phases/02-market-aware-catalog/02-RESEARCH.md
@.planning/phases/02-market-aware-catalog/02-VALIDATION.md
@.planning/phases/02-market-aware-catalog/02-02-SUMMARY.md
@src/auth/guards.ts
</context>

## Phase Goal

**As a** store admin and shopper, **I want to** manage and browse bilingual products with market-specific availability, pricing, variants, and protected digital assets, **so that** Vietnam and international customers can browse the correct catalog before checkout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit variant, pricing, and inventory rules</name>
  <files>src/catalog/variant-schemas.ts, src/catalog/variant-actions.ts, src/catalog/variant-pricing.ts, tests/unit/catalog/variants.test.ts</files>
  <behavior>
    - PDF products reject variant and inventory payloads per D-07.
    - Physical variants require explicit ID/SKU/attributes/display order and may reference optional media per D-08.
    - Missing variant market override falls back to parent offer; explicit override uses the same market currency per D-09.
    - Non-variant physical products own product inventory; variant products own only variant inventory per D-10.
  </behavior>
  <action>Write unit cases and implementation together. Create narrow Zod schemas and admin-only actions for variant CRUD, price overrides, and inventory adjustment. Call `requireAdmin()` on every mutation and let database constraints remain final authority. Never auto-generate hidden combinations. Reject negative quantities, duplicate SKU, wrong owner level, wrong currency, and PDF-product payloads.</action>
  <verify><automated>npm run test:unit -- tests/unit/catalog/variants.test.ts && npm run typecheck</automated></verify>
  <done>All D-07 through D-10 rules pass focused unit tests and actions reject invalid/unauthorized mutations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the physical variant and inventory editor</name>
  <files>src/app/admin/catalog/[productId]/variants/page.tsx, src/components/admin/catalog/variant-editor.tsx, tests/e2e/admin-variants.spec.ts</files>
  <behavior>
    - Physical product without variants edits one product-level quantity.
    - Variant product explicitly creates, reorders, edits, and removes variants with SKU, attributes, optional image, and stock.
    - Effective VN/INTL price is shown with parent fallback and optional override.
    - Publish remains blocked for invalid or missing physical inventory data.
  </behavior>
  <action>Build the protected nested variants page and accessible editor. Present distinct no-variant and explicit-variant modes without silently converting existing inventory ownership. Display effective prices and make overrides opt-in. E2E must create both physical shapes, edit stock at the correct level, verify wrong-level controls are absent/rejected, and finish green.</action>
  <verify><automated>npm run typecheck && npm run build && npm run test:e2e -- tests/e2e/admin-variants.spec.ts</automated></verify>
  <done>Admin can prepare physical products with valid explicit variants or product-level inventory and cannot create ambiguous inventory ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Variant editor to server actions | SKU, attributes, money overrides, and quantities are untrusted. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Tampering | variant/inventory | mitigate | Zod plus DB constraints, explicit IDs, no inferred combinations. |
| T-02-04-02 | Elevation of Privilege | inventory actions | mitigate | `requireAdmin()` and RLS. |
| T-02-04-03 | Information Disclosure | exact inventory | mitigate | Exact quantities remain admin-only; public query exposes availability later. |
</threat_model>

<verification>
Run focused variant unit tests, typecheck/build, and the green admin variant Playwright spec.
</verification>

<success_criteria>
- Both physical inventory models work and remain mutually exclusive.
- Variant overrides preserve market currency and parent fallback.
- PDF products cannot receive physical data.
</success_criteria>

<output>Create `.planning/phases/02-market-aware-catalog/02-04-SUMMARY.md` when done.</output>
