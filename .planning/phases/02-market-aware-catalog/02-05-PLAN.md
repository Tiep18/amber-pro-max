---
phase: 02-market-aware-catalog
plan: "05"
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/catalog/market.ts
  - src/catalog/money.ts
  - src/catalog/market-actions.ts
  - src/components/market-switcher.tsx
  - src/components/site-header.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - src/proxy.ts
  - tests/unit/catalog/market.test.ts
  - tests/unit/catalog/money.test.ts
  - tests/e2e/catalog-market.spec.ts
autonomous: true
requirements: [MKT-02, MKT-05]
must_haves:
  truths:
    - "Country signal only suggests the initial market; a valid explicit choice wins per D-04."
    - "Active market is visible and manually switchable without changing locale."
    - "VND uses Vietnamese formatting and USD uses US formatting from integer minor units per D-05."
  artifacts:
    - path: "src/catalog/market.ts"
      provides: "Validated market resolution"
      exports: ["MARKET_COOKIE", "suggestMarketFromCountry", "resolveActiveMarket"]
    - path: "src/catalog/money.ts"
      provides: "VND/USD integer money presentation"
      exports: ["formatMoney"]
    - path: "src/components/market-switcher.tsx"
      provides: "Visible VN/International control"
  key_links:
    - from: "src/proxy.ts"
      to: "src/catalog/market.ts"
      via: "initial x-vercel-ip-country suggestion"
      pattern: "x-vercel-ip-country|suggestMarketFromCountry"
    - from: "src/components/market-switcher.tsx"
      to: "src/catalog/market-actions.ts"
      via: "validated cookie action"
      pattern: "setActiveMarketAction"
---

<objective>
Deliver visible, persistent market selection and correct money formatting.

Purpose: Establish shopper market context independently from locale before catalog queries/pages consume it.
Output: Market resolver, cookie action, switcher, money formatter, and green unit/browser tests.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-market-aware-catalog/02-CONTEXT.md
@.planning/phases/02-market-aware-catalog/02-RESEARCH.md
@.planning/phases/02-market-aware-catalog/02-VALIDATION.md
@.planning/phases/01-secure-bilingual-foundation/01-02-SUMMARY.md
@.planning/phases/01-secure-bilingual-foundation/01-03-SUMMARY.md
@src/proxy.ts
@src/components/site-header.tsx
</context>

## Phase Goal

**As a** store admin and shopper, **I want to** manage and browse bilingual products with market-specific availability, pricing, variants, and protected digital assets, **so that** Vietnam and international customers can browse the correct catalog before checkout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement market resolution, persistence, and money formatting</name>
  <files>src/catalog/market.ts, src/catalog/money.ts, src/catalog/market-actions.ts, src/proxy.ts, tests/unit/catalog/market.test.ts, tests/unit/catalog/money.test.ts</files>
  <behavior>
    - Valid cookie wins over country signal; `VN` suggests `vn`; all other/missing countries suggest `intl`.
    - Invalid cookie/input is rejected and replaced with a deterministic valid suggestion.
    - Market action accepts only `vn`/`intl` and redirects only to a safe localized relative path.
    - VND keeps whole units; USD converts minor cents before `Intl.NumberFormat`.
  </behavior>
  <action>Implement pure resolver/formatter helpers and a server action that writes `ACTIVE_MARKET` with `httpOnly`, `sameSite=lax`, path `/`, and production `secure`. Extend `src/proxy.ts` without dropping next-intl or Supabase auth cookies. IP/country is initial suggestion only per D-04; never infer market from locale. Do not accept browser price or exchange-rate data.</action>
  <verify><automated>npm run test:unit -- tests/unit/catalog/market.test.ts tests/unit/catalog/money.test.ts && npm run typecheck</automated></verify>
  <done>Market priority, safe persistence, and D-05 VND/USD formatting pass focused unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the visible bilingual market control</name>
  <files>src/components/market-switcher.tsx, src/components/site-header.tsx, src/messages/en.json, src/messages/vi.json, tests/e2e/catalog-market.spec.ts</files>
  <behavior>
    - Desktop and mobile header show the active market.
    - Switching market persists through refresh/navigation and does not change locale.
    - VN and non-VN first requests receive the expected initial suggestion.
    - Control has accessible names and localized labels.
  </behavior>
  <action>Add `MarketSwitcher` to both header layouts using the server action. Build the Playwright cases in the same task and drive them green. Keep tests focused on suggestion, visibility, persistence, locale independence, safe return path, and separate browser contexts; product assortment assertions are completed after Plan 02-06 supplies public data.</action>
  <verify><automated>npm run typecheck && npm run build && npm run test:e2e -- tests/e2e/catalog-market.spec.ts --grep "suggestion|visible|switch|locale|refresh"</automated></verify>
  <done>Shopper always sees a valid active market and can change it independently of language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Headers/cookies to market state | Both values are request-controlled and must be allowlisted. |
| Market action to redirect | Return path can become an open redirect if not constrained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Spoofing | market cookie/header | mitigate | Strict `vn/intl` allowlist and deterministic fallback. |
| T-02-05-02 | Spoofing | return path | mitigate | Known localized same-origin relative path validation. |
| T-02-05-03 | Tampering | money display | mitigate | Authoritative currency plus integer formatter only. |
</threat_model>

<verification>
Run focused market/money unit tests, typecheck/build, and green market-control browser cases.
</verification>

<success_criteria>
- IP is only an initial suggestion.
- Manual market choice persists and remains visible.
- Locale and market remain independent.
- VND/USD formatting is correct.
</success_criteria>

<output>Create `.planning/phases/02-market-aware-catalog/02-05-SUMMARY.md` when done.</output>
