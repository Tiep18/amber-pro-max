---
phase: 02-market-aware-catalog
plan: "08"
type: execute
wave: 6
depends_on: [02-03, 02-04, 02-06, 02-07]
files_modified:
  - src/messages/en.json
  - src/messages/vi.json
  - src/components/catalog/product-gallery.tsx
  - src/components/catalog/variant-selector.tsx
  - src/components/catalog/unavailable-market.tsx
  - src/app/[locale]/product/[productSlug]/page.tsx
  - src/app/[locale]/category/[categorySlug]/page.tsx
  - src/app/[locale]/collection/[collectionSlug]/page.tsx
  - tests/e2e/catalog-detail-seo.spec.ts
  - tests/security/catalog-boundaries.test.ts
autonomous: true
requirements: [MKT-02, MKT-03, MKT-04, CAT-03, CAT-05, CAT-07, CAT-08, SEO-01]
must_haves:
  truths:
    - "Product detail clearly distinguishes PDF pattern and finished handmade product per D-20 through D-22."
    - "Unavailable direct product URL remains visible but shows no active-market price or purchase action and offers a market switch when eligible elsewhere per D-03 and D-24."
    - "Only explicit in-stock variants are selectable; out-of-stock variants remain visible and disabled per D-25."
    - "Product/category/collection metadata uses localized SEO rows, canonical URLs, alternates, and public social images per D-11, D-13, D-23."
  artifacts:
    - path: "src/app/[locale]/product/[productSlug]/page.tsx"
      provides: "Market-aware product detail and metadata"
    - path: "src/components/catalog/variant-selector.tsx"
      provides: "Explicit variant selection and disabled out-of-stock states"
    - path: "tests/e2e/catalog-detail-seo.spec.ts"
      provides: "Green detail, unavailable, variant, and localized metadata evidence"
  key_links:
    - from: "src/app/[locale]/product/[productSlug]/page.tsx"
      to: "src/catalog/queries.ts"
      via: "localized market-aware detail query"
      pattern: "getCatalogProductBySlug"
    - from: "src/app/[locale]/product/[productSlug]/page.tsx"
      to: "src/i18n/routing.ts"
      via: "canonical and alternate URL generation"
      pattern: "generateMetadata|getPathname"
---

<objective>
Deliver product detail, unavailable-market behavior, variant interaction, and localized SEO metadata.

Purpose: Let shoppers understand exactly what is sold and whether it is available without leaking another market's price or private PDF data.
Output: Product gallery/detail states, variant selector, unavailable state, metadata generation, and focused E2E.
</objective>

<execution_context>
@C:/Users/Pay Oo/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/Pay Oo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-market-aware-catalog/02-CONTEXT.md
@.planning/phases/02-market-aware-catalog/02-RESEARCH.md
@.planning/phases/02-market-aware-catalog/02-VALIDATION.md
@.planning/phases/02-market-aware-catalog/02-03-SUMMARY.md
@.planning/phases/02-market-aware-catalog/02-04-SUMMARY.md
@.planning/phases/02-market-aware-catalog/02-06-SUMMARY.md
@.planning/phases/02-market-aware-catalog/02-07-SUMMARY.md
</context>

## Phase Goal

**As a** store admin and shopper, **I want to** manage and browse bilingual products with market-specific availability, pricing, variants, and protected digital assets, **so that** Vietnam and international customers can browse the correct catalog before checkout.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build product detail, type-specific facts, and variant states</name>
  <files>src/messages/en.json, src/messages/vi.json, src/components/catalog/product-gallery.tsx, src/components/catalog/variant-selector.tsx, src/components/catalog/unavailable-market.tsx, src/app/[locale]/product/[productSlug]/page.tsx, tests/e2e/catalog-detail-seo.spec.ts</files>
  <behavior>
    - PDF detail shows type badge, explicit digital-pattern warning, difficulty, file, and language facts when available per D-20/D-21.
    - Physical detail shows finished-product badge, shipping note, explicit variants, effective price, and availability per D-20/D-22.
    - Only valid in-stock explicit variant IDs are selectable; out-of-stock remains visible-disabled per D-25.
    - Active-market unavailable detail returns 200/public content, no price/action, and conditional market switch per D-03/D-24.
  </behavior>
  <action>Build the server page from `getCatalogProductBySlug`, public image gallery, type-specific sections, and client-only selection state over server-validated variants. Do not add cart controls in Phase 2. Unavailable pages render no wrong-market price and no purchase action; use the existing market control only when another market is eligible. Public detail may show descriptive PDF metadata but never private bucket/path or download link. Build Playwright cases and implementation together.</action>
  <verify><automated>npm run typecheck && npm run build && npm run test:e2e -- tests/e2e/catalog-detail-seo.spec.ts --grep "PDF|physical|variant|unavailable"</automated></verify>
  <done>Both product types render correctly, unavailable detail is safe/useful, and invalid/out-of-stock variants cannot be selected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate localized product, category, and collection metadata</name>
  <files>src/app/[locale]/product/[productSlug]/page.tsx, src/app/[locale]/category/[categorySlug]/page.tsx, src/app/[locale]/collection/[collectionSlug]/page.tsx, tests/e2e/catalog-detail-seo.spec.ts</files>
  <behavior>
    - Each localized page emits its stored SEO title/description and public social image.
    - Canonical URL uses the current localized route and content slug.
    - `alternates.languages` links the corresponding Vietnamese and English route.
    - Unpublished/missing slug is not found; published unavailable product retains metadata.
  </behavior>
  <action>Implement `generateMetadata` from authoritative locale translation rows and the Plan 02-07 route contract. Generate canonical and language alternates from typed routing helpers, not arbitrary request host/query. Use public product-media social images. Do not implement Product structured data, sitemaps, or robots here because those belong to Phase 7 requirements SEO-02 through SEO-04. Complete exact head assertions in the focused E2E spec.</action>
  <verify><automated>npm run typecheck && npm run build && npm run test:e2e -- tests/e2e/catalog-detail-seo.spec.ts</automated></verify>
  <done>Localized product/category/collection metadata, canonical URLs, alternates, and social images pass browser assertions without exposing private PDFs.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce the Phase 2 security boundary and run the integrated phase gate</name>
  <files>tests/security/catalog-boundaries.test.ts</files>
  <action>Add a static security regression test proving Phase 2 exposes no customer PDF URL, entitlement creation, download email, or digital-fulfillment route, and that private PDF object paths are not rendered by public catalog pages. Run the repository's complete CI command from a clean local Supabase state after all Phase 2 implementation tasks are green. This task verifies the phase boundary only; it must not add checkout, payment, entitlement, or fulfillment implementation.</action>
  <verify><automated>npm run test:security && npm run ci</automated></verify>
  <done>The static boundary test passes and the complete Phase 2 CI gate succeeds without introducing pre-payment digital fulfillment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Slug/market to product detail | Shopper controls route and market context. |
| Admin-authored text to HTML/metadata | Content must remain plain encoded strings. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08-01 | Information Disclosure | unavailable detail/PDF | mitigate | No wrong-market price, private path, or download action. |
| T-02-08-02 | Tampering | variant selection | mitigate | Explicit server-projected IDs and disabled out-of-stock options. |
| T-02-08-03 | Spoofing | canonical/alternates | mitigate | Typed route generation from stored localized slugs. |
| T-02-08-04 | Tampering | admin content | mitigate | React/Metadata string encoding; no raw HTML rendering. |
</threat_model>

<verification>
Run typecheck/build, the complete green detail/SEO Playwright spec in both locales and markets, the static catalog-boundary security test, and the full CI gate.
</verification>

<success_criteria>
- Product detail distinguishes digital and physical products.
- Unavailable pages remain indexable/useful without a purchase path.
- Variant availability is accurate.
- Localized metadata and social images are correct.
- Static security checks prove Phase 2 contains no customer download or fulfillment-before-payment path, and the full CI gate passes.
</success_criteria>

<output>Create `.planning/phases/02-market-aware-catalog/02-08-SUMMARY.md` when done.</output>
