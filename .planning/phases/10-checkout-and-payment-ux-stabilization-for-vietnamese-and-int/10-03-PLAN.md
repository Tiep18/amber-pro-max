---
phase: 10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int
plan: "03"
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/checkout/shipping-address-ui.ts
  - src/components/ui/searchable-select.tsx
  - src/components/checkout/destination-form.tsx
  - src/components/checkout/checkout-page.tsx
  - src/components/checkout/contact-form.tsx
  - src/components/checkout/order-summary.tsx
  - src/components/checkout/discount-code-form.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/checkout/shipping-address-ui.test.ts
autonomous: true
requirements: [MKT-01, MKT-02, MKT-06, CART-03, CART-04, SHIP-03, SHIP-09, SHIP-10, SHIP-11, SHIP-12, SHIP-13, ACC-03, OPS-04]
must_haves:
  truths:
    - "A shopper can search localized countries, choose an official Vietnam Province/City and child Ward/Commune/Special zone, see readable US region labels, and submit only normalized stable codes/values."
    - "Blur reveals only the touched field, the first submit blocker receives focus, a valid same-tab draft hydrates safely, and signed-in save-address consent begins unchecked."
    - "On mobile, a shopper can open a near-top localized order summary that shows the same accepted quote, destination, discount, blocker, and total as the desktop rail without resetting any state."
    - "Desktop keeps one 380-400px sticky summary rail; mobile keeps one safe-area action dock, and inactive breakpoint duplicates are not focusable."
    - "Every essential destination, blocker, amount, and action string wraps at 375px/200% zoom, and all changed journey copy has bounded en/vi key parity."
    - "Discount success/error feedback stays local to the discount control and cannot overwrite submit, destination, or incident feedback."
  artifacts:
    - path: src/components/checkout/destination-form.tsx
      provides: Searchable destination, official Vietnam fields, field-scoped errors, and unchecked save-address interaction
    - path: src/components/checkout/order-summary.tsx
      provides: Reusable pure summary body rendered in controlled mobile disclosure and desktop rail
    - path: src/messages/en.json
      provides: Single remaining Phase 10 message-catalog owner for checkout, support, payment, VietQR, recovery, and success keys consumed by Plans 10-04 through 10-06
    - path: tests/unit/checkout/shipping-address-ui.test.ts
      provides: Localized search metadata and stable code option evidence; Plan 10-07 owns the consolidated Playwright/parity files
  key_links:
    - from: src/components/checkout/checkout-page.tsx
      to: src/components/checkout/order-summary.tsx
      via: one accepted quote/destination/discount/blocker view model
      pattern: "OrderSummary|Mobile.*Summary"
    - from: src/components/checkout/checkout-page.tsx
      to: src/checkout/editable-draft.ts
      via: reviewed hydrate/write/clear adapter plus explicit address-save action from Plan 10-02
      pattern: "readEditableDraft|writeEditableDraft|clearEditableDraft|save.*Address"
---

<objective>
Create the destination interaction, draft/save wiring, responsive checkout summary, and bounded-copy slice per D-01 through D-14.

Purpose: Put the order facts and complete disabled reason where mobile shoppers need them while preserving the existing desktop rail and one authoritative checkout state owner.
Output: Searchable localized destination controls, official VN interaction, field-scoped validation, draft/save consent wiring, a controlled mobile disclosure, shared summary body, disabled-child seam for Plan 10-04, isolated discount feedback, and the remaining bounded Phase 10 message catalog.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-CONTEXT.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-UI-SPEC.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-RESEARCH.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-VALIDATION.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-PATTERNS.md
@src/checkout/shipping-address-ui.ts
@src/checkout/editable-draft.ts
@src/account/address-actions.ts
@src/components/checkout/destination-form.tsx
@src/components/checkout/checkout-page.tsx
@src/components/checkout/contact-form.tsx
@src/components/checkout/order-summary.tsx
@src/components/checkout/discount-code-form.tsx
@src/messages/en.json
@src/messages/vi.json
@tests/unit/checkout/shipping-address-ui.test.ts

<interfaces>
- `CheckoutPage` owns `lifecycle.acceptedQuote`, `shippingAddress`, email validity, submit blockers, discount requotes, and material-change proposals; no responsive child may own a second copy.
- Plan 10-02 supplies pure official VN/mobile validation, strict `editable-draft` lifecycle functions, and an authenticated save action; this plan exclusively wires those contracts into checkout components.
- `getShippingCountryOptions(locale)` already yields `{code,label,searchText}` and `US_SHIPPING_REGION_OPTIONS` already provides labels; the new control submits stable codes while rendering localized labels.
- `OrderSummary` already accepts quote/payment/address/submit/discount props and `MobileCheckoutDock` consumes the same quote/disabled state; extract presentation, do not fork state.
- `DiscountCodeForm` already receives accepted quote, feedback revision, pending, and `onApply`; keep discount result feedback local to that component.
</interfaces>
</context>

<execution_scope>
The task-level file ownership lists below are authoritative. Do not modify, generate, or reassign any additional file; if execution proves another file is required, stop and return a blocking replan request instead of making an opportunistic deviation.
</execution_scope>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire searchable destinations, field-scoped validation, draft hydration, and explicit save consent</name>
  <files>src/checkout/shipping-address-ui.ts, src/components/ui/searchable-select.tsx, src/components/checkout/destination-form.tsx, src/components/checkout/checkout-page.tsx, tests/unit/checkout/shipping-address-ui.test.ts</files>
  <behavior>
    - Country search uses localized normalized `searchText`; US rows show localized name plus code while only stable normalized codes are submitted.
    - VN fields render Recipient, mobile, required official Province/City, required child Ward/Commune/Special zone, street, then optional legacy district; changing province clears an invalid ward and announces reselection.
    - Blurring one field reveals only that stable linked error; submit reveals all and focuses the first blocker without making browser validation authoritative.
    - Valid draft intent hydrates once without premature requote, persists only after interaction, clears only after known success, and never restores save consent; signed-in physical/mixed checkout alone shows an unchecked save-address control.
  </behavior>
  <action>Extend option tests first, then compose a project-owned searchable combobox from existing Popover/Input/Button primitives with accessible name, expanded/active-option/result semantics, arrow/Escape behavior, selection, and focus return; add no registry/dependency. Reuse `ShippingCountryOption.searchText`, preserve stable country/US codes, and render official VN pair options from Plan 10-02. Refactor DestinationForm from one form-wide touched flag to a field-key set/map with stable IDs, `aria-invalid`, `aria-describedby`/`aria-errormessage`, exact VN order, and optional district. In CheckoutPage, integrate Plan 10-02 draft functions only after interaction, clear only after known success, and render the signed-in-only existing Checkbox unchecked on every fresh checkout and excluded from storage. Invoke the authenticated save action only after server validation/known order success; show a sanitized non-blocking warning if it fails. Preserve international/US/digital-only, saved-address prefill precedence, quote lifecycle, material-change callbacks, guest checkout, and all D-07 through D-10/D-22/D-23 authority.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/vietnam-address.test.ts tests/unit/checkout/vietnam-phone.test.ts tests/unit/checkout/editable-draft.test.ts tests/unit/account/addresses.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>D-07 through D-10 and UI-SPEC assertions 3-6 are implemented through one component owner and compile against Plan 10-02's tested server/storage contracts; Plan 10-07 owns their bilingual browser matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render one summary model through mobile disclosure and desktop rail</name>
  <files>src/components/checkout/checkout-page.tsx, src/components/checkout/contact-form.tsx, src/components/checkout/destination-form.tsx, src/components/checkout/order-summary.tsx, src/components/checkout/discount-code-form.tsx</files>
  <behavior>
    - Below `lg`, DOM order is title/context, controlled summary disclosure, contact, destination, feedback, safe-area dock; at `lg`, the 380-400px sticky rail remains.
    - Expanded mobile summary shows the same accepted lines, shipping, discount, total, destination, derived payment, policies, and blocker as desktop.
    - Collapsing/reopening preserves every input and commercial presentation; inactive submit/sticky duplicates are unmounted/hidden/inert and untabbable.
    - Complete blocker/destination/CTA/amount text wraps without horizontal overflow at all five required viewports and 200% reflow.
    - Contact, destination/search, discount, save-consent, and submit components expose one consistent `disabled` contract that Plan 10-04 can drive from its explicit submit stage without reopening these child files.
  </behavior>
  <action>Extract a presentation-only summary body from `OrderSummary` and feed it a single immutable view model derived in `CheckoutPage` from the accepted quote, current address, discount, derived provider, and complete blocker. Render it inside a controlled near-top mobile disclosure and the existing desktop sticky Card; do not create a second form, total, quote, discount, destination, or submit model. Keep the mobile trigger at least 48px with `aria-expanded`/`aria-controls`, use current semantic colors/icons and restrained opacity/reduced-motion, and preserve one primary action per active region. Remove `truncate` from destination, blocker, total, and CTA text; allow two-line growing labels; reserve safe-area bottom space. In the same component pass, give ContactForm, DestinationForm/search, DiscountCodeForm, save consent, and active submit controls a single required `disabled`/read-only prop that actually locks their nested controls while preserving values. CheckoutPage may drive it from the existing pending state now; Plan 10-04 will add the explicit checking-total/creating-order stages by modifying CheckoutPage only. Preserve authoritative old-total-during-requote and explicit material acceptance per D-11/D-12/D-14.</action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <done>UI-SPEC assertion 7 is implemented with one state model and an explicit child-lock seam; Plan 10-07 owns the five-viewport/reflow/focus executable proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Isolate discount feedback and provision the remaining bounded Phase 10 message catalog</name>
  <files>src/components/checkout/discount-code-form.tsx, src/components/checkout/checkout-page.tsx, src/components/checkout/order-summary.tsx, src/components/checkout/destination-form.tsx, src/messages/en.json, src/messages/vi.json</files>
  <behavior>
    - Discount apply/remove feedback stays inside the discount region and survives unrelated summary disclosure toggles.
    - Destination, submit, incident, and discount feedback never clear or overwrite each other accidentally.
    - Disabled reasons enumerate all current corrections and remain visible/linked in desktop and mobile presentations.
    - Exact en/vi keys required by Plans 10-04 through 10-06 already exist in bounded `checkout.submit`, `support.contact`, `orders.status`, `payments.paypal`, `payments.vietqr`, recovery, and paid-success groups, leaving those plans as catalog consumers.
  </behavior>
  <action>Give DiscountCodeForm its own durable status/error region and request identity, fed by the existing authoritative requote result, so discount messages do not reuse checkout feedback revision in a way that clears submit/destination incidents. Complete bounded customer-language keys for the address, summary, submit stages/errors, support/contact/access denial, every payment state/recovery/timer, three-step VietQR/download/declaration, and verified-paid success contracts specified in the UI-SPEC. Plan 10-01 remains the earlier owner of its cart/PDP subtree; this plan is the sole owner for all remaining message additions, and Plans 10-04 through 10-06 consume those keys without editing `en.json`/`vi.json`. Keep exact en/vi parity by construction; Plan 10-07 owns the final recursive parity test. Preserve one authoritative server discount result/allocation and accepted quote, and keep all new copy free of forbidden internal terms per D-13.</action>
  <verify>
    <automated>npm run check:vi-diacritics &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <done>Discount feedback is isolated, complete blockers remain stable, and all remaining Phase 10 bilingual keys needed by Plans 10-04 through 10-06 are provisioned once for Plan 10-07 parity verification.</done>
</task>

</tasks>

<decision_coverage>
- D-01/D-02/D-03: This is slice 3 of seven and extends the current single CheckoutPage state owner without new dependencies or scope.
- D-07/D-08/D-09/D-10: Searchable localized destination fields, Plan 10-02 draft/save contracts, unchecked consent, field-scoped blur, and first-invalid focus have one component owner here.
- D-11: Desktop/mobile actions show one complete non-truncated reason; authoritative updating facts stay visible.
- D-12: The controlled top-mobile disclosure and current desktop sticky rail share one state.
- D-13: Bounded next-intl namespaces and customer-language replacements are implemented and parity-tested.
- D-14: Active-locale accessible names, 44/48px targets, breakpoint focus safety, wrapping, zoom/reflow, and reduced motion are enforced.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Checkout state owner -> responsive presentations | Children receive render-only facts and must not create alternate total, discount, destination, or provider authority. |
| Browser address/draft/save intent -> Plan 10-02 contracts | Component state and session data remain untrusted; official validation, authentication, quote, order, and persistence authority stay server-owned. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T10-05 | Tampering / Denial of Service | duplicate mobile/desktop state or hidden blocker | mitigate (ASVS V5) | One view model, active-breakpoint focus exclusion, stable linked blockers, and cross-viewport state-equality tests. |
| T10-SC | Tampering | package supply chain | avoid | Reuse current semantic disclosure/Button/Card primitives; no install or registry block is allowed. |
</threat_model>

<verification>
Run `npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run check:vi-diacritics &amp;&amp; npm run test:unit -- tests/unit/checkout/shipping-address-ui.test.ts tests/unit/checkout/vietnam-address.test.ts tests/unit/checkout/vietnam-phone.test.ts tests/unit/checkout/editable-draft.test.ts tests/unit/account/addresses.test.ts &amp;&amp; npm run test:security`. Plan 10-07 exclusively owns `checkout-ux.spec.ts` and the recursive Phase 10 parity test.
</verification>

<success_criteria>
- Mobile and desktop show the same current commercial facts and complete reason with one active primary action.
- Searchable destination, official VN fields, field-scoped validation, draft hydration, and unchecked save consent are wired to Plan 10-02's validated contracts.
- All remaining Phase 10 copy is bounded, bilingual, and customer-facing; Plan 10-07 performs the final recursive parity and five-viewport proof.
- Discount feedback is isolated, component locks are ready for Plan 10-04's explicit stages, and commercial authority remains server-owned.
</success_criteria>

<output>
Create `.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-03-SUMMARY.md` when done.
</output>
