---
phase: 10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int
plan: "06"
type: execute
wave: 6
depends_on: ["10-05"]
files_modified:
  - src/payments/vietqr/instructions.ts
  - src/components/payments/vietqr-instructions.tsx
  - src/components/payments/order-payment-page.tsx
  - src/app/[locale]/orders/[orderNumber]/qr/route.ts
  - src/i18n/routing.ts
  - tests/unit/payments/vietqr.test.ts
  - tests/security/payment-boundaries.test.mjs
autonomous: true
requirements: [INV-04, ORD-01, ORD-02, PAY-05, PAY-06, PAY-07, PAY-08, OPS-04]
must_haves:
  truths:
    - "A pending VietQR shopper sees three numbered steps, exact amount/reference/account manual fallbacks, accessible copy, and an authorized same-origin QR download even when the image fails."
    - "QR download authorizes the current guest/customer order before a fixed HTTPS VietQR fetch, rejects redirects/wrong MIME/oversize, returns private no-store attachment, and performs no state mutation."
    - "A transfer declaration only requests reconciliation and can never mark paid, upload a receipt, grant download access, affect inventory, or style the order as successful."
    - "Verified paid begins with Payment confirmed, confirmed facts, masked email, and relevant digital/physical next steps; private entitlement/download authorization is unchanged."
  artifacts:
    - path: src/app/[locale]/orders/[orderNumber]/qr/route.ts
      provides: Authorized bounded fixed-upstream VietQR attachment
    - path: src/components/payments/vietqr-instructions.tsx
      provides: Three-step transfer UI with download, copy, manual, and truthful declaration fallbacks
    - path: src/components/payments/order-payment-page.tsx
      provides: Verified-paid success-first composition using existing authorized fulfillment/download panels
  key_links:
    - from: src/app/[locale]/orders/[orderNumber]/qr/route.ts
      to: src/payments/queries.ts
      via: getAuthorizedOrderPayment using server user/guest proof
      pattern: "getAuthorizedOrderPayment|getGuestOrderAccessHashFromServer"
    - from: src/components/payments/order-payment-page.tsx
      to: src/components/fulfillment/download-panel.tsx
      via: server-verified paid/entitlement-gated rendering
      pattern: "status.isPaid|DownloadPanel|contactEmailMasked"
---

<objective>
Finish the VietQR transfer/download and verified-paid success slice per D-18 and D-20 through D-23.

Purpose: Give Vietnamese shoppers usable bank-transfer instructions and a safe QR attachment while keeping bank verification, paid state, inventory, and private downloads strictly server-authorized.
Output: Three-step bilingual VietQR UI consuming Plan 10-03 keys, bounded same-origin QR route, truthful declaration/manual fallbacks, success-first masked-email paid composition in Plan 10-05's authorized branch, and focused unit/security evidence.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-CONTEXT.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-UI-SPEC.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-RESEARCH.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-VALIDATION.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-PATTERNS.md
@src/payments/vietqr/instructions.ts
@src/payments/queries.ts
@src/components/payments/vietqr-instructions.tsx
@src/components/payments/order-payment-page.tsx
@src/app/api/downloads/route.ts
@tests/unit/payments/vietqr.test.ts
@tests/security/payment-boundaries.test.mjs

<interfaces>
- `buildQuickLinkUrl` already derives the exact `https://img.vietqr.io/image/...png` URL from server configuration, amount, and printable transfer reference; the new route reuses this pure builder without calling transition-recording `getVietQrInstructions`.
- `getAuthorizedOrderPayment` plus server-derived user and `getGuestOrderAccessHashFromServer(orderNumber)` is the current customer/guest order authorization seam; the QR route must use it before any external fetch.
- `declareVietQrTransferAction` currently records customer intent/reconciliation only; preserve its non-paid behavior and do not introduce receipt upload.
- `DownloadPanel` and `/api/downloads` already enforce entitlement/private signed-link access; paid composition only reorders/relabels authorized output.
- Plan 10-03 owns all VietQR/success message keys, Plan 10-04 owns the generic denial branch, and Plan 10-05 owns PaymentStatePanel plus the authorized state shell; this plan adds only VietQR and verified-paid content at their declared seams.
- Plan 10-04 already adds contact/guest-recovery localized routing. This plan's sequential `src/i18n/routing.ts` edit is additive only: add the authorized QR-download path/helper and preserve every existing/contact route unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reorganize VietQR into three truthful numbered steps with durable fallbacks</name>
  <files>src/payments/vietqr/instructions.ts, src/components/payments/vietqr-instructions.tsx, src/i18n/routing.ts, tests/unit/payments/vietqr.test.ts</files>
  <behavior>
    - Step 1 shows/scans 220px mobile/240px desktop QR and a localized 44px Download QR action with safe public-order filename.
    - Step 2 emphasizes exact formatted amount/reference and always exposes bank/account name/account number plus accessible copy/manual-selection fallbacks.
    - Step 3 explains seller confirmation; declaration feedback explicitly says it does not confirm payment and has no receipt upload/paid/download/inventory mutation.
    - QR load/copy/download failure preserves all manual facts and a recovery path; paid status removes instruction/declaration/recheck immediately.
  </behavior>
  <action>Write focused VietQR unit assertions first. Extend the existing component, copy helpers, and instruction result rather than creating a second payment surface. Render the exact three-step sequence using Plan 10-03's approved bilingual `payments.vietqr` keys, keep amount strongest, preserve printable reference validation and manual facts, add a localized same-origin download href/helper, and keep 44px copy/download/declaration actions with polite inline outcomes/manual selection fallback. Remove repeated fulfillment-lock messaging already owned by Plan 10-05's state page. The declaration remains intent/reconciliation only: no receipt field/upload, no paid mutation, no optimistic success styling, and no inventory/order/entitlement update per D-18/D-20/D-22/D-23.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/vietqr.test.ts &amp;&amp; npm run check:vi-diacritics &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>UI-SPEC assertion 14 presentation behaviors pass in both locales and manual transfer remains usable with no client paid authority or receipt scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add an authorized fixed-upstream bounded QR attachment route</name>
  <files>src/app/[locale]/orders/[orderNumber]/qr/route.ts, src/payments/vietqr/instructions.ts, src/i18n/routing.ts, tests/security/payment-boundaries.test.mjs</files>
  <behavior>
    - Missing/forbidden orders receive one generic non-enumerating denial before any external fetch; authorized owner/guest pending VND/VietQR orders may download.
    - Route accepts no upstream URL, account, amount, provider, token, or filename from query/body; it re-derives exact HTTPS `img.vietqr.io` `/image/` URL from authorized facts/server config.
    - Fetch uses `redirect: 'error'`, bounded timeout and 1MiB streaming byte cap, requires `image/png`, rejects non-2xx, and never logs the account-bearing URL.
    - Response sanitizes public order number into `vietqr-{order}.png`, sets `Content-Disposition: attachment`, `Cache-Control: private, no-store`, `X-Content-Type-Options: nosniff`, and performs zero payment/order/inventory/fulfillment mutations.
  </behavior>
  <action>Implement the localized route using Zod-validated `locale`/`orderNumber`, server Supabase client, server-derived auth, guest cookie hash, and `getAuthorizedOrderPayment`; do not copy the token-redeem semantics of `/api/orders/access`. Authorize and verify pending VND/VietQR facts before reusing pure `buildQuickLinkUrl`; do not call `getVietQrInstructions` because that can record a transition. Enforce exact URL protocol/hostname/path, no redirects, AbortController/timeout, 1MiB bounded stream read, exact `image/png`, sanitized filename, private/no-store/nosniff headers, and generic access/upstream errors. Add security source/runtime tests for authorization-before-fetch, no arbitrary URL, redirect/MIME/size/timeout rejection, no sensitive logging/cache, and absence of paid/order/inventory/entitlement mutation per ASVS V4/V5 and D-20/D-23.</action>
  <verify>
    <automated>node --test tests/security/payment-boundaries.test.mjs &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>T10-10 controls and the QR download portions of UI-SPEC assertion 14 pass for authorized/unauthorized, redirect, timeout, oversized, wrong-MIME, filename, cache, and no-mutation cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lead verified paid orders with masked-email success and relevant authorized next steps</name>
  <files>src/components/payments/order-payment-page.tsx, tests/security/payment-boundaries.test.mjs</files>
  <behavior>
    - Only server-projected `isPaid` renders Payment confirmed, confirmed total/time, and masked-email acknowledgement; return/declaration/timer/query cannot trigger it.
    - Digital, physical, and mixed paid orders show only relevant Pattern downloads and/or Shipping updates next steps, with no reservation deadline or duplicate locked/eligibility copy.
    - Download actions remain through existing entitlement-authorized panel/private route; tracking remains authorized; raw contact email, tokens, signed URLs, and internal terms never render.
  </behavior>
  <action>Modify only the verified-paid branch of Plan 10-05's authorized OrderPaymentPage composition: add the success semantic surface and CircleCheck, Plan 10-03's approved heading, confirmed facts, `contactEmailMasked`, then separate digital/physical next-step language and existing authorized DownloadPanel/PhysicalTrackingPanel. Preserve Plan 10-04's generic denial and Plan 10-05's non-paid/refund/recovery/deadline branches. Deduplicate paid fulfillment/download lock copy, never imply shipped before actual physical state, and remove pending controls from paid. Extend payment security assertions so the sole trigger is the verified server projection and the existing private entitlement/signed-link path is untouched per D-21/D-23; do not expose unmasked email or create a client download URL.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/payments/vietqr.test.ts &amp;&amp; node --test tests/security/payment-boundaries.test.mjs &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>UI-SPEC assertion 15 passes for digital/physical/mixed paid fixtures, T10-11 remains closed, and private entitlement/download authorization is unchanged.</done>
</task>

</tasks>

<decision_coverage>
- D-01/D-02/D-03: This is slice 6 of seven and extends current VietQR/paid seams only; no new provider, receipt upload, analytics, carrier, package, migration, or state-machine scope.
- D-18: Pending/paid composition has one next action/deadline and removes duplicate downstream messages.
- D-20: Three steps, safe QR download, manual fallbacks, and explicit no-receipt/no-paid declaration are delivered.
- D-21: Verified paid begins with confirmation, masked email, and relevant authorized next steps; guest proof/private downloads remain unchanged.
- D-22/D-23: VND/VietQR authority, verified admin transition, inventory, immutable order facts, and private entitlement remain server-owned.
- D-24: Terminal provider controls remain absent after Plan 10-05; this plan does not add retry.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Guest/customer request -> QR route -> external VietQR image | Order access is authorized before a fixed bounded external fetch; external bytes are untrusted. |
| Verified order/payment projection -> paid/download UI | Client cannot infer paid or bypass entitlement/private signed-link checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T10-10 | Spoofing / Information Disclosure / DoS | QR attachment | mitigate (ASVS V4/V5) | Server authorization, fixed exact host/path, no caller URL, redirect error, timeout/1MiB/MIME bounds, sanitized filename, private no-store, generic denial, no sensitive logging. |
| T10-11 | Tampering / Elevation of Privilege | paid success/private downloads | mitigate (ASVS V4/V6) | Server verified-paid projection only; declaration cannot mutate; reuse entitlement-authorized short-lived signed-link route and masked email. |
| T10-SC | Tampering | dependencies/schema | avoid | No package, storage bucket, migration, RLS policy, or receipt-upload subsystem is introduced. |
</threat_model>

<verification>
Run `npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run check:vi-diacritics &amp;&amp; npm run test:unit -- tests/unit/payments/vietqr.test.ts &amp;&amp; node --test tests/security/payment-boundaries.test.mjs`. Plan 10-07 exclusively owns payment-state fixtures and `payment-ux.spec.ts`.
</verification>

<success_criteria>
- Pending VietQR works as three numbered bilingual steps with safe download and complete manual/copy fallbacks.
- SSRF/authorization/redirect/timeout/size/MIME/cache/filename/logging/no-mutation controls pass.
- Verified paid alone renders success, masked email, relevant authorized next steps, no deadline, and unchanged private entitlement access.
</success_criteria>

<output>
Create `.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-06-SUMMARY.md` when done.
</output>
