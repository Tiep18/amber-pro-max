---
phase: 10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/catalog/add-to-cart.tsx
  - src/components/cart/cart-page.tsx
  - src/components/cart/cart-line.tsx
  - src/components/cart/mini-cart.tsx
  - src/messages/en.json
  - src/messages/vi.json
  - tests/unit/catalog/add-to-cart.test.ts
  - tests/e2e/cart.spec.ts
autonomous: true
requirements: [CART-01, CART-02, CART-03, CART-05, INV-03, OPS-04]
must_haves:
  truths:
    - "A shopper can change cart quantities through item-specific 44px controls and receives the same durable remove/Undo contract in the cart and mini-cart."
    - "Every unavailable PDP/cart action exposes one complete localized reason, while inactive sticky duplicates are absent from keyboard focus."
    - "Cart subtotal wording never implies shipping or a final payable total, and displayed eligibility/money remains sourced from the authoritative projection/quote."
  artifacts:
    - path: src/components/catalog/add-to-cart.tsx
      provides: Shared canonical blockedReason and inert/unmounted mobile sticky action
    - path: src/components/cart/cart-line.tsx
      provides: 44px item-specific quantity and remove controls
    - path: tests/e2e/cart.spec.ts
      provides: Plan-owned cart mutation, blocker, and Undo browser evidence; Plan 10-07 owns the consolidated responsive focus matrix
  key_links:
    - from: src/components/catalog/add-to-cart.tsx
      to: src/catalog/add-to-cart-eligibility.ts
      via: existing exact projection agreement and blockedReason
      pattern: "blockedReason|createAddToCartIntent"
    - from: src/components/cart/mini-cart.tsx
      to: src/components/cart/cart-provider.tsx
      via: shared removedLine and undoRemove state
      pattern: "removedLine|undoRemove"
---

<objective>
Stabilize the PDP, full cart, and mini-cart controls and feedback per D-01, D-02, D-03, D-11, D-13, and D-14 without changing commerce authority.

Purpose: Make the first purchase steps understandable and keyboard/touch safe while preserving the already-correct exact agreement, server quote, price, stock, and eligibility seams.
Output: Shared 44px quantity controls, complete linked blockers, inactive sticky focus safety, one remove/Undo behavior, precise subtotal copy, bounded bilingual messages, and owned automated coverage.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-CONTEXT.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-UI-SPEC.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-RESEARCH.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-VALIDATION.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-PATTERNS.md
@src/components/catalog/add-to-cart.tsx
@src/components/cart/cart-page.tsx
@src/components/cart/cart-line.tsx
@src/components/cart/mini-cart.tsx
@tests/unit/catalog/add-to-cart.test.ts
@tests/e2e/cart.spec.ts

<interfaces>
- `AddToCart` already derives the one canonical `blockedReason` from the exact `AddToCartAgreement`/projection and passes it to the main action through `aria-describedby`; retain that authority seam.
- `CartPageContent` already consumes `removedLine` and `undoRemove` from `useCart`; make `MiniCart` consume the same state instead of adding another undo stack.
- `CartLine` receives authoritative `CartQuoteLine` display facts plus intent-only `CartIntentLine`; UI changes must not calculate price, stock, or eligibility independently.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock PDP and cart accessibility contracts before changing presentation</name>
  <files>tests/unit/catalog/add-to-cart.test.ts, tests/e2e/cart.spec.ts, src/components/catalog/add-to-cart.tsx, src/components/cart/cart-line.tsx</files>
  <behavior>
    - The main and mobile sticky Add to Cart controls consume the same `blockedReason`; unavailable/stale/variant reasons are visible, complete, and programmatically linked.
    - When the sticky control is visually inactive, its subtree has no focusable element; when active, its localized accessible name and target are at least 44px.
    - Quantity decrease/increase and remove controls are at least 44px and include the affected item title in the active locale.
  </behavior>
  <action>Create the smallest failing unit/browser assertions first, then update the existing components per D-02, D-11, and D-14. Keep `createAddToCartIntent` and `blockedReason` as the only PDP eligibility predicate; conditionally unmount the inactive mobile sticky subtree (preferred over `aria-hidden` alone), remove truncation from its title/reason, and preserve the already-implemented durable inline Added state with View cart and Continue shopping. Raise every touched CartLine quantity/remove target from 40px to at least 44px, preserve affected-line-only mutation locking, item-specific accessible names, current focus ring, safe-area spacing, semantic tokens, four-size/two-weight typography contract, and reduced-motion behavior. Do not add a package, new registry component, or client stock/price calculation.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/catalog/add-to-cart.test.ts &amp;&amp; npm run test:e2e -- tests/e2e/cart.spec.ts --grep "PDP|cart accessibility|sticky"</automated>
  </verify>
  <done>UI-SPEC assertions 1 and the applicable target/focus portions of 16 pass at desktop and 375px, with authoritative eligibility unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify cart and mini-cart blocker, subtotal, mutation, and Undo behavior</name>
  <files>src/components/cart/cart-page.tsx, src/components/cart/mini-cart.tsx, src/components/cart/cart-line.tsx, tests/e2e/cart.spec.ts</files>
  <behavior>
    - Removing from either cart surface exposes the same one-level durable Undo state and restores through `undoRemove`.
    - Disabled checkout controls reference a complete blocker naming/counting affected authoritative quote lines; essential availability text wraps.
    - The displayed amount is explicitly a product/item subtotal and the shipping/final-total caveat is unambiguous.
  </behavior>
  <action>Extend the existing cart-provider removal state into MiniCart and render the same consistent localized Undo contract; do not invent a second stack or mutate order snapshots. Derive blocker count/names only from the current `selectCartDisplayLines`, quote status, and existing change sets, then give the full cart and mini-cart blocker stable IDs used by `aria-describedby` on disabled checkout buttons. Replace generic/truncated essential strings with wrapping copy and remove `truncate`/one-line clamping only where it affects availability, blocker, or action meaning. Rename subtotal presentation so it clearly covers products before shipping and never implies a final total. Preserve previous confirmed intent/amount presentation during requote, explicit material-change handling, affected-line locks, empty-cart catalog recovery, and server authority per D-11, D-13, D-14, CART-03, CART-05, and INV-03.</action>
  <verify>
    <automated>npm run test:e2e -- tests/e2e/cart.spec.ts --grep "undo|blocked|subtotal|quantity" &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>Both cart surfaces have consistent Undo, complete linked blockers, clear subtotal language, 44px controls, no essential truncation, and no new commerce authority.</done>
</task>

<task type="auto">
  <name>Task 3: Move only touched cart/PDP copy into bounded bilingual keys</name>
  <files>src/messages/en.json, src/messages/vi.json, src/components/catalog/add-to-cart.tsx, src/components/cart/cart-page.tsx, src/components/cart/cart-line.tsx, src/components/cart/mini-cart.tsx, tests/e2e/cart.spec.ts</files>
  <action>Add a bounded cart/PDP Phase 10 namespace with exact en/vi parity for blocked reasons, item-specific quantity/remove names, subtotal/shipping explanation, Undo, durable success, and sticky actions per D-13/D-14. Use customer language from the approved UI-SPEC and remove only the touched component-local bilingual strings; do not begin a site-wide message migration. Keep semantic roles stable so tests assert active-locale accessible names rather than fragile DOM classes.</action>
  <verify>
    <automated>npm run check:vi-diacritics &amp;&amp; npm run test:e2e -- tests/e2e/cart.spec.ts --grep "localized cart" &amp;&amp; npm run lint</automated>
  </verify>
  <done>All new/migrated Plan 10-01 customer copy has Vietnamese/English key parity and forbidden internal commerce terms are absent from touched UI.</done>
</task>

</tasks>

<decision_coverage>
- D-01: This is slice 1 of exactly seven executable plans.
- D-02: Existing canonical blocker, durable success, quote hydration, empty recovery, and cart mutation behavior are preserved and protected rather than re-planned.
- D-03: Only P0/P1 and low-risk P2 cart presentation work is included; no provider, carrier, analytics, or state-machine scope appears.
- D-11, D-13, D-14: Complete reasons, customer language, 44px targets, active-locale names, and hidden-sticky focus safety are implemented here.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser intent -> cart provider/server quote | Quantity/removal intent is untrusted; displayed availability and money come from the authoritative quote. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T10-01 | Tampering / Repudiation | PDP/cart checkout intent | mitigate (ASVS V5) | Reuse exact agreement and quote lines, lock only affected mutations, keep disabled checkout linked to authoritative blockers, and regression-test no client totals/eligibility override. |
| T10-SC | Tampering | package supply chain | avoid | This plan performs no package-manager install and changes neither package manifest nor lockfile. |
</threat_model>

<verification>
Run `npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run check:vi-diacritics &amp;&amp; npm run test:unit -- tests/unit/catalog/add-to-cart.test.ts &amp;&amp; npm run test:security &amp;&amp; npm run test:e2e -- tests/e2e/cart.spec.ts`. Plan 10-07 owns the additive five-viewport `checkout-ux.spec.ts` matrix, avoiding cross-plan test-file edits.
</verification>

<success_criteria>
- UI-SPEC assertion 1 is executable in both locales and inactive sticky controls never receive keyboard focus.
- All modified controls meet the 44px target, essential copy wraps at 375px/200% reflow, and durable feedback is not toast-only.
- Authoritative quote/eligibility/totals and exact mixed-cart semantics remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-01-SUMMARY.md` when done.
</output>
