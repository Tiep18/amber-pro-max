---
phase: 10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int
plan: "04"
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/app/[locale]/checkout/page.tsx
  - src/components/checkout/checkout-page.tsx
  - src/checkout/submit-error-copy.ts
  - src/support/config.ts
  - src/components/support/incident-reference.tsx
  - src/components/support/support-links.tsx
  - src/app/[locale]/contact/page.tsx
  - src/i18n/routing.ts
  - src/lib/env/server.ts
  - .env.example
  - src/components/payments/order-payment-page.tsx
  - tests/unit/support/config.test.ts
  - tests/security/checkout-boundaries.test.mjs
  - tests/security/payment-boundaries.test.mjs
autonomous: true
requirements: [MKT-01, CART-03, CART-04, CART-05, ACC-03, OPS-04]
must_haves:
  truths:
    - "During preflight and order creation every editable checkout control is locked under one aria-busy region without clearing values, and navigation occurs only after known success."
    - "Known validation/stale failures and unknown network outcomes present different truthful recovery; an unknown outcome never invites immediate resubmission."
    - "Safe opaque incident IDs are copyable and link to bilingual contact only when a validated email or Zalo channel exists."
    - "An order access denial is generic, non-enumerating, and offers localized guest-order recovery plus optional safe support."
  artifacts:
    - path: src/app/[locale]/checkout/page.tsx
      provides: Server-only support-config resolution and narrow DTO handoff to the client checkout tree
    - path: src/support/config.ts
      provides: Sanitized optional public support DTO and configurable store-time-zone policy
    - path: src/components/support/incident-reference.tsx
      provides: 44px labelled incident copy with polite result and contextual support
    - path: src/app/[locale]/contact/page.tsx
      provides: Bilingual server-rendered contact surface
    - path: tests/unit/support/config.test.ts
      provides: Absent/valid/malformed channel and timezone fallback contract
  key_links:
    - from: src/app/[locale]/checkout/page.tsx
      to: src/support/config.ts
      via: server-only getPublicSupportConfig resolution returning validated PublicSupportConfig or absence
      pattern: "getPublicSupportConfig|PublicSupportConfig"
    - from: src/app/[locale]/checkout/page.tsx
      to: src/components/checkout/checkout-page.tsx
      via: serializable publicSupportConfig prop only, forwarded to SupportLinks for conditional checkout-error recovery
      pattern: "publicSupportConfig|CheckoutPage"
    - from: src/components/checkout/checkout-page.tsx
      to: src/checkout/submit-error-copy.ts
      via: distinct known-versus-unknown presentation and first focus target
      pattern: "presentSubmitError|router.push|aria-busy"
    - from: src/components/checkout/checkout-page.tsx
      to: src/components/support/support-links.tsx
      via: PublicSupportConfig-or-absence prop forwarding without a server config import
      pattern: "publicSupportConfig|SupportLinks"
---

<objective>
Complete submit locking, honest error recovery, incident handling, centralized support/contact, and guest-order recovery per D-10, D-11, D-13, D-15, and D-16.

Purpose: Make checkout failures actionable without duplicate orders, leaked operational detail, invented support channels, or lost customer input.
Output: CheckoutPage-owned submit stages driving Plan 10-03's child lock seam, router navigation, first-error recovery, distinct uncertain-outcome copy, safe support/timezone config, copyable incidents, localized contact route, generic order denial recovery, and owned unit/security evidence.
</objective>

<execution_context>
@C:/Users/HNBV12714/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/HNBV12714/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-CONTEXT.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-UI-SPEC.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-RESEARCH.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-VALIDATION.md
@.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-PATTERNS.md
@src/app/[locale]/checkout/page.tsx
@src/components/checkout/checkout-page.tsx
@src/components/checkout/contact-form.tsx
@src/checkout/submit-error-copy.ts
@src/support/config.ts
@src/components/support/support-links.tsx
@src/lib/env/server.ts
@src/i18n/routing.ts
@src/app/[locale]/guest-order/page.tsx
@src/components/payments/order-payment-page.tsx
@tests/security/checkout-boundaries.test.mjs

<interfaces>
- Checkout currently preflight-requotes, mints/reuses an idempotency key, exchanges guest recovery server-side, writes a recovery snapshot, clears idempotency on success, and then uses `window.location.assign`; preserve all authority and replace only navigation/orchestration.
- Plan 10-03 already makes every editable child honor one `disabled` contract and provisions the submit/support/access message keys; this plan changes only CheckoutPage stage orchestration and consumes those keys.
- `presentSubmitError` already maps server codes to a safe variant/message/focus target; extend it rather than rendering raw RPC/provider errors.
- `getServerEnv` is the existing Zod server-config boundary. Project a separate narrow public support DTO; never send the full env object to clients.
- `src/app/[locale]/checkout/page.tsx` is the server component boundary above the client-only `CheckoutPage`; it must call the server-only support resolver and pass only validated `PublicSupportConfig` or absence as a serializable prop. Neither `CheckoutPage` nor `SupportLinks` may import `src/support/config.ts`, `src/lib/env/server.ts`, or any raw env/config object.
- `OrderPaymentPage` already gets an authorized server projection and generic denial; denial must not include requested order/provider facts.
</interfaces>
</context>

<execution_scope>
The task-level file ownership lists below are authoritative. Plan 10-04 owns exactly the 14 frontmatter files and no others; if execution proves another file is required, stop and return a blocking replan request instead of modifying, generating, or reassigning it opportunistically.
</execution_scope>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the editable checkout region and preserve idempotent honest submission</name>
  <files>src/components/checkout/checkout-page.tsx, src/checkout/submit-error-copy.ts, tests/security/checkout-boundaries.test.mjs</files>
  <behavior>
    - Activating submit moves through localized `checking-total` then `creating-order`, marks one form region `aria-busy`, and disables contact/address/search/discount/save-consent/submit without clearing displayed values.
    - Repeated activation cannot issue a duplicate request; preflight requote/material-change evidence and the idempotency key remain authoritative.
    - Known field/stale/unsupported failures preserve values and focus the owning field/alert; unknown outcome says do not submit again and points to order list or guest recovery.
    - Known success clears reviewed draft/idempotency at the safe point, completes only final ordered quantities, and calls localized `router.push` exactly once.
  </behavior>
  <action>Write security/source assertions first. Replace the one `submitting` presentation in CheckoutPage with an explicit checking-total/creating-order stage while retaining existing preflight requote, material proposal acceptance, guest HttpOnly recovery exchange, idempotency reuse, snapshot, atomic reservation call, and final line completion. Drive Plan 10-03's existing child `disabled` contract from that stage and wrap the region with `aria-busy`; do not reopen ContactForm, DestinationForm, or DiscountCodeForm in this plan, replace the form with a spinner, or clear values. Use `useRouter().push(result.orderPath)` only after the known successful server response. Extend `presentSubmitError` with distinct safe known-vs-unknown presentation and deterministic first field/alert focus using message keys already provisioned by Plan 10-03; an unknown outcome keeps key/draft and points to orders/guest recovery, not Try again. Preserve Plan 10-03 field-scoped touched behavior and D-10/D-11/D-13/D-23.</action>
  <verify>
    <automated>node --test tests/security/checkout-boundaries.test.mjs &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <done>UI-SPEC assertion 8 passes with locked visible values, no duplicate submission, authoritative preflight preserved, correct navigation, and honest recovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate public support/timezone config and publish the bilingual contact surface</name>
  <files>src/support/config.ts, src/components/support/support-links.tsx, src/app/[locale]/contact/page.tsx, src/i18n/routing.ts, src/lib/env/server.ts, .env.example, tests/unit/support/config.test.ts</files>
  <behavior>
    - Zero configured channels, email-only, Zalo-only, and both-channel inputs produce the exact conditional safe DTO/rendering contract; missing/malformed values expose no contextual dead link, and valid channels expose only safe href/label facts.
    - `STORE_TIME_ZONE` is configurable and validated; absent/invalid display configuration falls back to `Asia/Ho_Chi_Minh` while stored instants are untouched.
    - `/vi/lien-he` and `/en/contact` render a 720px server page with configured channel list, warning not to share credentials/access links, store and guest-recovery navigation, and neutral no-channel state.
  </behavior>
  <action>Create `support/config.ts` and its tests first. Extend the server env schema/example with optional `SUPPORT_EMAIL`, `SUPPORT_ZALO_URL`, and `STORE_TIME_ZONE`; never use `NEXT_PUBLIC_`. Validate email and HTTPS URL with exact allowed hostname `zalo.me`, and expose a server-only `getPublicSupportConfig` resolver that sanitizes to a narrow serializable `PublicSupportConfig` (`emailHref`, `zaloHref`, `hasChannels`, `storeTimeZone`) or absence. Test zero, email-only, Zalo-only, both-channel, and malformed-channel cases explicitly; omit malformed optional channels rather than exposing fabricated values or crashing the journey. Implement the resolved configurable timezone contract with `Asia/Ho_Chi_Minh` fallback; this changes formatting only, never stored timestamps. Add the `/contact` localized pathname/helper and server-rendered page consuming the exact bilingual support keys provisioned by Plan 10-03, with a semantic channel list, 44px actions, safe external-link name, no hard-coded business identity, and no secrets/full env DTO in client bundles per D-15/D-16.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/support/config.test.ts &amp;&amp; npm run check:vi-diacritics &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The optional email/Zalo and configurable store timezone contract is safe and bilingual; no fabricated channel, secret projection, dead contextual link, or stored-instant mutation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add copyable incident and non-enumerating contextual recovery</name>
  <files>src/app/[locale]/checkout/page.tsx, src/components/support/incident-reference.tsx, src/components/support/support-links.tsx, src/components/checkout/checkout-page.tsx, src/components/payments/order-payment-page.tsx, tests/security/checkout-boundaries.test.mjs, tests/security/payment-boundaries.test.mjs</files>
  <behavior>
    - An opaque incident ID renders as text with a localized 44px Copy incident action, polite success, and manual-selection fallback; URLs/logs contain no incident internals or customer/payment data.
    - Checkout errors needing help show Contact support only when at least one safe channel exists.
    - The checkout server page resolves `PublicSupportConfig` or absence and passes that prop through client `CheckoutPage` to `SupportLinks`; zero/one/both channels render conditionally, and no full env object, raw config, credential, secret, or unvalidated URL is client-reachable.
    - Unauthorized/missing order states use identical generic content, primarily link to localized guest-order recovery, optionally link to contact, and never echo order number/provider/amount/existence.
  </behavior>
  <action>Build a reusable IncidentReference from the current safe `errorId` only; never render or URL-encode request payloads, email/address, tokens, provider facts, bank details, idempotency facts, or raw errors. At the existing server component `src/app/[locale]/checkout/page.tsx`, call the server-only `getPublicSupportConfig` resolver and pass only validated `PublicSupportConfig` or absence into the client `CheckoutPage`; add the narrow prop to `CheckoutPage` and forward it unchanged to `SupportLinks` for durable checkout-error Alerts. Explicitly forbid any client component from importing `src/support/config.ts`, `src/lib/env/server.ts`, `process.env`, a full env object, or raw/unvalidated support values. Extend `tests/security/checkout-boundaries.test.mjs` with source/import and prop-shape assertions proving that legal server-to-client seam and conditional zero/one/both-channel rendering, and proving credentials, secrets, full/raw config, and unvalidated URLs cannot enter the checkout client bundle. Extend the current server-rendered order denial with localized guest-order recovery as primary and optional contact as secondary, keeping identical generic denial for not-found/forbidden and consuming Plan 10-03 message keys. Use active-locale accessible names, a stable polite result region, manual copy fallback, 44px targets, and no toast-only feedback. This plan is the additive access-denial owner of OrderPaymentPage; Plan 10-05 may later change only the authorized-state composition and must preserve this denial branch verbatim.</action>
  <verify>
    <automated>node --test tests/security/checkout-boundaries.test.mjs tests/security/payment-boundaries.test.mjs &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <done>UI-SPEC assertions 9-10 pass in both locales with safe copy, conditional support, primary guest recovery, and non-enumerating denial.</done>
</task>

</tasks>

<decision_coverage>
- D-01/D-02/D-03: This is slice 4 of seven, extending current idempotency/error/access seams without a new provider, state machine, package, table, or analytics surface.
- D-10/D-11: Plan 10-03 owns per-field component interaction; this plan owns submit-level first focus, complete reason, locked values, and `aria-busy` stages through the declared CheckoutPage seam.
- D-13: Submit/error/support/access copy is bounded bilingual customer language.
- D-15: Central optional safe support config and bilingual localized contact are created here.
- D-16: Incident copy, conditional contextual support, and localized guest-order recovery are implemented here.
- D-22/D-23: Submit still requotes/reconstructs, preserves payment pair/idempotency/reservation/snapshots, and navigates only after server success.
</decision_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser submit -> checkout server/RPC | Duplicate, stale, malformed, and unknown-outcome requests cannot establish commerce facts. |
| Server env/operational error -> public UI | Only validated optional channel/timezone and opaque customer-safe incident IDs may cross. |
| Unauthenticated order request -> denial/recovery | Response must not enumerate order existence or leak provider/customer facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T10-06 | Tampering / Repudiation | submit lifecycle | mitigate (ASVS V3/V5) | Full form lock, idempotency reuse, authoritative preflight, unknown-outcome no-resubmit copy, and server-success-only navigation. |
| T10-07 | Information Disclosure / Spoofing | support config and incident/access errors | mitigate (ASVS V4/V5) | Server-page-only resolver, validated narrow DTO/absence prop, exact Zalo host, client import/bundle source gate, no fabricated values/secrets/sensitive URLs, opaque ID only, and generic denial. |
| T10-SC | Tampering | package/schema supply chain | avoid | No package, support table, migration, or RLS change is allowed. |
</threat_model>

<verification>
Run `npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run check:vi-diacritics &amp;&amp; npm run test:unit -- tests/unit/support/config.test.ts &amp;&amp; node --test tests/security/checkout-boundaries.test.mjs tests/security/payment-boundaries.test.mjs`. The unit/security evidence must cover zero/email-only/Zalo-only/both support rendering plus the server-page-only DTO handoff and forbidden client imports/full-env/raw-config/credential/unvalidated-URL bundle paths. Plan 10-07 exclusively owns the additive checkout/payment Playwright specifications and final message-parity consolidation.
</verification>

<success_criteria>
- Order creation locks all editable controls, preserves values and idempotency, distinguishes known/unknown outcomes, and routes only after success.
- Optional support/timezone config is validated server-side and reaches checkout only through the localized server page's narrow DTO-or-absence prop; zero/one/both channels, bilingual contact/access recovery, and safely copyable incidents are proven.
- No raw errors, secrets, credentials, PII, provider facts, or internal identifiers enter public messages, URLs, client config, or logs.
</success_criteria>

<output>
Create `.planning/phases/10-checkout-and-payment-ux-stabilization-for-vietnamese-and-int/10-04-SUMMARY.md` when done.
</output>
