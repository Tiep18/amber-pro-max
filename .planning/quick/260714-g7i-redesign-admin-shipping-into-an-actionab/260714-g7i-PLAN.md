---
quick_id: 260714-g7i
status: complete
created_at: '2026-07-14T11:40:13+07:00'
description: Redesign admin shipping into an actionable package-rate workspace with safe edit/reactivate flows, accurate readiness, accessible forms, aligned tests, and optimized server loading
must_haves:
  truths:
    - Admin can create and edit package types, destination fees, and US adjustments without database access.
    - Admin can reactivate inactive package types and safely change the store default with explicit impact copy.
    - Readiness reflects the active default and every active profile that has product or variant assignments, not unrelated global rules.
    - Package types and their VN, US, fallback, assignment, and status state can be compared from one responsive workspace.
    - Forms expose field-level recovery, keyboard focus, 44px targets, and safe dismissal behavior.
  artifacts:
    - src/app/admin/shipping/page.tsx
    - src/components/admin/commerce/shipping-management.tsx
    - src/components/admin/commerce/shipping-profile-form.tsx
    - src/components/admin/commerce/shipping-rule-sheet.tsx
    - src/components/admin/commerce/shipping-region-adjustment-sheet.tsx
    - src/checkout/admin-shipping-actions.ts
    - tests/unit/checkout/admin-commerce-actions.test.ts
    - tests/e2e/admin-shipping.spec.ts
  key_links:
    - Shipping readiness uses the same profile-first rule hierarchy as src/checkout/shipping-resolution.ts.
    - Edit sheets pass profileId, ruleId, or adjustmentId into the existing validated server actions.
    - Package assignment counts and links remain consistent with product and variant assignment actions.
---

# Quick Task 260714-g7i: Actionable admin shipping workspace

## Goal

Turn `/admin/shipping` from a mostly read-only dashboard into the primary operational workspace for reviewing and safely changing package-specific shipping rates. Preserve the Phase 08 resolver, database schema, market currencies, and catalog assignment behavior.

## Task 1 — Complete safe shipping mutation flows

**Files**

- `src/checkout/admin-shipping-actions.ts`
- `src/components/admin/commerce/shipping-profile-form.tsx`
- `src/components/admin/commerce/shipping-create-sheet.tsx`
- `src/components/admin/commerce/shipping-rule-sheet.tsx`
- `src/components/admin/commerce/shipping-region-adjustment-sheet.tsx`
- `src/components/admin/commerce/deactivate-shipping-profile-button.tsx`

**Action**

- Reuse the existing save actions for create/edit by passing stable record IDs and initial values.
- Add package edit/reactivate, destination-fee edit/disable/enable, and US-adjustment edit/disable/enable controls.
- Replace immediate default switching with an accessible confirmation dialog that names old/new profiles and assignment impact.
- Keep destructive or availability-changing actions separated from normal edits and preserve server-side admin authorization and Zod validation.
- Add field-level validation/recovery, explicit currency context, pending states, and dirty-sheet dismissal protection without adding dependencies.

**Verify**

- `npm.cmd run typecheck`
- `npm.cmd run test:unit -- --run tests/unit/checkout/admin-commerce-actions.test.ts`

**Done**

- Existing configurations can be corrected from the UI, inactive package types can be restored, and no high-impact default/status change happens without clear impact feedback.

## Task 2 — Replace summary-card sprawl with a package-rate workspace

**Files**

- `src/app/admin/shipping/page.tsx`
- `src/components/admin/commerce/shipping-management.tsx`
- `src/components/ui/input.tsx`
- `src/components/ui/sheet.tsx`

**Action**

- Load independent shipping datasets in parallel and pass only the operational fields needed by the client workspace.
- Replace duplicated metric/readiness cards and disconnected lists with one compact, responsive package-rate matrix/list showing default state, assignments, VN fee, US fee, fallback fee, custom-destination count, status, and stable row actions.
- Keep US adjustments associated with their parent US rate and expose full state/territory detail plus an edit action.
- Compute blocking readiness per effective profile: active default plus active profiles with assignments; show actionable blocker text and links/buttons for missing coverage.
- Remove the hidden cross-section currency filter coupling; preserve filters in URL-compatible/local section state only where they are visible.
- Maintain a stacked mobile layout with no horizontal viewport scrolling at 375px and no loss of fee/status/action information.

**Verify**

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- Browser check at 1280px and 375px: no viewport overflow; every record exposes Edit and availability actions; keyboard focus is visible.

**Done**

- An admin can answer “which package applies, what does it cost for VN/US/other, is it ready, and how do I change it?” from one scan without navigating through four unrelated sections.

## Task 3 — Align regression coverage and verification

**Files**

- `tests/unit/checkout/admin-commerce-actions.test.ts`
- `tests/e2e/admin-shipping.spec.ts`
- `tests/security/shipping-ui-boundaries.test.mjs`

**Action**

- Add unit coverage for update/reactivation/status mutation branches and duplicate/invalid recovery contracts.
- Rewrite stale Playwright selectors and flows around the package-rate workspace, including edit, default confirmation, reactivation, mobile target size, and no horizontal overflow.
- Extend static security/UI boundaries only where required to protect server authorization and prevent sensitive output.

**Verify**

- `npm.cmd run test:unit`
- `npm.cmd run test:security`
- `npm.cmd run build`
- `npm.cmd run test:e2e -- tests/e2e/admin-shipping.spec.ts`

**Done**

- Automated coverage matches current copy and interactions, focused shipping tests pass, and the production build succeeds.

## Boundaries

- No database migration or shipping resolver algorithm changes.
- No carrier API, label purchasing, order fulfillment, or customer checkout redesign.
- Admin copy remains English and customer-facing localization files remain untouched.
- Preserve the user's existing `.gitignore` modification and unrelated working-tree changes.

## Recommended execution order

Task 1 → Task 2 → Task 3. Mutation completeness must exist before the matrix can expose reliable row actions; characterization coverage follows the final interaction contract.
