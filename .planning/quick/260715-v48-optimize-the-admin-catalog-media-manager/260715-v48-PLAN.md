---
quick_id: 260715-v48
status: planned
created_at: '2026-07-15T19:00:00+07:00'
description: Optimize the admin catalog media manager into a safe, actionable, responsive gallery workspace with atomic ordering and verified admin flows
must_haves:
  truths:
    - Admin can understand image count, primary-image readiness, Vietnamese/English social-image readiness, and private-PDF readiness without opening each media record.
    - Existing images render as a responsive gallery; metadata is edited in one focused inspector instead of every card expanding into a full form.
    - Image order can be changed with keyboard- and touch-accessible controls, and every accepted reorder commits the complete exact order atomically without transient unique-order collisions or cross-product IDs.
    - Removing an image or PDF and replacing an existing PDF always requires explicit confirmation that names the affected role and published-product consequence before the mutation starts.
    - Upload, metadata, role, reorder, remove, and PDF operations identify their own pending state and return specific, actionable success, warning, validation, retry, or stale-record feedback.
    - Image and PDF format/size limits are visible before selection, invalid client selections are rejected before upload, and the server remains authoritative for MIME, suffix, byte-size, ownership, and admin validation.
    - The gallery remains usable at 375px with no viewport overflow or clipped actions, 44px interactive targets, visible keyboard focus, lazy non-primary image loading, and no public access to the private PDF.
    - Existing storage-first removal, sanitized monitoring, RLS, private-bucket, cache-invalidation, publish-invariant, and guest/download security contracts remain intact.
    - Baseline and after evidence use the same exact linked-remote product, three media rows, role assignments, PDF asset, viewport sizes, filenames, and scroll anchors; the run-owned fixture is then removed and its absence proved in a fail-closed finally path.
  artifacts:
    - supabase/migrations/20260715190000_atomic_catalog_media_reorder.sql
    - supabase/tests/database/02_atomic_catalog_media_reorder.test.sql
    - src/types/supabase.ts
    - src/catalog/media-schemas.ts
    - src/catalog/media-actions.ts
    - src/app/admin/catalog/[productId]/media/page.tsx
    - src/components/admin/catalog/media-manager.tsx
    - tests/unit/catalog/media-actions.test.ts
    - tests/e2e/admin-media.spec.ts
    - output/playwright/v48-baseline/
    - output/playwright/v48-after/
  key_links:
    - MediaManager move controls derive one ordered media-ID array from the current gallery and pass it to reorderProductMediaAction, which validates it and invokes public.admin_reorder_product_media in one database transaction.
    - public.admin_reorder_product_media locks and validates the target product media set, temporarily shifts existing nonnegative orders, then writes contiguous zero-based positions before commit; the unique product/order constraint is never worked around in the browser.
    - The media page projects product status, product type, role ownership, and private-asset metadata into readiness and confirmation copy without exposing a private bucket path or signed PDF URL.
    - Inspector forms update only localized alt text; ordering is owned solely by the reorder action so metadata saves cannot collide with another image position.
    - Destructive and replacement dialogs call the existing storage-safe actions only after confirmation, and action result codes map to contextual UI recovery text without including raw database/storage/provider details.
    - A temporary exact-ID fixture journal connects the linked-remote baseline and after sessions; it contains no password or Supabase key and is deleted only after exact role/user/product/child-row/storage absence checks pass.
---

# Quick Task 260715-v48: Optimize the admin catalog media manager

## Goal

Replace the current sequence of always-expanded image forms with an operational media workspace: readiness at a glance, an upload surface with honest constraints, a responsive role-aware gallery, one metadata inspector, atomic accessible ordering, and a distinct protected-PDF panel. Preserve the existing catalog, storage, monitoring, and publish security boundaries; use existing Radix/shadcn-style primitives and `lucide-react`, with no new runtime dependency.

## Linked-remote visual evidence and fixture protocol

The baseline/after comparison uses the **linked remote Supabase project**, not local Supabase. Both UI versions run as production builds on one isolated, recorded local port (for example `3210`) with `.env.local`; port 3000 and unrelated processes remain untouched. Task 2 captures the old-media-manager baseline before editing that component. Task 3 pushes the already-tested reorder migration, rebuilds the optimized component, captures the matching after evidence against the same fixture, and executes cleanup in `finally`. Any abort after fixture creation must enter the same cleanup path; if absence cannot be proved, retain the journal and mark the quick task blocked rather than claiming completion.

### Fail-closed target and credential preflight

- Load exactly `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY`, and `SUPABASE_SECRET_KEY` from `.env.local` in the server-side launcher without echoing values, serializing them, forwarding them to the browser, or placing them in a command argument. Require all three to be nonempty.
- Parse the URL and require HTTPS plus a non-local host: reject `localhost`, `127.0.0.1`, `::1`, `.local`, or any URL that does not resolve to a Supabase project host. Read the linked ref from `supabase/.temp/project-ref` without printing secrets and require it to equal the project ref derived from `NEXT_PUBLIC_SUPABASE_URL`; stop before mutation on mismatch or a missing link.
- Treat `SUPABASE_SECRET_KEY` as opaque. Never decode it, test JWT claims, alias it to `SUPABASE_SERVICE_ROLE_KEY`, or manually send it as `Authorization: Bearer`. First make a zero-row `products?select=id&limit=0` REST request with the secret only in the `apikey` header. Continue only on the expected successful status and response shape.
- Use the same `apikey`-only server-side transport for exact REST, Storage, and Auth Admin fixture requests. Create the confirmed Auth user with `POST /auth/v1/admin/users` plus `email_confirm: true`; if the remote Auth endpoint rejects opaque `apikey` authentication with 401/403 or does not return one UUID, fail closed and do not fall back to a legacy bearer JWT. The browser receives only the ephemeral email/password through the sign-in UI; the password remains process-memory-only and is never written to the journal, screenshots, traces, or logs.

### One exact run-owned fixture and journal

- Generate one high-entropy `runId`, email, and password. Immediately create `output/playwright/v48-fixture-journal.json` as an untracked recovery file containing only `runId`, non-secret target host/project ref, Auth `userId`, role key (`userId` + `admin`), `productId`, three `mediaIds`, and four exact Storage object paths. Write each returned identifier/path to the journal immediately after its successful creation; never store a password, publishable key, secret key, access token, cookie, or raw response.
- Create exactly one confirmed ephemeral Auth user, its exact `user_roles(user_id, role='admin')` row, and one draft `pdf_pattern` product with VI/EN translations and market offers. Seed exactly three 1x1 PNG Storage objects and `product_media` rows ordered `0,1,2`; media 1 is Primary, media 2 is Social VI, and media 3 is Social EN. Seed one tiny valid private PDF object plus its `product_digital_assets` row. Use run-scoped titles/slugs/object names and only IDs returned for this run.
- Keep that same product, media IDs/order, role assignments, asset, and object paths unchanged for the six matching baseline/after screenshots. Interactions that mutate order, alt text, roles, replacement, or deletion happen only after the matching after screenshots are captured; use the journal to target subsequent UAT and cleanup exactly.

### Stable screenshot contract

Capture the following matching names in both `output/playwright/v48-baseline/` and `output/playwright/v48-after/`:

| File                       | Viewport  | Stable state and anchor                                                                             |
| -------------------------- | --------- | --------------------------------------------------------------------------------------------------- |
| `01-workspace-desktop.png` | 1440x1000 | Page top, page header and first media-manager/upload content visible, scroll Y=0                    |
| `02-gallery-desktop.png`   | 1440x1000 | First gallery/media record aligned to the top content edge; all three run-owned images in DOM order |
| `03-pdf-desktop.png`       | 1440x1000 | Protected-PDF heading/asset aligned to the top content edge                                         |
| `04-workspace-mobile.png`  | 375x812   | Page top, scroll Y=0                                                                                |
| `05-gallery-mobile.png`    | 375x812   | First gallery/media record aligned below the admin header; same three IDs/order                     |
| `06-pdf-mobile.png`        | 375x812   | Protected-PDF heading/asset aligned below the admin header                                          |

After the six matching images exist, capture optimized-only interaction evidence in `v48-after/`: `07-inspector-desktop.png` (media 2 inspector), `08-remove-image-dialog-desktop.png` (media 1 role-impact dialog), `09-replace-pdf-dialog-desktop.png`, `10-inspector-mobile.png`, and `11-remove-pdf-dialog-mobile.png`. Disable animation for deterministic capture, wait for fonts/images/network idle, use the same browser color scheme and device scale, and record viewport/scroll/selected-media metadata in a sanitized `manifest.json` in each evidence directory.

### Exact cleanup and proof

- In `finally`, close the browser and recorded server PID; delete only the four journaled Storage paths, the exact product ID (allowing its catalog children to cascade), the exact `user_roles` key, and the exact Auth user ID. A missing exact object/row during retry counts as converged cleanup; no prefix, date-range, email-domain, bucket-wide, or other broad deletion is allowed.
- Prove every journaled Storage path returns absent/404; exact REST queries for the product, three media IDs, product translations, market offers, digital asset, and role return empty; and Auth Admin GET for the exact user returns absent/404. Only then delete `v48-fixture-journal.json`. If any delete or proof fails, keep the journal, scrub secrets from diagnostics, stop completion, and report the exact recovery step still required.

## Task 1 - Add an admin-authorized atomic media-order contract

**Files**

- `supabase/migrations/20260715190000_atomic_catalog_media_reorder.sql`
- `supabase/tests/database/02_atomic_catalog_media_reorder.test.sql`
- `src/types/supabase.ts`
- `src/catalog/media-schemas.ts`
- `src/catalog/media-actions.ts`
- `tests/unit/catalog/media-actions.test.ts`

**Action**

- Add `public.admin_reorder_product_media(p_product_id uuid, p_media_ids uuid[])` as a fixed-`search_path`, `security definer` RPC. Revoke execution from `public`/`anon`, grant only `authenticated`, and fail unless `private.is_admin()` is true.
- Lock the product and its media rows. Reject a missing product, null/duplicate IDs, foreign-product IDs, omitted current IDs, or extra IDs; the submitted array must be an exact permutation of the current media set. Shift existing `display_order` values to a collision-free nonnegative range, then assign the submitted IDs contiguous `0..n-1` positions by ordinality inside the same function transaction. A validation or write failure must leave every prior order unchanged.
- Add a Zod reorder input contract with bounded unique UUIDs and a `reorderProductMediaAction`. Keep `requireAdmin` in the action, call only the typed RPC, revalidate the product/editor/catalog caches on success, and return/monitor a stable sanitized `reorder_failed` result on database failure. Never include submitted alt text, object paths, filenames, raw SQL, or provider messages in monitoring facts or browser results.
- Remove `displayOrder` from `mediaDetailsInputSchema` and from `updateProductMediaDetailsAction`; metadata saves update only `alt_text_vi` and `alt_text_en`. Ordering must have one authority and no longer be a free-form number field.
- Regenerate `src/types/supabase.ts` from the reset local database so the RPC is called without casts or hand-edited type declarations.
- Add pgTAP coverage for function existence/config/privileges, non-admin denial, exact-set validation, cross-product rejection, successful contiguous reorder, and rollback/no-change on invalid input. Add Vitest coverage for schema rejection, RPC payload/order, success revalidation, sanitized failure monitoring, and the metadata action no longer writing `display_order`.

**Verify**

- `npm.cmd run db:reset`
- `npm.cmd run db:lint`
- `npm.cmd run db:test`
- `npm.cmd run db:types`
- `git diff --exit-code -- src/types/supabase.ts` after the generated type update is staged in the working diff (regenerate a second time and require no further change)
- `npm.cmd run test:unit -- tests/unit/catalog/media-actions.test.ts tests/unit/catalog/media-routes.test.ts`
- `npx.cmd eslint src/catalog/media-schemas.ts src/catalog/media-actions.ts tests/unit/catalog/media-actions.test.ts`
- `npm.cmd run typecheck`

**Done**

- Only an authenticated admin can submit an exact media permutation; valid orders become contiguous atomically, invalid/stale/cross-product submissions change nothing, the client has a typed action/result contract, and alt-text saves cannot mutate ordering.

## Task 2 - Build the readiness, upload, gallery, inspector, and protected-PDF workspace

**Files**

- `src/app/admin/catalog/[productId]/media/page.tsx`
- `src/components/admin/catalog/media-manager.tsx`
- `src/components/ui/confirmation-dialog.tsx` only if a generic accessible pending label or focus behavior is required by the implemented dialog; do not fork a media-only dialog
- `output/playwright/v48-baseline/`

**Action**

- Before changing `media-manager.tsx`, execute the linked-remote target/credential preflight, create and journal the single exact fixture, start the pre-change production build, and capture all six baseline files plus its sanitized manifest exactly as defined in the visual evidence protocol. Stop the recorded server PID but intentionally retain only the journaled fixture for matching after capture; on any failure or early exit, run exact cleanup immediately instead of leaving an unowned remote fixture.
- Keep the server page admin-only and its independent queries parallel. Pass `productStatus` plus the existing product type, ordered images, role ownership, and asset metadata; do not pass `object_path` for the private PDF or create a PDF URL. Continue deriving public image URLs server-side.
- Add a compact readiness strip showing image count and the required states: primary, Vietnamese social, English social, and private PDF only for `pdf_pattern` products. Use actionable ready/missing labels, a completed/required count, and current draft/published context; do not imply that media readiness is the complete product publish checklist.
- Replace the upload form presentation with a clear file-selection/drop surface that states JPG/PNG/WebP/GIF and 10 MB before selection, shows selected filename/size and a local preview, retains optional VI/EN alt inputs with 500-character client limits, and revokes object-preview URLs on replacement/unmount. Reject unsupported/oversized client selections with contextual copy but preserve all existing route/action validation as the authority. Keep a single-file upload in this task rather than adding a batch-upload dependency or protocol.
- Replace one expanded form per image with a responsive `sm`/desktop gallery. Each card shows a stable thumbnail, position, Primary/Social VI/Social EN status pills, an explicit Edit details trigger, accessible move-earlier/move-later controls, and a compact actions menu for role assignment/removal. Preserve DOM order as gallery order; disable only impossible/conflicting operations, identify the exact pending operation in labels, and announce reorder/result changes through an appropriate live region.
- Use the existing `Sheet` as the single VI/EN alt-text inspector. Keep the selected image stable by ID across refresh, enforce `maxLength={500}`, expose dirty/pending state clearly, and close only after a successful save. Do not hide role state or irreversible actions inside the metadata form.
- Reorder through the new action using the complete current ID array. Use explicit accessible move controls rather than a new drag/drop package: they work for keyboard, touch, and assistive technology, avoid sensor/overlay complexity, and still provide immediate gallery feedback followed by server refresh. If using optimistic order, restore the prior array and show recovery copy when the RPC rejects stale state.
- Add `loading="lazy"` and `decoding="async"` to non-primary thumbnails (the first/primary image may load eagerly), suitable responsive sizing/aspect containment, and card-level rendering containment only if it does not break focus/menus. No Next Image host/config change is required for these admin-only Supabase public thumbnails.
- Map every `MediaActionCode` plus cleanup warning to action-specific, non-sensitive copy. Show which operation is uploading/saving/assigning/moving/removing; do not freeze unrelated reading/inspection behind one generic `isPending`, and do not claim failure when a replacement succeeded with old-object cleanup warning.
- Require `ConfirmationDialog` for image removal, existing-PDF replacement, and PDF removal. Image copy lists Primary/Social VI/Social EN roles currently held and warns that published products automatically return to draft when a required role is removed; PDF copy states that replacement changes the protected customer-download file and removal makes a PDF pattern ineligible to remain published. Cancel performs no mutation, pending dialogs cannot dismiss, and focus returns safely.
- Redesign the PDF area as an asset panel. Empty state states PDF/50 MB/private requirements; the existing state shows Protected, filename, human-readable size, last-updated time, and a shortened/checksummed integrity label without a public link. Use `Upload private PDF` when empty and `Replace PDF` when present, with Remove visually separated as destructive. Physical products show a concise not-applicable state rather than an active uploader.
- Run every pre-after interaction that can mutate media against the reset **local** Supabase fixture only. The linked-remote journaled fixture remains read-only between baseline and the six matching Task 3 after captures; no upload, alt save, role change, reorder, replacement, or removal may target it early.

**Verify**

- `npx.cmd eslint src/components/admin/catalog/media-manager.tsx src/app/admin/catalog/[productId]/media/page.tsx src/components/ui/confirmation-dialog.tsx`
- `npm.cmd run typecheck`
- Baseline evidence check: all six named PNGs and `manifest.json` exist in `output/playwright/v48-baseline/`, record the specified viewports/anchors/media IDs, contain no secret or password, and use the one journaled linked-remote fixture.
- Keyboard-only component pass against local Supabase: select/upload, open/close inspector, move both directions, open role menu, cancel/confirm a destructive dialog, and return focus to the initiating control.
- Browser geometry at 1440x1000 and 375x812: `scrollWidth <= clientWidth`, no clipped card/menu/sheet/dialog content, changed controls are at least 44px in both dimensions where applicable, focus rings are visible, and long filename/alt/checksum content wraps rather than widening the viewport.
- Browser network/DOM check: offscreen non-primary images are lazy, no `pattern-pdfs` public/signed URL is rendered, and only the selected action enters a pending state.

**Done**

- Admin can scan readiness, upload with known constraints, manage roles/order from compact gallery cards, edit one image at a time in an inspector, and understand/confirm every destructive or replacement consequence on desktop and mobile without exposing private media; the traceable baseline is complete and its exact fixture is either journaled for immediate Task 3 matching captures or already cleaned after an abort.

## Task 3 - Lock regression coverage, apply the migration safely, and perform visual UAT

**Files**

- `tests/unit/catalog/media-actions.test.ts`
- `tests/e2e/admin-media.spec.ts`
- `output/playwright/v48-after/`

**Action**

- Extend the serial media Playwright fixture/helpers to read `display_order` and alt text, and keep exact created user/product/storage-object ownership for cleanup. Preserve the existing customer-denial and private-PDF assertions.
- Cover the admin path with at least three images: upload constraints/feedback, readiness transitions, inspector alt save, primary and both social assignments, move-earlier/move-later persistence after reload and contiguous database order, impossible-edge buttons, delete cancel/no-change, delete confirmation/role impact, PDF upload, existing-PDF replacement confirmation, cleanup warning/success presentation where deterministically mockable, and PDF remove cancel/confirm. Assert no raw bucket path/provider/database detail is rendered on errors.
- Add a 375px browser pass that opens the inspector and destructive dialog, checks no horizontal overflow, verifies key gallery/menu/dialog targets are at least 44px, and exercises reorder by keyboard. Keep selectors role/label based and update existing stale copy selectors to the final UI contract.
- Run the complete local database/unit/security/build gates before touching the linked project. Then run `npx.cmd supabase db push --linked --dry-run`; require the dry run to contain only the intended pending migration(s), stop on unexpected remote drift, and apply with `npx.cmd supabase db push --linked` only after local gates are green. Confirm `npx.cmd supabase migration list --linked` records `20260715190000` locally and remotely. Never rewrite historical migrations or paste `.env.local` secrets into commands/logs.
- Re-run the fail-closed linked-ref and opaque-key preflight, load the existing run journal without exposing its contents, and prove the exact user/role/product/media/asset/object set still exists before starting the optimized production build on the same isolated recorded port. Sign in through the UI with only the process-memory email/password; if the browser credentials are no longer available or any journaled fixture record differs, clean the exact journaled fixture and recreate the complete baseline/after cycle rather than substituting another product.
- Before any UAT mutation, capture the six matching after screenshots and sanitized manifest using the exact filenames, viewports, anchors, media IDs/order, and linked-remote fixture defined above. Then capture the five optimized-only inspector/dialog states and compare every baseline/after pair at original resolution.
- After matching evidence exists, exercise the journaled draft product through upload validation, role assignment, atomic reorder, inspector save, confirmation cancel/confirm, PDF replacement, and removal. Inspect console, failed requests, accessibility names, focus behavior, pending states, and layout geometry; iterate until there is no overlap, clipping, overflow, generic pending/error feedback, ambiguous destructive action, or private-path leakage.
- Wrap remote push verification, final browser work, and fixture teardown in the protocol's `finally`. Whether migration push, sign-in, capture, or UAT succeeds or fails, stop only the recorded PID and attempt exact object/product/role/user cleanup plus absence proof. Completion is blocked while `v48-fixture-journal.json` remains.

**Verify**

- `npm.cmd run lint`
- `npm.cmd run typecheck`
- `npm.cmd run test:unit -- tests/unit/catalog/media-actions.test.ts tests/unit/catalog/media-routes.test.ts`
- `npm.cmd run db:reset`
- `npm.cmd run db:lint`
- `npm.cmd run db:test`
- `npm.cmd run db:types` followed by `git diff --exit-code -- src/types/supabase.ts`
- `npm.cmd run test:security`
- `npm.cmd run build`
- `npm.cmd run test:e2e -- tests/e2e/admin-media.spec.ts`
- `npx.cmd supabase db push --linked --dry-run`, then approved `npx.cmd supabase db push --linked`, then `npx.cmd supabase migration list --linked`
- Authenticated production-browser UAT and original-resolution screenshot review at 1440x1000 and 375x812, with clean console/network and exact remote fixture cleanup verification
- Evidence trace check: both manifests reference the same linked ref, product ID, three media IDs/order, anchors, viewports, color scheme, and device scale; the six matching PNG names exist in both folders and the five optimized-only states exist only in `v48-after/`
- Cleanup proof: four exact Storage paths are absent, exact product/media/translation/offer/asset/role queries are empty, Auth Admin GET for the exact user is absent/404, and `output/playwright/v48-fixture-journal.json` no longer exists
- `git diff --check`
- `git status --short` contains only the planned source/migration/test/planning changes plus known user-owned changes and untracked `output/playwright/v48-*` evidence; `.env.local` remains unchanged and untracked

**Done**

- Local database, unit, security, type, lint, build, and focused E2E gates pass; the single new migration is confirmed on the linked remote; desktop/mobile visual and keyboard UAT prove the final workflow; private media and secrets stay out of browser output; and all run-owned remote fixtures are proven removed.

## Boundaries

- No batch uploader, drag/drop ordering dependency, image editor/cropper, automatic alt-text generation, CDN/storage policy change, PDF preview/download URL, or customer storefront redesign.
- Do not weaken `requireAdmin`, RLS, storage policies, request-size/MIME/suffix/actual-file-size checks, storage-first deletion, monitored-action sanitization, cache invalidation, or publish-invariant triggers.
- Do not rewrite existing migrations or manually edit generated Supabase types. The reorder RPC is the only new database capability.
- Do not make the private PDF bucket public, log checksums/object paths/secret keys as operational details, or put remote credentials in browser state, screenshots, source, plans, shell history, or committed artifacts.
- Preserve unrelated working-tree changes. Only exact task-owned remote fixtures and storage objects may be created or deleted.
- Admin copy remains English to match the existing admin surface; customer-facing localization files are out of scope.

## Recommended execution order

Task 1 -> Task 2 -> Task 3. Establish and test the only ordering authority first, build the workspace against that contract second, then run the full regression/remote/visual acceptance pipeline on the final implementation.
