---
quick_id: 260714-ij7
status: complete
created_at: '2026-07-14T13:20:38+07:00'
description: Polish admin shipping package cards, lock rate context, and fix Select interactions inside shared Sheets
must_haves:
  truths:
    - Each package reads as one clearly bounded record and cannot be visually confused with its neighbors.
    - Primary VN, US, and fallback rates are compact and comparable without oversized nested cards.
    - Package actions and overrides use concise, subordinate controls instead of repeated bulky disclosures.
    - A rate opened from a package and destination cannot be reassigned to another package or destination.
    - Selecting the current value in any Select inside a Sheet never dismisses the Sheet.
    - The workspace and all Sheets remain operable at 375px without horizontal overflow.
  artifacts:
    - src/components/ui/sheet.tsx
    - src/components/ui/select.tsx
    - src/components/admin/commerce/shipping-management.tsx
    - src/components/admin/commerce/shipping-rule-sheet.tsx
    - src/components/admin/commerce/shipping-region-adjustment-sheet.tsx
    - tests/e2e/admin-shipping.spec.ts
  key_links:
    - Shared Sheet outside-interaction handling recognizes portalled Select content.
    - Rate cells pass an immutable profile and destination context to ShippingRuleSheet.
    - Expanded override content repeats its parent package identity and stays inside that package card.
---

# Quick Task 260714-ij7: Polish admin shipping package UI and interaction context

## Task 1 - Fix shared Sheet context behavior

**Files**

- `src/components/ui/select.tsx`
- `src/components/ui/sheet.tsx`
- `src/components/admin/commerce/shipping-rule-sheet.tsx`
- `src/components/admin/commerce/shipping-region-adjustment-sheet.tsx`

**Action**

- Mark Select portal content and prevent Sheet dismissal when pointer/focus interaction belongs to that content.
- Render package, destination, and fixed currency as read-only context when a rule is opened from a package rate cell.
- Keep custom-country entry editable only when creating a new custom-country override; lock it while editing an existing rule.
- Lock the parent US rule when adding or editing a state adjustment from a package.

**Verify**

- `npm run typecheck`
- Browser: reselect a Select's current value and confirm its Sheet remains open.
- Browser: open a VN rate and confirm package/destination cannot be changed.

**Done**

- Sheet/Select interaction is stable across shared admin forms and contextual edits cannot move records across packages or destinations.

## Task 2 - Redesign each package as a compact bounded record

**Files**

- `src/components/admin/commerce/shipping-management.tsx`
- `src/components/admin/commerce/shipping-create-sheet.tsx`
- `src/components/admin/commerce/deactivate-shipping-profile-button.tsx`

**Action**

- Replace the continuous divided list with separated package cards, a tinted identity header, and a compact action group.
- Replace tall nested rate cards with a segmented three-column rate strip using aligned labels, amounts, status, and edit actions.
- Replace the long advanced disclosure with a concise Overrides footer showing country/state counts; repeat the package name inside the expanded panel.
- Preserve responsive stacking, semantic headings, visible focus, 44px targets, and no horizontal scrolling.

**Verify**

- `npm run lint`
- Visual review at 1440px, 768px, and 375px.

**Done**

- A scan clearly shows where one package starts and ends, while common rates stay compact and advanced data remains subordinate.

## Task 3 - Align regression coverage and production verification

**Files**

- `tests/e2e/admin-shipping.spec.ts`
- `tests/unit/checkout/admin-commerce-actions.test.ts`

**Action**

- Extend the admin shipping browser contract for locked rate context and current-value Select reselection.
- Update selectors for the compact package action and override controls.
- Exercise the live development UI with credentials sourced from `.env.local`, then remove temporary records.

**Verify**

- `npm run test:unit`
- `node --test tests/security/shipping-ui-boundaries.test.mjs`
- `npm run build`
- Targeted admin shipping browser flow or equivalent headless browser assertions against the existing server.

**Done**

- Automated and browser verification cover the reported regressions, and the production build succeeds.

## Boundaries

- No shipping schema, resolver precedence, checkout calculation, or catalog assignment behavior changes.
- No new UI dependency.
- Preserve the user's existing `.gitignore` modification.

## Execution notes

- Completed in code commits `c344c1dc` and `098fc7fd`.
- The equivalent browser flow was verified against the already-running development server with a temporary admin sourced from `.env.local`; the temporary admin was removed afterward.
- A separate `next build` was not started because the workspace's existing `next dev` process owns `.next`; lint, typecheck, unit, security, and live-browser checks passed.
