---
quick_id: 260711-j2o
status: complete
mode: quick
date: 2026-07-11
must_haves:
  truths:
    - Admins can scan, search, and select taxonomy records without rendering every edit form at once.
    - Bilingual editing exposes field-level validation and clear save/delete affordances on desktop and mobile.
    - Usage counts require a bounded number of reference queries and explain why deletion is blocked.
    - Catalog and blog taxonomy workflows retain admin authorization and CRUD behavior.
  artifacts:
    - src/components/admin/taxonomy/taxonomy-manager.tsx
    - src/admin/taxonomy-admin.ts
    - tests/e2e/admin-taxonomy.spec.ts
  key_links:
    - TaxonomyManager posts compatible FormData to taxonomy-actions server actions.
    - getTaxonomyTerms maps batched reference rows to each taxonomy term.
---

# Redesign admin taxonomy workspace

## Task 1: Build progressive master-detail taxonomy UI

**Files:** `src/components/admin/taxonomy/taxonomy-manager.tsx`

**Action:** Replace the repeated full-form layout with searchable section navigation, a term list, one active bilingual editor, create mode, readiness indicators, accessible field validation, usage details, and responsive sticky actions.

**Verify:** Run TypeScript and lint checks; inspect desktop and mobile behavior in Playwright.

**Done:** Only the selected term is editable, new-item mode is explicit, fields report actionable errors, and delete state explains usage.

## Task 2: Bound taxonomy usage queries

**Files:** `src/admin/taxonomy-admin.ts`

**Action:** Query each reference source once per section, aggregate counts by term, and expose a safe source breakdown for the UI.

**Verify:** Run TypeScript and focused tests.

**Done:** Query count scales with configured reference sources rather than term count and existing totals remain correct.

## Task 3: Cover core admin taxonomy flow

**Files:** `tests/e2e/admin-taxonomy.spec.ts`

**Action:** Add Playwright coverage for workspace navigation, search, validation, create/edit, and in-use delete messaging using local Supabase fixtures.

**Verify:** Run the focused Playwright spec when the local test stack is available, plus project typecheck/lint.

**Done:** The redesigned catalog taxonomy workflow has automated regression coverage without weakening authorization boundaries.
