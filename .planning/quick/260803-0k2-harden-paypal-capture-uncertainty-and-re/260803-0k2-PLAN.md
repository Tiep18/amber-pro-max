---
quick_id: 260803-0k2
status: complete
description: Harden PayPal capture uncertainty and reconciliation feedback
---

# Quick Task 260803-0k2 Plan

## Goal

Ensure every PayPal outcome shown after buyer approval is financially safe: transport failures never claim that no charge occurred, and reconciliation mismatches never masquerade as a late payment.

## Task 1: Harden provider and route outcomes

**Files:** `src/payments/paypal/client.ts`, `src/app/api/paypal/orders/[paypalOrderId]/capture/route.ts`, `src/payments/paypal-capture-outcome.ts`

**Action:** Convert failures after the capture request is issued into an uncertain result, defensively catch provider exceptions in the route, and return a distinct reconciliation status for mismatched provider facts.

**Verify:** Unit tests cover rejected transport promises, provider non-success responses, route recovery through GET, unresolved uncertainty, and reconciliation mismatch classification.

**Done:** No post-approval branch makes an unsupported assertion that money did not move.

## Task 2: Align customer feedback and controls

**Files:** `src/components/payments/paypal-buttons.tsx`, `src/components/payments/order-payment-page.tsx`, `src/messages/en.json`, `src/messages/vi.json`, `tests/unit/payments/paypal-buttons.test.ts`, `tests/unit/payments/paypal-client.test.ts`

**Action:** Add provider-reconciliation copy, remove unsafe no-charge wording, and hide the PayPal button while payment may already have moved.

**Verify:** Targeted payment tests, typecheck, lint, security tests, and production build pass.

**Done:** Customers receive distinct, truthful next steps and cannot immediately initiate another PayPal attempt while status is uncertain or under review.
