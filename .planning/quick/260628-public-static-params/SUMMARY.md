# Summary

Added static params for published blog posts and catalog collection pages so these public SEO pages are included in static generation with existing 5-minute revalidation. Also extended sitemap XML to include `lastmod` for product and blog URLs that already expose publication dates.
