---
quick_id: 260716-w20
status: complete
completed_at: '2026-07-16T23:44:27+07:00'
commits:
  - 4a641d1
---

# Quick task 260716-w20 summary

## Outcome

The admin variant workspace now uses forgiving numeric text controls for product stock, variant stock, and display order. Admins can type directly, temporarily clear or mistype a field without it becoming zero, recover with labelled step controls, use ArrowUp/ArrowDown, and apply stock-only `+5`/`+10` adjustments. Every invalid value has an actionable inline error and blocks save; valid values normalize on blur or save.

Custom prices are now authored in human units. VND accepts whole đồng with valid comma grouping, while USD accepts dollars with at most two decimal places. Exact digit/string parsing converts these values to the existing integer minor-unit payload without `parseFloat` or floating-point multiplication. Currency adornments, formatted effective-price previews, market modes, fallback behavior, inventory ownership, dirty/discard behavior, operation tokens, and the single aggregate variant save remain intact.

The responsive controls use explicit labels, spinbutton semantics, visible compound focus, 44px actions, boundary-disabled decrement buttons, and mobile-safe grids verified at 375px.

## Verification

- Focused ESLint: passed.
- `npm run typecheck`: passed after clearing stale generated `.next/dev` types.
- `npx vitest run tests/unit/catalog/variants.test.ts`: passed, 68 tests.
- `npm run db:reset`: passed against the configured local Supabase project.
- `npx playwright test tests/e2e/admin-variants.spec.ts --project=chromium`: passed, 2 tests, including desktop and 375x812 interactions and authoritative integer database assertions.
- `npm run build`: passed.
- `git diff --check` / cached diff check: passed.

The same-workspace port 3000 development process was stopped only for the configured Playwright port 3210 run, then restored successfully on port 3000. No dependency, migration, remote Supabase mutation, or server/database contract was added.
