---
quick_id: 260731-24q
description: Smooth catalog filtering transitions, preserve scroll, and keep sidebar facets stable without breaking SEO or ISR
status: complete
date: 2026-07-31
---

# Smooth catalog filtering transitions

## Task 1: Preserve navigation position

- Add `scroll={false}` to same-page catalog filter, product type, active chip, and clear links.
- Intercept search/sort GET forms with App Router navigation using `{scroll: false}` while retaining the native form as a no-JavaScript fallback.
- Keep URL state, Back/Forward behavior, focus visibility, and mobile Sheet close behavior.

## Task 2: Keep facets stable and animate only results

- Cache settled facet snapshots by locale, market, surface, context generation, and context version for client remounts.
- Keep known same-context facets on projection failure and show sidebar skeletons only for a true first/context load.
- Keep the slim pending rail and subtly animate facet counts instead of replacing the sidebar.
- Add reduced-motion-aware fade/translate transitions to the product result stage only; preserve the safe product skeleton barrier.

## Task 3: Verify

- Add unit coverage for cache identity and same-context failure behavior.
- Add Playwright coverage for scroll preservation, stable facets during delayed filter requests, and result-stage transition state.
- Run typecheck, lint, focused unit/browser tests, security boundaries, and production build.

## SEO/ISR boundary

Do not change catalog metadata, canonical URLs, JSON-LD, `dynamic = 'force-static'`, `revalidate = 300`, or server-rendered SEO products.
