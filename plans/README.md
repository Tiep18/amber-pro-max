# Implementation Plans

Generated by the improve skill on 2026-06-25. Reconciled through GSD quick execution and manual vetting against the live codebase.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Fix downloads cardinality and product scoping | P1 | S | - | DONE |
| 002  | Fix admin dashboard subscribers mismatch | P1 | S | - | DONE |
| 003  | Add digital entitlements owner index | P2 | S | - | REJECTED (no measured launch-path bottleneck) |
| 004  | Add wishlist product index | P2 | S | - | REJECTED (no measured launch-path bottleneck) |
| 005  | Update stale README | P3 | S | - | DONE |
| 006  | Introduce shadcn form management | P1 | M | - | DONE |
| 007  | Redesign account addresses UX with safer shadcn form flow | P1 | M | - | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` | `REJECTED`

## Dependency notes

- Plans `001`, `002`, and `005` were small enough to route through one GSD quick task: `260625-k3o`.
- Plans `003` and `004` were not executed because the current Phase 7 launch work has no verified evidence that either index is needed now.
- Plan `006` was executed in the current working tree with follow-up debug fixes for localized routing and guest-order server actions.
- Plan `007` was executed with source-level server actions unchanged; focused E2E was attempted but blocked by local Supabase `127.0.0.1:55431` being unavailable.

## Findings considered and rejected

- **N+1 query loading catalog products in cart quote (`src/checkout/quote.ts`)**: Not worth doing as the maximum number of items in a cart is small and the added complexity would not pay for itself.
- **Add `digital_entitlements` owner index**: Rejected during GSD reconciliation because the current Phase 7 launch path has no measured entitlement-library hotspot or failing verification tied to this query.
- **Add `wishlist_items.product_id` index**: Rejected during GSD reconciliation because no current launch-critical workflow demonstrates this as a real bottleneck.
